---
phase: 01-assessment-findings-register
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/FINDINGS.md
autonomous: true
requirements: [ASSESS-01]
must_haves:
  truths:
    - "A reader can go finding-by-finding from CONCERNS.md into FINDINGS.md and find a verdict for every concern"
    - "Every code-assessable verdict cites file:line evidence that resolves on current HEAD"
    - "The three live-state findings (RLS, pgvector, webhook) are present but marked PENDING with a Block reference"
    - "Net-new findings (if any) are flagged in a separate section routed to backlog, not added to milestone scope"
  artifacts:
    - path: ".planning/codebase/FINDINGS.md"
      provides: "Verdict register overlay on CONCERNS.md — summary table (26 rows) + per-finding detail sections"
      contains: "# Codebase Findings Register"
      min_lines: 120
  key_links:
    - from: ".planning/codebase/FINDINGS.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "each detail section title maps back to a CONCERNS ### finding title"
      pattern: "### F-\\d+"
---

<objective>
Author Pass A of the findings register — `.planning/codebase/FINDINGS.md` — a verdict overlay on every one of the 26 findings in `.planning/codebase/CONCERNS.md` (D-01, D-04). This plan finalizes ALL code-assessable verdicts immediately (D-10 partial-complete) so the parallel Phases 2/4/5 are unblocked, and leaves exactly three named sections in "PENDING — live-state" for the operator checkpoint (Plan 03) to fill.

Purpose: The Core Value requires every documented concern to be "fixed OR have a verified, deliberate disposition." This register is that disposition layer. It is the canonical_ref every later phase points to.

Output: `.planning/codebase/FINDINGS.md` with a 26-row summary table + 26 per-finding detail sections (23 FINAL, 3 PENDING) + a Net-new findings section.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-assessment-findings-register/01-CONTEXT.md
@.planning/phases/01-assessment-findings-register/01-RESEARCH.md
@.planning/phases/01-assessment-findings-register/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the FINDINGS.md skeleton — header + 26-row summary table</name>
  <read_first>
    - .planning/codebase/CONCERNS.md (the input — 26 `### ` findings across Tech Debt / Known Bugs / Security / Performance / Fragile Areas / Scaling Limits / Dependencies / Test Coverage Gaps)
    - .planning/phases/01-assessment-findings-register/01-PATTERNS.md (FINDINGS.md structure spec, lines 37-46; CONCERNS section map, lines 24-35)
    - .planning/phases/01-assessment-findings-register/01-RESEARCH.md (Per-Finding Verdict Pre-Assessment seeds, lines 278-303; Evidence-Depth Heuristic, lines 269-276)
    - .planning/REQUIREMENTS.md (requirement IDs + Out-of-Scope reasons for the out-of-scope verdicts)
  </read_first>
  <action>
    Create `.planning/codebase/FINDINGS.md` (NEW file — does not exist yet; use Write).

    Header (mirror CONCERNS.md convention per 01-PATTERNS.md line 38):
    - Title line: `# Codebase Findings Register`
    - `**Assessment Date:** 2026-06-18`
    - One-line pointer: a sentence stating this is the verdict overlay on `.planning/codebase/CONCERNS.md` (dated 2026-06-19), and that each F-NN maps back to a CONCERNS finding.
    - A 2-line legend: verdict vocabulary `confirmed / stale / already-resolved / out-of-scope` (D-06), and that "PENDING — live-state" rows are filled at the operator checkpoint (D-10).

    Then a `## Summary` section with ONE markdown table, exactly 26 data rows (one per CONCERNS `### ` finding, in CONCERNS document order). Column order (D-02, D-03; ordering is Claude's discretion — use this):
    `| ID | CONCERNS finding (title) | area | requirement ID | verdict | gated phase | scope impact |`

    Apply the D-05 evidence tiers: in-scope findings (the 21 reqs) get full assessment with evidence; pre-resolved web-portal findings → `already-resolved`; out-of-scope items (test-coverage gaps, web-portal-v2 dormant, scaling-limit/v2 perf notes) → `out-of-scope` with the REQUIREMENTS.md Out-of-Scope reason.

    Assign stable IDs `F-01` … `F-26` in CONCERNS document order. Map each row using the verified verdict seeds from 01-RESEARCH.md (lines 278-303). The 26 findings in CONCERNS order are:
    1. Ingredient Pipeline — Orphaned DB Schema → F-01, area Tech Debt, req DEBT-01/DEBT-02, verdict `confirmed`, gated Phase 6, scope impact "Phase 6 work stands (migration 151 authored; tables still in schema)"
    2. DishKind / DISH_KIND_META Shims → F-02, Tech Debt, DEBT-03, `confirmed`, Phase 6, "last importer is apps/web-portal-v2/.../KindSelector.tsx (web-portal gone, v2 remains)"
    3. apps/web-portal Onboarding-Only App → F-03, Tech Debt, (no req), `already-resolved`, Phase 5, "apps/web-portal deleted AND committed c1a7e3f 2026-06-18; Phase 5 = residual-doc cleanup only"
    4. Two Admin Codepaths → F-04, Tech Debt, (no req), `already-resolved`, (none), "web-portal deleted c1a7e3f; apps/admin is sole admin surface"
    5. apps/web-portal-v2 Dormant / On-Ice → F-05, Tech Debt, (no req), `out-of-scope`, (none), "on-ice, kept for revival (REQUIREMENTS Out-of-Scope); do NOT delete; blocks full DishKind shim removal until Phase 6 updates KindSelector"
    6. enrich-dish Stale Comment References → F-06, Tech Debt, CLEAN-03, `confirmed`, Phase 5, "header lines 8-21 reference retired ingredient/parent-dish pipeline"
    7. packages/database/src/types.ts Stale Generated Types → F-07, Tech Debt, DEBT-04, `confirmed`, Phase 6, "3226 lines; regen ONCE post-teardown (Phase 6), assess-only here"
    8. apps/web-portal Dish Create/Edit Broken → F-08, Known Bugs, (no req), `already-resolved`, (none), "app deleted c1a7e3f; broken-by-design path no longer exists"
    9. Map Restaurant View Mode Disabled → F-09, Known Bugs, CLEAN-01, `confirmed`, Phase 5, "viewModeStore + ViewModeToggle + dead BasicMapScreen branch present; remove in Phase 5"
    10. Feed Edge Function Wildcard CORS → F-10, Security, SEC-01, `confirmed`, Phase 2, "feed CORS '*' at feed/index.ts:20; enrich-dish:31; invalidate-cache:20"
    11. dish_analytics / User Behavioral Tables RLS Audit Gap → F-11, Security, SEC-02 + ASSESS-02, `confirmed — per-table PENDING`, Phase 3, "zero ENABLE RLS for behavioral tables in any migration/baseline; per-table verdict PENDING (Block 1). dish_analytics is dish-keyed NOT user-owned — Phase 3 must NOT add an owner policy on it"
    12. infra/scripts Production Mutation Scripts Without Guard → F-12, Security, SEC-03, `confirmed`, Phase 4, "replay-menu-scan-ab.ts / apply-phase6-flag-fixes.ts hit prod service-role; manual --dry-run only"
    13. generate_candidates Radius Timeout → F-13, Performance, PERF-01, `confirmed — perf-detail PENDING`, Phase 7, "migration 169 pushdown exists; hnsw.iterative_scan gating PENDING (Block 2)"
    14. feed/index.ts Single-Threaded JS Ranking Pass → F-14, Performance, PERF-02, `confirmed`, Phase 7, "Stage-2 ranking in JS; payload reduction is a Phase 7 design call"
    15. packages/database/src/types.ts 3226-Line File → F-15, Performance, DEBT-04, `confirmed — duplicate of F-07`, Phase 6, "same artifact as F-07; regen post-teardown sheds orphaned types"
    16. BasicMapScreen.tsx 608 Lines → F-16, Fragile Areas, RFCT-02, `confirmed`, Phase 9, "decompose after CLEAN-01 dead branch removed (Phase 5)"
    17. filterStore.ts 927 Lines → F-17, Fragile Areas, RFCT-01, `confirmed`, Phase 8, "split into slices; preserve persistence shape byte-for-byte"
    18. DailyFilterModal.tsx 894 Lines → F-18, Fragile Areas, RFCT-03, `confirmed`, Phase 9, "decompose per filter section"
    19. ReviewDishEditor.tsx 1258 Lines → F-19, Fragile Areas, RFCT-04, `confirmed`, Phase 10, "decompose along form regions; preserve admin_confirm_menu_scan contract"
    20. Vector Candidate Generation Dense Urban → F-20, Scaling Limits, (no req), `out-of-scope`, (none), "durable geo-aware ANN rebuild is PERF-V2-01 (REQUIREMENTS v2); Phase 7 tiered-radius is the v1 mitigation under F-13"
    21. Upstash Redis Feed Cache Single Namespace → F-21, Scaling Limits, PERF-03, `confirmed — tension flagged`, Phase 7, "invalidate-cache already flush-alls feed:v2:* on every change; the 'never flush-all' goal is a Phase 7 design call — flag as scope-impact, NOT a verdict (per RESEARCH anti-pattern)"
    22. deno.land/std@0.168.0 Pinned Old Version → F-22, Dependencies at Risk, DEBT-05, `confirmed`, Phase 4, "std@0.168.0/http/server everywhere; replace with native Deno.serve"
    23. esm.sh/@supabase/supabase-js@2 Unpinned Minor → F-23, Dependencies at Risk, DEBT-05, `confirmed`, Phase 4, "unpinned @2 AND pinned @2.39.3 coexist; @upstash/redis@latest in invalidate-cache vs @1.38.0 elsewhere"
    24. apps/mobile Zero Automated Tests → F-24, Test Coverage Gaps, (no req), `out-of-scope`, (none), "broad test-coverage push out of scope (REQUIREMENTS Out-of-Scope, minimal-tests decision); targeted persistence-seam test allowed in Phase 8 (RFCT-01 SC#4)"
    25. apps/admin No Component Tests, E2E Only → F-25, Test Coverage Gaps, (no req), `out-of-scope`, (none), "broad test-coverage push out of scope (REQUIREMENTS Out-of-Scope); admin single-operator"
    26. infra/supabase/functions No Deno Unit Tests for feed/ → F-26, Test Coverage Gaps, (no req), `out-of-scope`, (none), "broad test-coverage push out of scope (REQUIREMENTS Out-of-Scope, minimal-tests decision)"

    Every cell must be non-empty. For the three PENDING-detail findings (F-11 per-table, F-13 hnsw, F-21/PERF-03 webhook events), the summary verdict still carries the code-first determination and the scope-impact notes the live-state dependency.
  </action>
  <verify>
    <automated>test -f .planning/codebase/FINDINGS.md && grep -c '| F-' .planning/codebase/FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/codebase/FINDINGS.md` succeeds (file created)
    - `grep -c '| F-' .planning/codebase/FINDINGS.md` returns ≥ 26 (one summary row per CONCERNS finding)
    - `grep -c '# Codebase Findings Register' .planning/codebase/FINDINGS.md` returns 1
    - The summary table header row contains the cells `ID`, `verdict`, `gated phase`, and `scope impact` (`grep -E '\| *ID *\|.*verdict.*scope impact' .planning/codebase/FINDINGS.md` returns ≥ 1)
    - No summary data row has an empty verdict cell (manual scan: every `| F-NN |` row has a non-empty verdict column)
  </acceptance_criteria>
  <done>FINDINGS.md exists with a complete 26-row summary table; every row has ID, area, verdict, gated phase, and scope impact populated from the verified seeds.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 26 per-finding detail sections + Net-new section</name>
  <read_first>
    - .planning/codebase/FINDINGS.md (the skeleton from Task 1 — append detail sections below the summary table)
    - .planning/codebase/CONCERNS.md (per-finding detail blocks — Files / Issue / Risk / Impact labels to map back to)
    - .planning/phases/01-assessment-findings-register/01-RESEARCH.md (verdict seeds lines 278-303; drift corrections lines 239-243, 305-315; dish_analytics correction lines 245-249)
    - .planning/phases/01-assessment-findings-register/01-PATTERNS.md (per-finding section shape line 40; content corrections lines 43-46)
    - infra/supabase/functions/enrich-dish/index.ts (CORS line 31; stale header lines 8-21 — F-06/F-10 evidence)
    - infra/supabase/functions/feed/index.ts (CORS line 20 — F-10/F-14 evidence)
    - infra/supabase/functions/invalidate-cache/index.ts (CORS line 20; UPDATE-only header line 3; @upstash/redis@latest line 17 — F-21/F-23 evidence)
  </read_first>
  <action>
    Append to `.planning/codebase/FINDINGS.md` a `## Findings Detail` section, then one `### F-NN — <CONCERNS title>` subsection per finding (F-01 … F-26, same order as the summary table). Each detail section carries these bolded fields (per 01-PATTERNS.md line 40):
    - `**Verdict:**` — one of confirmed / stale / already-resolved / out-of-scope (D-06), optionally with a short modifier (e.g. "confirmed — partially mitigated", "confirmed — per-table PENDING").
    - `**Maps to:**` — the CONCERNS section + requirement ID + gated phase.
    - `**Evidence:**` — file:line on CURRENT HEAD for code-assessable findings; for the three prod-only findings write exactly `PENDING — live-state, see assess-live-state.sql Block N` (F-11 → Block 1 + Block 4; F-13 → Block 2; F-21/PERF-03 webhook → Block 3).
    - `**Scope impact:**` — the D-11 propagation note (mirrors the summary cell, expanded).
    - Separate sections with `---`.

    Bake in these verified corrections (CONCERNS.md is dated 2026-06-19 and has drifted — re-grep current HEAD, do NOT copy CONCERNS line numbers verbatim):
    - F-03/F-04/F-08 (web-portal): verdict `already-resolved`. Evidence: "apps/web-portal deleted AND committed in commit c1a7e3f (2026-06-18); `ls apps/web-portal` returns no such directory." CONCERNS says "uncommitted" — that is stale; note the correction explicitly. Scope impact: Phase 5 is residual-doc cleanup only (CLAUDE.md, agent_docs/architecture.md, .github/copilot-instructions.md, INTEGRATION_COMPLETE_SUMMARY.md).
    - F-06/F-10 (enrich-dish): CORS is `enrich-dish/index.ts:31` (NOT line 33 as CONCERNS line 164 says — line drift). Stale header at lines 8-21.
    - F-10 (feed CORS): `feed/index.ts:20`. F-10 also covers `invalidate-cache/index.ts:20`.
    - F-11 (RLS gap): Evidence `PENDING — live-state, see assess-live-state.sql Block 1 + Block 4`. In the scope-impact, state the verified correction: `dish_analytics` PK is `dish_id` with FK to `dishes` and has NO `user_id` column (verified in database_schema.sql) — it is a dish-keyed public aggregate, so Phase 3 must NOT plan an owner policy for it (CONCERNS line 174 mislists it among user-owned tables). Note that ZERO `ENABLE ROW LEVEL SECURITY` exists for behavioral tables in any migration or in database_schema.sql (confirming this is a genuine gap, per-table verdict awaiting Block 1).
    - F-21 (Upstash flush-all): record AS-IS — invalidate-cache already does a `feed:v2:*` flush-all on every change. Do NOT pre-judge this against ROADMAP Phase-7 SC#4's "never flush-all" goal; flag the tension as a scope-impact note for Phase 7 to resolve (per RESEARCH anti-pattern, lines 208). The deployed INSERT/UPDATE/DELETE event coverage is `PENDING — live-state, see Block 3`; code-first finding: invalidate-cache header (line 3) documents UPDATE-only invocation and its triggers are dashboard-configured (not in any migration).
    - F-23 (deps): note the verified nuance — unpinned `@supabase/supabase-js@2` AND pinned `@2.39.3` coexist across functions; `@upstash/redis@latest` (invalidate-cache line 17) vs `@1.38.0` elsewhere.

    After F-26, add a `## Net-new findings` section (D-07). If assessment surfaced no issue outside CONCERNS.md, write exactly one line: `None surfaced during Phase 1 assessment.` If any net-new issue IS surfaced, list it under a clear `### NN-01 — <title>` heading with a one-line description and the note `Routed to backlog / insert-phase DECISION — NOT actioned this milestone (per D-07).` Do NOT add net-new items to the summary table or to milestone scope.

    Do NOT include fenced code blocks containing SQL write keywords or the literal forbidden-keyword list inside this file — keep evidence as prose + file:line citations.
  </action>
  <verify>
    <automated>grep -c '^### F-' .planning/codebase/FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### F-' .planning/codebase/FINDINGS.md` returns ≥ 26 (one detail section per finding)
    - `grep -c '\*\*Verdict:\*\*' .planning/codebase/FINDINGS.md` returns ≥ 26 (every detail section has a Verdict field)
    - `grep -c 'PENDING — live-state' .planning/codebase/FINDINGS.md` returns ≥ 3 (the three live-state findings F-11, F-13, F-21 marked PENDING)
    - `grep -c '## Net-new findings' .planning/codebase/FINDINGS.md` returns 1
    - `grep -c 'c1a7e3f' .planning/codebase/FINDINGS.md` returns ≥ 1 (web-portal deletion commit cited for already-resolved verdicts)
    - `grep -E 'enrich-dish/index.ts:31' .planning/codebase/FINDINGS.md` returns ≥ 1 (correct CORS line, not the stale :33)
    - `grep -iE 'dish_analytics' .planning/codebase/FINDINGS.md` returns a line stating dish_analytics is NOT user-owned / has no user_id
    - Every cited `file:line` in a non-PENDING Evidence field resolves on current HEAD (spot-check: `grep -n "Access-Control-Allow-Origin" infra/supabase/functions/feed/index.ts` confirms line 20)
  </acceptance_criteria>
  <done>All 26 detail sections written with Verdict/Maps-to/Evidence/Scope-impact; the three live-state findings carry PENDING + Block refs; the Net-new section is present; verified corrections (web-portal committed, CORS line 31, dish_analytics not user-owned, flush-all tension) are baked in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (current HEAD) → register verdict | A wrong/stale verdict here propagates into a later phase's scope and could cause it to break working prod or re-fix a resolved item |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering (downstream) | FINDINGS.md verdicts driving later-phase scope | mitigate | Re-grep current HEAD for every code-assessable verdict; cite file:line that resolves now (not CONCERNS' dated line numbers); the three prod-only findings stay PENDING until Block output rather than being guessed |
| T-1-02 | Information Disclosure | FINDINGS.md content | accept | The register contains only repo metadata + verdicts (no prod row data, no secrets); no PII selected or pasted |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages and writes no application code (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `test -f .planning/codebase/FINDINGS.md` — register exists
- `grep -c '^### F-' .planning/codebase/FINDINGS.md` ≥ 26 — finding-count parity with CONCERNS.md (which has exactly 26 `### ` findings)
- `grep -c 'PENDING — live-state' .planning/codebase/FINDINGS.md` ≥ 3 — D-10 partial-complete: live-state findings deferred to checkpoint
- `grep -c '## Net-new findings' .planning/codebase/FINDINGS.md` = 1 — D-07 honored
- No verdict cites a stale line number (spot-check cited file:line against current HEAD)
</verification>

<success_criteria>
- All 23 code-assessable verdicts are FINAL with file:line evidence on current HEAD (unblocks Phases 2/4/5 per D-10)
- The 3 live-state findings (F-11 RLS, F-13 pgvector, F-21/PERF-03 webhook) are present and marked PENDING with Block references
- Verified drift corrections baked in: web-portal already-resolved (commit c1a7e3f), CORS line 31, dish_analytics not user-owned, flush-all tension flagged not pre-judged
- Net-new findings section present and routed to backlog (D-07), not added to milestone scope
</success_criteria>

<output>
Create `.planning/phases/01-assessment-findings-register/01-01-SUMMARY.md` when done
</output>
