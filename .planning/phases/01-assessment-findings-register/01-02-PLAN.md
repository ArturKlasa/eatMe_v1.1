---
phase: 01-assessment-findings-register
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/scripts/assess-live-state.sql
autonomous: true
requirements: [ASSESS-02, ASSESS-03]
must_haves:
  truths:
    - "An operator can run one .sql file once in the Supabase SQL editor and paste back all four blocks of output"
    - "The script cannot mutate prod — it carries a read-only transaction guard and contains zero write keywords outside comments"
    - "The script captures RLS state, pgvector version, deployed triggers, owner columns, AND a catch-all for any unprotected user-owned table"
  artifacts:
    - path: "infra/scripts/assess-live-state.sql"
      provides: "ONE consolidated read-only prod-state probe (D-08) — sibling to the .ts operator scripts"
      contains: "SET TRANSACTION READ ONLY"
      min_lines: 40
  key_links:
    - from: "infra/scripts/assess-live-state.sql"
      to: ".planning/codebase/FINDINGS.md"
      via: "the four labeled blocks fill FINDINGS.md PENDING sections (F-11 Block 1/4, F-13 Block 2, F-21 Block 3) at the checkpoint"
      pattern: "BLOCK [1-4]"
---

<objective>
Author `infra/scripts/assess-live-state.sql` (D-08): ONE consolidated, strictly READ-ONLY SQL probe the operator runs once in the Supabase SQL editor and pastes back, covering in a single pass — per-table RLS + policies for the behavioral tables (Block 1), pgvector `extversion` (Block 2), deployed-trigger dump for feed-affecting tables (Block 3), owner-column sanity (Block 4), and a catch-all for any unprotected user-owned table. Then run the Wave-0 static-safety gate (write-keyword grep + read-only-guard presence) BEFORE the script is ever handed to the operator.

Purpose: The three live-state unknowns (RLS status → Phase 3, pgvector version → Phase 7, webhook event coverage → Phase 7) live outside git — only the prod catalog knows them. This probe is the only sanctioned path (no local psql; stage-don't-apply). Read-only enforcement is the security gate: a stray write keyword would mutate live prod.

Output: `infra/scripts/assess-live-state.sql` — statically validated read-only, ready for the operator checkpoint (Plan 03).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-assessment-findings-register/01-CONTEXT.md
@.planning/phases/01-assessment-findings-register/01-RESEARCH.md
@.planning/phases/01-assessment-findings-register/01-PATTERNS.md
@.planning/phases/01-assessment-findings-register/01-VALIDATION.md
@infra/supabase/migrations/151_retire_ingredient_triggers.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the read-only probe — header + 4 labeled blocks + catch-all + guard</name>
  <read_first>
    - .planning/phases/01-assessment-findings-register/01-RESEARCH.md (the authored probe design — copy query bodies VERBATIM from lines 149-200; A2 catch-all note line 326; pg_policies column citation line 200)
    - .planning/phases/01-assessment-findings-register/01-PATTERNS.md (SQL header convention from migration 151, lines 54-64; read-only banner ethos lines 66-78; query-body source pointer lines 76, 134)
    - infra/supabase/migrations/151_retire_ingredient_triggers.sql (header-comment convention to mirror: leading `-- <filename>`, `-- Created:` date, blank `--`, rationale block)
    - .planning/codebase/CONCERNS.md (RLS Audit Gap finding lines 172-182 — the behavioral-table list source)
  </read_first>
  <action>
    Create `infra/scripts/assess-live-state.sql` (NEW file — no `.sql` exists in infra/scripts/ yet; use Write). Copy the query bodies VERBATIM from 01-RESEARCH.md lines 149-200 — do NOT invent new SQL.

    Header comment block (mirror migration 151 convention, 01-PATTERNS.md lines 54-64):
    - `-- assess-live-state.sql`
    - `-- Created: 2026-06-18`
    - blank `--` line
    - A banner: `-- READ ONLY. Run once in the Supabase SQL editor. Paste the FULL output of all blocks back.` and `-- No writes. No DDL. (Phase 1 live-state probe — gates Phases 3/6/7 per FINDINGS.md.)`

    Read-only hard guard (RESEARCH Pattern 2, lines 152-153 + 198):
    - Open with `BEGIN;` then on its own line `SET TRANSACTION READ ONLY;` with an inline comment noting it aborts any accidental write.
    - Close the whole script with `ROLLBACK;` and a comment `-- read-only; nothing to commit`. Do NOT use COMMIT anywhere.

    Then the four labeled SELECT-only blocks, each preceded by a `-- [BLOCK N] <label>` comment (copy bodies verbatim from RESEARCH lines 155-197):
    - `-- [BLOCK 1] RLS + policies for behavioral tables` — the `pg_tables` LEFT JOIN `pg_policies` query (RESEARCH 156-171) over the behavioral-table IN-list: favorites, dish_opinions, user_dish_interactions, user_behavior_profiles, dish_analytics, user_points, user_sessions, user_visits, session_views, dish_photos, restaurant_experience_responses. Keep the comment placeholder noting eat_together_* tables should be added if verified to exist.
    - Immediately after Block 1, add the A2 CATCH-ALL query (RESEARCH line 326) on its own labeled line `-- [BLOCK 1b] Catch-all: any public table with RLS disabled (so no unprotected table is missed)`: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND rowsecurity=false ORDER BY tablename;`
    - `-- [BLOCK 2] pgvector version (gates hnsw.iterative_scan in Phase 7)` — the `pg_extension` + `pg_available_extensions` queries (RESEARCH 174-176).
    - `-- [BLOCK 3] Deployed triggers on feed-affecting tables (webhook coverage, code-first confirm)` — the `information_schema.triggers` query over `('restaurants','menus','dishes')` (RESEARCH 179-185).
    - `-- [BLOCK 4] Owner-column sanity (confirm user_id exists where Phase 3 will add policies)` — the `information_schema.columns` query over the owner-table IN-list (RESEARCH 188-197). NOTE: this Block 4 IN-list intentionally EXCLUDES `dish_analytics` because it is dish-keyed (no user_id) — add a `--` comment on the Block 4 line explaining the exclusion so a reader knows it is deliberate.

    The script must contain SQL + SQL comments ONLY — no secrets, no connection strings, no service-role key (the operator runs it under their own Supabase SQL editor session, per the security_requirements). It selects only catalog metadata (table names, RLS flags, policy expressions, trigger DDL, extension versions, column names) — no row data from user tables, so no PII can be pasted back.
  </action>
  <verify>
    <automated>test -f infra/scripts/assess-live-state.sql && grep -c 'BLOCK' infra/scripts/assess-live-state.sql</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/scripts/assess-live-state.sql` succeeds
    - `grep -c 'SET TRANSACTION READ ONLY' infra/scripts/assess-live-state.sql` returns 1
    - `grep -c 'ROLLBACK' infra/scripts/assess-live-state.sql` returns ≥ 1 and `grep -ic 'COMMIT' infra/scripts/assess-live-state.sql` returns 0 (no COMMIT anywhere)
    - `grep -cE '\-\- \[BLOCK [1-4]' infra/scripts/assess-live-state.sql` returns ≥ 4 (all four blocks labeled)
    - `grep -c 'pg_policies' infra/scripts/assess-live-state.sql` ≥ 1, `grep -c 'pg_extension' infra/scripts/assess-live-state.sql` ≥ 1, `grep -c 'information_schema.triggers' infra/scripts/assess-live-state.sql` ≥ 1, `grep -c 'rowsecurity=false' infra/scripts/assess-live-state.sql` ≥ 1 (catch-all present)
    - The Block 4 owner-column IN-list does NOT contain `dish_analytics` (`grep -A6 'BLOCK 4' infra/scripts/assess-live-state.sql | grep -c dish_analytics` returns 0)
  </acceptance_criteria>
  <done>assess-live-state.sql exists with the read-only guard, the four labeled blocks copied verbatim from RESEARCH, the catch-all unprotected-table query, and dish_analytics correctly excluded from the owner-column block.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 static-safety gate — prove the script is read-only before operator handoff</name>
  <read_first>
    - infra/scripts/assess-live-state.sql (the file written in Task 1 — the target of the gate)
    - .planning/phases/01-assessment-findings-register/01-VALIDATION.md (Wave 0 Requirements lines 54-58: write-keyword grep + guard presence; quick run command line 24)
    - .planning/phases/01-assessment-findings-register/01-RESEARCH.md (Validation map D-08 safety row line 375; anti-pattern "Writes in the SQL probe" line 211)
  </read_first>
  <action>
    Run the SQL read-only static-safety gate against `infra/scripts/assess-live-state.sql`. This is the Wave-0 checker the VALIDATION contract requires before the operator ever executes the script.

    Strip comment lines first (so the comment banner and labels do not produce false positives), then grep for write keywords. The keyword list MUST match the VALIDATION.md canonical quick-run list exactly (`insert|update|delete|alter|drop|create|truncate|grant|revoke` — note `create` is included, since a stray `CREATE` would also mutate prod). Run exactly:
    `grep -vE '^[[:space:]]*--' infra/scripts/assess-live-state.sql | grep -inE '\b(insert|update|delete|alter|drop|create|truncate|grant|revoke)\b' && { echo 'FAIL: write keyword found'; exit 1; } || echo READONLY_OK`
    This MUST print `READONLY_OK` and exit 0 (no write keyword found). If it prints `FAIL: write keyword found`, the script has a write keyword in executable SQL — fix the script (the only legitimate keyword in this probe is in comments; `SET TRANSACTION READ ONLY` contains none of the forbidden tokens).

    Then confirm the guard is present:
    `grep -c 'SET TRANSACTION READ ONLY' infra/scripts/assess-live-state.sql` MUST return 1.

    Record the gate result (both commands + their output) in the plan's SUMMARY so the checkpoint plan (03) and `/gsd-verify-work` can see the script was validated read-only before handoff. Do NOT run the script against prod — that is the operator's job at the Plan 03 checkpoint.

    Note: `SET TRANSACTION READ ONLY` is intentionally NOT a write keyword and `READ ONLY` does not match the forbidden-token list, so the gate passes cleanly on a correct script. The `pg_available_extensions` / `pg_extension` / `information_schema` references likewise contain no forbidden tokens.
  </action>
  <verify>
    <automated>grep -vE '^[[:space:]]*--' infra/scripts/assess-live-state.sql | grep -inE '\b(insert|update|delete|alter|drop|create|truncate|grant|revoke)\b' && { echo 'FAIL: write keyword found'; exit 1; } || echo READONLY_OK</automated>
  </verify>
  <acceptance_criteria>
    - The write-keyword gate prints `READONLY_OK` and exits 0 (no write keyword on any non-comment line). The keyword list includes `create` to match the VALIDATION.md canonical list: `grep -vE '^[[:space:]]*--' infra/scripts/assess-live-state.sql | grep -inE '\b(insert|update|delete|alter|drop|create|truncate|grant|revoke)\b' && { echo 'FAIL: write keyword found'; exit 1; } || echo READONLY_OK`
    - `grep -c 'SET TRANSACTION READ ONLY' infra/scripts/assess-live-state.sql` returns 1 (guard present)
    - `grep -ic 'COMMIT' infra/scripts/assess-live-state.sql` returns 0 (no commit path)
    - The SUMMARY records the two gate commands and their (passing) output
  </acceptance_criteria>
  <done>The static-safety gate passes — zero write keywords (the full canonical list `insert|update|delete|alter|drop|create|truncate|grant|revoke`, `create` included) in executable SQL, the read-only guard is present, no COMMIT path — the gate prints `READONLY_OK` and exits 0, and the result is recorded in the SUMMARY, clearing the script for operator handoff at the Plan 03 checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed .sql artifact → operator's prod Supabase session | The operator pastes this script into a live prod SQL editor; any executable write keyword would mutate prod |
| prod catalog output → pasted-back text | The output is pasted into the register; it must contain no user-row data / PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | assess-live-state.sql run on prod | mitigate | `SET TRANSACTION READ ONLY` guard + `ROLLBACK` close + no COMMIT + Wave-0 write-keyword grep gate (Task 2) BEFORE the operator runs it; SELECT-only blocks |
| T-1-04 | Information Disclosure | pasted-back SQL output | mitigate | Probe selects only catalog metadata (table names, RLS flags, policy expressions, trigger DDL, extension versions, column names) — never row data from user tables, so no PII is exposed |
| T-1-05 | Information Disclosure | the committed .sql file | mitigate | Script contains SQL + comments only — no service-role key, no connection string, no secrets (operator runs it under their own session) |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed; this is a `.sql` artifact (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `test -f infra/scripts/assess-live-state.sql` — probe exists
- Write-keyword grep over non-comment lines returns zero matches (read-only proven)
- `grep -c 'SET TRANSACTION READ ONLY' infra/scripts/assess-live-state.sql` = 1 — guard present
- `grep -cE '\-\- \[BLOCK [1-4]' infra/scripts/assess-live-state.sql` ≥ 4 — all four blocks present
- Catch-all unprotected-table query present (`rowsecurity=false`); dish_analytics excluded from Block 4 owner-column list
</verification>

<success_criteria>
- ONE consolidated read-only probe authored (D-08), query bodies copied verbatim from RESEARCH (not invented)
- Read-only hard guard present, zero write keywords in executable SQL (Wave-0 gate passes), no COMMIT path
- All four blocks + catch-all present; pgvector, RLS+policies, deployed triggers, owner columns all captured in one pass
- dish_analytics correctly excluded from the owner-column block (it is dish-keyed, not user-owned)
- Script is cleared for the operator checkpoint (Plan 03) — no prod execution happens in this plan
</success_criteria>

<output>
Create `.planning/phases/01-assessment-findings-register/01-02-SUMMARY.md` when done
</output>
