---
phase: 01-assessment-findings-register
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/codebase/FINDINGS.md
autonomous: false
requirements: [ASSESS-02, ASSESS-03]
must_haves:
  truths:
    - "The operator has run assess-live-state.sql once on prod and pasted the full output back"
    - "FINDINGS.md's three PENDING live-state sections are filled from that output"
    - "Each behavioral table is labeled mobile-direct vs service-role-only, with its actual rowsecurity + policy state"
    - "The webhook code-first assessment is reconciled against the deployed-trigger dump (states agree or drift)"
  artifacts:
    - path: ".planning/codebase/FINDINGS.md"
      provides: "Live-state sections F-11 (RLS), F-13 (pgvector), F-21/PERF-03 (webhook) filled from prod paste-back"
      contains: "rowsecurity"
  key_links:
    - from: "operator prod SQL output"
      to: ".planning/codebase/FINDINGS.md"
      via: "Block 1/4 → F-11; Block 2 → F-13; Block 3 → F-21 webhook coverage"
      pattern: "PENDING — live-state"
---

<objective>
The D-10 execution checkpoint: gate on the OPERATOR running `infra/scripts/assess-live-state.sql` once on prod and pasting the full output back, then transcribe that output into the three PENDING live-state sections of FINDINGS.md (F-11 RLS, F-13 pgvector, F-21/PERF-03 webhook), label each behavioral table by caller, and reconcile the code-first webhook assessment against the deployed-trigger dump.

Purpose: These three facts (live RLS per-table, pgvector extversion, deployed webhook event coverage) cannot be known from the repo — they gate Phases 3 (RLS), 6 (schema), and 7 (pgvector/cache). Until they are captured, those phases cannot safely scope. This is the partial-complete boundary in D-10: Plans 01/02 unblocked Phases 2/4/5; this plan unblocks 3/6/7.

Output: FINDINGS.md with all three live-state sections FINAL — no `PENDING — live-state` markers remaining.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/FINDINGS.md
@infra/scripts/assess-live-state.sql
@.planning/phases/01-assessment-findings-register/01-RESEARCH.md
@.planning/phases/01-assessment-findings-register/01-VALIDATION.md
@infra/supabase/functions/invalidate-cache/index.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator runs the prod probe and pastes the full output back</name>
  <action>Blocking human-action checkpoint (no CLI/API path — prod access is operator-only). Halt execution and ask the operator to run `infra/scripts/assess-live-state.sql` once in the Supabase SQL editor and paste back the full output of all blocks. Do not proceed to Task 2 until the paste-back is received. See &lt;how-to-verify&gt; for the exact steps and &lt;resume-signal&gt;.</action>
  <what-built>
    `infra/scripts/assess-live-state.sql` (from Plan 02) — a statically-validated read-only probe that captures, in one pass: per-table RLS + policies for the behavioral tables (Block 1) + a catch-all for any unprotected public table (Block 1b), pgvector extversion (Block 2), deployed triggers on restaurants/menus/dishes (Block 3), and owner-column sanity (Block 4).
  </what-built>
  <how-to-verify>
    This step requires the human operator — there is NO CLI/API path (no local psql; stage-don't-apply; prod access is operator-only by project constraint). Steps:
    1. Open the Supabase SQL editor for the EatMe prod project.
    2. Paste the ENTIRE contents of `infra/scripts/assess-live-state.sql` and run it once. (It opens a read-only transaction and rolls back — it makes no changes.)
    3. Copy the FULL result of all blocks — Block 1 (RLS + policies), Block 1b (catch-all unprotected tables), Block 2 (pgvector version), Block 3 (deployed triggers), Block 4 (owner columns) — and paste it back here as one block.
    Expected: a table-per-block result set; Block 2 returns one `vector` extension row with an `extversion` value; Block 3 lists the deployed triggers (with INSERT/UPDATE/DELETE event_manipulation) on restaurants/menus/dishes.
  </how-to-verify>
  <resume-signal>Paste the full SQL output for all blocks, or describe any error encountered running it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Transcribe the paste-back into FINDINGS.md live-state sections + label + reconcile</name>
  <read_first>
    - .planning/codebase/FINDINGS.md (the three PENDING sections F-11, F-13, F-21/PERF-03 to fill — written in Plan 01)
    - infra/supabase/functions/invalidate-cache/index.ts (header line 3 documents UPDATE-only invocation — the code-first side of the webhook reconciliation)
    - .planning/phases/01-assessment-findings-register/01-RESEARCH.md (reconciliation check requirement line 378-379; mobile-direct vs service-role labeling; dish_analytics-not-user-owned correction lines 245-249; hnsw >=0.8.0 gate A1 line 321)
    - .planning/phases/01-assessment-findings-register/01-VALIDATION.md (wave-2 checkpoint rows lines 47-48 — completeness criteria)
  </read_first>
  <!-- planner-discipline-allow: PENDING — live-state -->
  <action>
    Using ONLY the operator's pasted output from Task 1, edit `.planning/codebase/FINDINGS.md` to replace each live-state placeholder marker (the "PENDING — live-state, see Block N" string written by Plan 01) with the captured fact. Edit the three sections:

    F-11 (RLS audit gap) — from Block 1, Block 1b, Block 4:
    - For every behavioral table in Block 1, record its actual `rowsecurity` (true/false) and any policy (name, cmd, qual, with_check) from the paste-back.
    - Label each table mobile-direct vs service-role-only (mobile-direct = read/written by the mobile app via the anon/authed REST client and therefore needs RLS; service-role-only = only touched by edge functions / service-role, which bypasses RLS). Use the captured policy/role data plus the known caller pattern.
    - Cross-check Block 1b (catch-all): if it surfaced any public table with `rowsecurity=false` that is NOT already in the F-11 behavioral list, add it as a sub-note so no unprotected user-owned table is missed (RESEARCH A2/A3).
    - Re-state the verified correction: `dish_analytics` is dish-keyed (no `user_id`, confirmed via Block 4 absence) → Phase 3 must NOT add an owner policy on it.
    - Update the F-11 scope-impact: list which tables Phase 3 must enable RLS + owner policy on (those with `rowsecurity=false` + a user_id owner col) vs which are already protected (skip) vs which are service-role-only (leave unchanged).

    F-13 (generate_candidates / pgvector) — from Block 2:
    - Record the exact `vector` `extversion` value.
    - State whether `hnsw.iterative_scan` is available at that version (the documented gate is >= 0.8.0, per RESEARCH A1 — but record the value and the determination; the final applicability call is Phase 7's). Update the scope-impact accordingly.

    F-21 / PERF-03 (webhook cache coverage) — from Block 3, RECONCILED against code-first:
    - Record the deployed triggers on restaurants/menus/dishes and which event_manipulation values (INSERT/UPDATE/DELETE) are actually wired.
    - Write the code-first side (D-09: assess webhook coverage code-first, then confirm against the deployed dump): invalidate-cache header (line 3) documents UPDATE-only invocation; its triggers are dashboard-configured (not in any migration). Per D-09 the deployed `information_schema.triggers` dump (Block 3) confirms whether the deployed state matches the code-first intent (prod can drift).
    - Explicitly state whether the deployed dump AGREES with the code-first assessment or DRIFTS (e.g. "deployed shows UPDATE-only on dishes → agrees; INSERT/DELETE not wired → Phase 7 must widen coverage"). A live-state section that shows only one side is incomplete (RESEARCH reconciliation check).
    - Keep the existing flush-all tension note (do NOT resolve it — Phase 7's call).

    After editing, NO `PENDING — live-state` marker may remain in FINDINGS.md. If the operator's output was incomplete for any block, do NOT fabricate — leave that specific sub-fact marked and report it in the SUMMARY as still-blocked rather than guessing.
  </action>
  <verify>
    <automated>grep -c 'PENDING — live-state' .planning/codebase/FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PENDING — live-state' .planning/codebase/FINDINGS.md` returns 0 (all three live-state sections filled) — OR the SUMMARY documents exactly which sub-fact remained blocked and why (no fabrication)
    - F-11 section contains the string `rowsecurity` with per-table true/false values transcribed from the paste-back
    - F-11 contains a `mobile-direct` and/or `service-role-only` caller label for the behavioral tables
    - F-13 section records a concrete pgvector `extversion` value (a version string, not "PENDING")
    - F-21/PERF-03 section contains BOTH a code-first statement (invalidate-cache UPDATE-only) AND the deployed-trigger result, plus an explicit "agree"/"drift" reconciliation word
    - dish_analytics is still flagged as not-user-owned in F-11 (Phase 3 must not add an owner policy)
  </acceptance_criteria>
  <done>All three live-state sections are FINAL from the operator paste-back; behavioral tables labeled by caller; pgvector version recorded with the hnsw gate determination; webhook coverage reconciled code-first vs deployed with an explicit agree/drift statement; no PENDING markers remain (or remaining blocks are honestly reported, not fabricated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's prod Supabase session → pasted-back text | The operator runs the read-only probe on live prod; output is transcribed into the register |
| pasted output → FINDINGS.md verdicts | A mis-transcribed RLS state could cause Phase 3 to deny-all the live mobile client or skip a genuinely-unprotected table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering | prod, during the probe run | mitigate | The probe is already statically validated read-only in Plan 02 (write-keyword gate + SET TRANSACTION READ ONLY); the operator runs that exact file, nothing else |
| T-1-07 | Tampering (downstream) | F-11 RLS transcription | mitigate | Transcribe only from the paste-back (no fabrication); cross-check Block 1b catch-all so no unprotected table is missed; keep dish_analytics flagged as not-user-owned so Phase 3 doesn't add a bad owner policy |
| T-1-08 | Information Disclosure | pasted output | accept | Probe selects only catalog metadata (RLS flags, policy expressions, trigger DDL, extension version, column names) — no user-row data / PII, so the pasted text carries no secrets |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- The operator has pasted back the full probe output (checkpoint cleared)
- `grep -c 'PENDING — live-state' .planning/codebase/FINDINGS.md` = 0 (or remaining blocks honestly reported in SUMMARY)
- F-11 has per-table rowsecurity + caller labels; F-13 has a concrete extversion; F-21/PERF-03 has both sides + an agree/drift word
- dish_analytics still flagged not-user-owned
</verification>

<success_criteria>
- Operator paste-back captured (single clean run, per the specifics — not a drip of separate queries)
- All three live-state sections FINAL; Phases 3/6/7 can now scope from real prod state (D-10 fully satisfied)
- Each behavioral table labeled mobile-direct vs service-role-only with its actual RLS + policy state
- Webhook code-first vs deployed reconciliation explicit (agree or drift)
- No fabrication: any block the operator could not supply is reported as still-blocked, not invented
</success_criteria>

<output>
Create `.planning/phases/01-assessment-findings-register/01-03-SUMMARY.md` when done
</output>
