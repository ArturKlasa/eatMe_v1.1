---
phase: 01-assessment-findings-register
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [ASSESS-01]
must_haves:
  truths:
    - "The user reviews the proposed scope changes BEFORE any ROADMAP/REQUIREMENTS edit is applied"
    - "Only user-approved scope annotations are written; nothing is auto-edited without sign-off"
    - "Phase numbers and the 21-requirement set are unchanged — annotations only, no renumber"
    - "Later phases read already-trimmed scope so they don't re-fix resolved items or break working prod"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Verdict-driven scope annotations on affected phase blocks (D-11/D-12)"
      contains: "Scope note (per FINDINGS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability status edits for any requirement a verdict fully resolves"
      contains: "Satisfied (per FINDINGS"   # status-flip annotation anchor; N/A if the user picks approve-none at the Task-1 scope-review checkpoint (no edits applied)
  key_links:
    - from: ".planning/codebase/FINDINGS.md"
      to: ".planning/ROADMAP.md"
      via: "each finding's scope-impact cell becomes an approved ROADMAP annotation"
      pattern: "Scope note \\(per FINDINGS"
---

<objective>
At Phase-1 close, apply the verdict-driven scope changes to `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — but ONLY after the user reviews and approves the proposed edits (D-11). For any verdict that fully resolves a phase or requirement, annotate "descoped per FINDINGS — <reason>" / mark the requirement satisfied (D-12) — and NEVER renumber phases or remap requirements.

Purpose: This is the one mutation Phase 1 makes to milestone scope, and it is gated. It makes the parallel later phases read already-trimmed scope (e.g. Phase 3 skips already-RLS-protected tables; Phase 5 is residual-doc-cleanup-only because web-portal is already deleted; Phase 7 inherits the real pgvector/webhook facts). One gated review — never auto-edit.

Output: ROADMAP.md + REQUIREMENTS.md carrying only the user-approved annotations, with stable phase numbers and the 21-requirement set intact.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/FINDINGS.md
@.planning/phases/01-assessment-findings-register/01-RESEARCH.md
@.planning/phases/01-assessment-findings-register/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Present proposed scope changes for user sign-off (D-11)</name>
  <action>Blocking decision checkpoint (D-11). Halt execution, build the candidate scope-change set from the final FINDINGS.md scope-impact cells, and present it to the user for sign-off. Do not apply any ROADMAP/REQUIREMENTS edit until the user selects an option. See &lt;decision&gt;, &lt;context&gt;, &lt;options&gt;, and &lt;resume-signal&gt; for the candidate edits and the choices.</action>
  <read_first>
    - .planning/codebase/FINDINGS.md (the final register — every row's scope-impact cell is a candidate edit; live-state sections now filled from Plan 03)
    - .planning/ROADMAP.md (the phase blocks that would receive annotations)
    - .planning/REQUIREMENTS.md (the traceability table + requirement checklists that would receive status edits)
    - .planning/phases/01-assessment-findings-register/01-PATTERNS.md (exact annotation formats: ROADMAP lines 86-98; REQUIREMENTS lines 102-113; no-renumber rule lines 126-128)
  </read_first>
  <decision>
    Which proposed scope changes (derived from FINDINGS.md scope-impact cells) should be applied to ROADMAP.md and REQUIREMENTS.md? Present the candidate edit set; the user approves all / a subset / none, or amends.
  </decision>
  <context>
    Per D-11, scope edits are user-gated — never applied without sign-off. Per D-12, fully-resolved phases/requirements are ANNOTATED, not renumbered. Build the candidate list from the final FINDINGS verdicts (including the now-filled live-state sections from Plan 03). Likely candidates, each shown with its exact proposed text:
    - Phase 5: add `**Scope note (per FINDINGS):** F-03/F-04/F-08 already-resolved — apps/web-portal deleted+committed (c1a7e3f); Phase 5 narrows to residual-doc cleanup (CLAUDE.md, agent_docs/architecture.md, .github/copilot-instructions.md, INTEGRATION_COMPLETE_SUMMARY.md) + CLEAN-01 dead view-mode + CLEAN-03 comments.` (annotation only — CLEAN-01/02/03 all still in scope, so the phase is NOT descoped).
    - Phase 3: add a `**Scope note (per FINDINGS):**` line driven by F-11's filled Block-1 results — list the tables that need RLS+owner-policy vs those already protected (skip) vs service-role-only (leave unchanged); explicitly note dish_analytics is dish-keyed → NO owner policy. If Block 1 showed ALL behavioral tables already RLS-protected, propose marking SEC-02 `Satisfied (per FINDINGS — <reason>)` and annotating Phase 3 "descoped per FINDINGS" (D-12) — but ONLY if the live-state actually shows that.
    - Phase 7: add a `**Scope note (per FINDINGS):**` line from F-13 (pgvector extversion → hnsw.iterative_scan available or not) and F-21/PERF-03 (deployed webhook event coverage → how much INSERT/DELETE widening is actually needed; flush-all tension noted).
    Present each candidate with: the target phase/requirement, the exact line to add (or status flip), and the FINDINGS row it traces to. Do NOT pre-apply anything.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve all proposed annotations</name>
      <pros>Later phases immediately read trimmed scope; full audit trail in FINDINGS</pros>
      <cons>None if the candidate list matches the user's intent</cons>
    </option>
    <option id="approve-subset">
      <name>Approve a subset / amend wording</name>
      <pros>User controls exactly which scope changes land</pros>
      <cons>Requires the user to specify which</cons>
    </option>
    <option id="approve-none">
      <name>Apply none — leave scope as-is</name>
      <pros>Zero ROADMAP/REQUIREMENTS change; verdicts still live in FINDINGS for later reference</pros>
      <cons>Later phases must consult FINDINGS manually for descope info</cons>
    </option>
  </options>
  <resume-signal>Select: approve-all, approve-subset (list which), or approve-none. Amend any wording inline.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply ONLY the approved annotations to ROADMAP.md + REQUIREMENTS.md (no renumber)</name>
  <read_first>
    - .planning/ROADMAP.md (target — apply scoped Edits to the approved phase blocks only)
    - .planning/REQUIREMENTS.md (target — traceability status + checklist edits)
    - .planning/codebase/FINDINGS.md (source of truth for the approved annotation text)
    - .planning/phases/01-assessment-findings-register/01-PATTERNS.md (exact formats lines 86-113; no-renumber rule lines 126-128; coverage-count rule line 113)
  </read_first>
  <action>
    Apply ONLY the edits the user approved in Task 1. Use the Edit tool (scoped replacement) on both files — NEVER Write (a whole-file write would destroy phase/requirement entries outside the diff). If the user chose approve-none, make no edits and record that in the SUMMARY.

    ROADMAP.md (D-12 annotation format, 01-PATTERNS.md lines 86-98):
    - For each approved phase: add a `**Scope note (per FINDINGS):** <approved text>` line inside that `### Phase N:` detail block (e.g. after its **Success Criteria** list or its **Plans:** line). For a fully-resolved phase, the note reads `descoped per FINDINGS — <reason>`.
    - Do NOT touch the `- [ ] **Phase N: ...**` checklist (lines 17-26) numbering, and do NOT touch the Progress-table phase rows (lines 146-157) numbering. Phase numbers stay stable (D-12).

    REQUIREMENTS.md (D-12 status format, 01-PATTERNS.md lines 102-113):
    - For each requirement an approved verdict fully resolves: flip its Traceability `Status` cell from `Pending` to `Satisfied (per FINDINGS — <reason>)`, keep the Phase column unchanged (no renumber), and flip the matching `- [ ]` → `- [x]` in the requirement checklist.
    - Do NOT alter the `v1 requirements: 21 total` coverage count (verdicts change status, not the requirement set). Update only the `**Last updated:**` footer date.

    After applying, verify with a diff that ONLY annotation lines / status-cell flips changed and no phase number / requirement ID moved. Note that ASSESS-01/02/03 themselves are satisfied by Phase 1's own completion — flip those to satisfied if the user approves (they are this phase's requirements).
  </action>
  <verify>
    <automated>git -C /home/art/Documents/eatMe_v1 diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md phase numbering is unchanged: `grep -cE '^\- \[ \] \*\*Phase [0-9]+:' .planning/ROADMAP.md` returns 10 (same as before — no renumber)
    - If any phase was annotated: `grep -c 'Scope note (per FINDINGS' .planning/ROADMAP.md` returns ≥ 1 (or 0 if approve-none, recorded in SUMMARY)
    - REQUIREMENTS.md still lists 21 requirements: `grep -cE '^\| (ASSESS|SEC|CLEAN|DEBT|PERF|RFCT)-[0-9]+ \|' .planning/REQUIREMENTS.md` returns 21 (set unchanged)
    - `git diff .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows ONLY added annotation lines + status-cell flips + footer date — no phase number or requirement ID moved (manual diff review)
    - No edit was applied that the user did not approve in Task 1 (SUMMARY records the approved set)
  </acceptance_criteria>
  <done>Only user-approved annotations applied via scoped Edits; phase numbers and the 21-requirement set intact; satisfied requirements flipped in both the traceability table and the checklist; footer date updated; diff confirms annotations-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FINDINGS verdicts → ROADMAP/REQUIREMENTS scope | An applied scope edit changes what later phases will (or won't) do — an over-aggressive descope could skip genuinely-needed hardening |
| proposed edits → user → applied edits | The user is the gate; nothing lands without sign-off (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Tampering | ROADMAP/REQUIREMENTS scope edits | mitigate | User-gated review (Task 1 decision checkpoint) before any edit; apply ONLY the approved set; scoped Edit (never whole-file Write) so unrelated entries are untouched |
| T-1-10 | Tampering | phase numbering / requirement set | mitigate | D-12 no-renumber rule enforced via acceptance grep (10 phases, 21 requirements unchanged); annotations + status flips only |
| T-1-11 | Repudiation | scope-change audit trail | mitigate | Every annotation traces to a FINDINGS row ("per FINDINGS — <reason>"); the approved set is recorded in the SUMMARY |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- User approved the scope-change set BEFORE any edit (Task 1 checkpoint cleared)
- ROADMAP.md still has 10 phase checklist entries; numbering unchanged (no renumber, D-12)
- REQUIREMENTS.md still lists 21 requirements; coverage count unchanged
- Any annotation present traces to a FINDINGS row ("per FINDINGS — <reason>")
- `git diff` shows annotations + status flips only — no structural change
</verification>

<success_criteria>
- One gated review at phase close; only user-approved edits applied (D-11)
- Fully-resolved phases/requirements annotated "descoped/satisfied per FINDINGS", NOT renumbered (D-12)
- Phase numbers (10) and requirement set (21) intact; later phases read already-trimmed scope
- Full audit trail: every scope change references its FINDINGS verdict
</success_criteria>

<output>
Create `.planning/phases/01-assessment-findings-register/01-04-SUMMARY.md` when done
</output>
