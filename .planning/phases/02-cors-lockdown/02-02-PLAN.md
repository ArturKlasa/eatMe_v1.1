---
phase: 02-cors-lockdown
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - infra/supabase/functions/feed/index.ts
  - infra/supabase/functions/enrich-dish/index.ts
  - infra/supabase/functions/invalidate-cache/index.ts
  - infra/supabase/functions/README.md
autonomous: false
requirements: [SEC-01]
user_setup:
  - service: supabase
    why: "Per-deploy allowlist config + prod deploy of the 3 functions + SC smoke calls (stage-don't-apply — operator owns prod)"
    env_vars:
      - name: ALLOWED_ORIGINS
        source: "Supabase Dashboard -> Edge Functions -> Secrets (or `supabase secrets set`); value: https://eat-me-v1-1-admin.vercel.app,http://localhost:3001"
    dashboard_config:
      - task: "Set ALLOWED_ORIGINS secret FIRST, then deploy feed/enrich-dish/invalidate-cache, then run the 3 smoke calls"
        location: "Supabase project (Edge Functions) — see operator checklist in Task 4"
must_haves:
  truths:
    - "feed, enrich-dish, and invalidate-cache compute corsHeaders per-request via buildCorsHeaders(req.headers.get('Origin')) as the first line of their serve handler"
    - "No module-level `const corsHeaders = {` remains in any of the 3 functions"
    - "Each function imports buildCorsHeaders from '../_shared/cors.ts'"
    - "The OPTIONS preflight and the main response in each function emit the same CORS header set (both read the one per-request corsHeaders)"
    - "README documents the allowlist + ALLOWED_ORIGINS + redeploy-all-importers rule (no longer says Allow-Origin: *)"
  artifacts:
    - path: "infra/supabase/functions/feed/index.ts"
      provides: "feed wired to the shared allowlist CORS helper, per-request"
      contains: "buildCorsHeaders(req.headers.get('Origin'))"
    - path: "infra/supabase/functions/enrich-dish/index.ts"
      provides: "enrich-dish wired to the shared allowlist CORS helper, per-request"
      contains: "buildCorsHeaders(req.headers.get('Origin'))"
    - path: "infra/supabase/functions/invalidate-cache/index.ts"
      provides: "invalidate-cache CORS locked (PERF-03 lands here; Redis logic untouched)"
      contains: "buildCorsHeaders(req.headers.get('Origin'))"
    - path: "infra/supabase/functions/README.md"
      provides: "Updated CORS Configuration section (allowlist + env var + redeploy warning)"
      contains: "ALLOWED_ORIGINS"
  key_links:
    - from: "infra/supabase/functions/feed/index.ts"
      to: "infra/supabase/functions/_shared/cors.ts"
      via: "import { buildCorsHeaders } from '../_shared/cors.ts'"
      pattern: "from '\\.\\./_shared/cors\\.ts'"
    - from: "infra/supabase/functions/enrich-dish/index.ts"
      to: "infra/supabase/functions/_shared/cors.ts"
      via: "import { buildCorsHeaders } from '../_shared/cors.ts'"
      pattern: "from '\\.\\./_shared/cors\\.ts'"
    - from: "infra/supabase/functions/invalidate-cache/index.ts"
      to: "infra/supabase/functions/_shared/cors.ts"
      via: "import { buildCorsHeaders } from '../_shared/cors.ts'"
      pattern: "from '\\.\\./_shared/cors\\.ts'"
---

<objective>
Wire the three SEC-01 functions — `feed`, `enrich-dish`, `invalidate-cache` — to the shared `../_shared/cors.ts` helper from Plan 01. In each function: delete the module-level wildcard `const corsHeaders`, add `import { buildCorsHeaders } from '../_shared/cors.ts'`, and insert `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'))` as the FIRST line of the `serve` handler so every existing spread site and the OPTIONS preflight now emit the per-request allowlist header set (D-04/D-05/D-13). Update the README CORS section to document the allowlist + `ALLOWED_ORIGINS` env var + the redeploy-all-importers rule. Hand the operator an ordered deploy + smoke checklist (set secret → deploy 3 → run SC#1/SC#2/SC#3) as a non-autonomous tail.

This is the strictly-3-functions scope of SEC-01 (D-07). The other 4 wildcard functions are NOT touched (Deferred). No auth/JWT logic, no business logic, no Redis/`feed:v2:*` flush-all logic (Phase 7 / PERF-03 owns that), and no migrations are touched.

Purpose: Replaces the live wildcard `Access-Control-Allow-Origin: *` with allowlist reflection on all three SEC-01 functions while preserving the no-Origin mobile happy path and the admin preflight — closing SEC-01 (and PERF-03's CORS half for `invalidate-cache`).
Output: 3 edited functions + updated README; operator deploy/smoke checklist captured for stage-don't-apply.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cors-lockdown/02-CONTEXT.md
@.planning/phases/02-cors-lockdown/02-RESEARCH.md
@.planning/phases/02-cors-lockdown/02-PATTERNS.md
@.planning/phases/02-cors-lockdown/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (_shared/cors.ts) — the contract this plan consumes. -->
buildCorsHeaders(origin: string | null): Record<string, string>
  - returns a plain object always containing Access-Control-Allow-Headers (verbatim), Access-Control-Allow-Methods, Vary: Origin
  - sets Access-Control-Allow-Origin only when origin is an exact ALLOWED_ORIGINS member; omits it otherwise
  - import as: import { buildCorsHeaders } from '../_shared/cors.ts'

<!-- Confirmed Edit Inventory on current HEAD (re-confirm exact lines before editing — file may shift). -->
feed/index.ts:          import block ~12-15 | const corsHeaders 19-22 | serve( 708 | OPTIONS 709-710 | compressedJsonResponse call ~1098 (sig 26-28 forwards headers) | spreads 726,754,919,1102,1108
enrich-dish/index.ts:   import block ~23-24 | const corsHeaders 30-33 | serve( 107 | OPTIONS 108-109 | service-role JWT check 112-118 (DO NOT TOUCH) | spreads 117,132,150,158,203,210,216
invalidate-cache/index.ts: import block ~15-17 | const corsHeaders 19-22 | serve( 49 | OPTIONS 50-51 | Redis logic 24-47 + feed:v2:* flush-all line 80 (DO NOT TOUCH) | spreads 64,72,117,123
</interfaces>
</context>

<forward_coordination_note>
NOTE — no action this phase. Phase 4 (DEBT-05) swaps `serve(...)` → `Deno.serve(...)` across these same 3 files. Its swap MUST preserve (a) the `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'))` first line of each handler and (b) the `import { buildCorsHeaders } from '../_shared/cors.ts'` import. The Plan-02 executor only ADDS the per-request first line inside the existing `serve(...)` handler — it does NOT change the `serve` import (that is Phase 4's job).
</forward_coordination_note>

<tasks>

<task type="auto">
  <name>Task 1: Wire feed/index.ts to the shared per-request CORS helper</name>
  <files>infra/supabase/functions/feed/index.ts</files>
  <read_first>
    - infra/supabase/functions/feed/index.ts — re-read the import block (~12-15), the const (19-22), the serve entry (708), OPTIONS (709-710), compressedJsonResponse definition (26-45) and its call site (~1098), and every spread site (726, 754, 919, 1102, 1108) to confirm current line numbers before editing
    - infra/supabase/functions/_shared/cors.ts — the buildCorsHeaders signature/shape this file consumes
    - .planning/phases/02-cors-lockdown/02-RESEARCH.md — "Edit Inventory" → feed/index.ts section (the line-accurate checklist)
    - .planning/phases/02-cors-lockdown/02-PATTERNS.md — `feed/index.ts (MODIFY)` section (compressedJsonResponse thread-through A1 confirmed RESOLVED)
  </read_first>
  <action>
    Make exactly three edits in `infra/supabase/functions/feed/index.ts`, using the RESEARCH Edit Inventory line numbers as a guide (re-confirm against the file you just read — lines may have shifted):

    1. DELETE the module-level wildcard corsHeaders declaration (the object-literal assignment to a `corsHeaders` constant at module scope, currently lines 19-22 — the one whose Access-Control-Allow-Origin value is the wildcard). Removing it is intentional: per RESEARCH Pitfall 2, the compiler then flags any spread site you missed as an undefined identifier — this is the safety net, so do NOT leave a module-level fallback.

    2. ADD the import alongside the other imports near the top (current import block ~12-15): `import { buildCorsHeaders } from '../_shared/cors.ts';`. Keep the existing `serve` import unchanged (Phase 4 owns the serve→Deno.serve swap).

    3. INSERT, as the FIRST line inside the `serve(async (req: Request) => {` handler body (current line 708), before the `if (req.method === 'OPTIONS')` check: `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'));`. This declares a per-request local that shadows nothing (the module const is gone) and is read by the OPTIONS short-circuit and every downstream spread.

    Do NOT edit any spread site text — lines 726/754/919/1102/1108 already use `{ ...corsHeaders, 'Content-Type': 'application/json' }` and now resolve to the per-request local automatically. Do NOT change the OPTIONS line text (709-710) — it already reads `corsHeaders`. Do NOT change the `compressedJsonResponse(responseData, corsHeaders)` call (~1098) or its definition (26-45) — the signature already takes a `headers` param and forwards it onto the final `new Response` (PATTERNS A1 RESOLVED), so the per-request object reaches the gzip path unchanged. Touch nothing else — no auth, no business logic, no Redis.
  </action>
  <verify>
    <automated>test "$(grep -c 'const corsHeaders = {' infra/supabase/functions/feed/index.ts)" -eq 0 && grep -q "buildCorsHeaders(req.headers.get('Origin'))" infra/supabase/functions/feed/index.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const corsHeaders = {" infra/supabase/functions/feed/index.ts` returns 0 (old wildcard const gone)
    - file contains `import { buildCorsHeaders } from '../_shared/cors.ts'`
    - file contains `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'))`
    - the `serve` import line (`from 'https://deno.land/std@0.168.0/http/server.ts'`) is UNCHANGED (Phase 4 owns that swap)
    - `grep -c "'Access-Control-Allow-Origin': '\\*'" infra/supabase/functions/feed/index.ts` returns 0 (no wildcard literal left)
  </acceptance_criteria>
  <done>feed computes corsHeaders per-request as the handler's first line via the shared helper, imports it by relative path, has no module-level corsHeaders const, retains the std serve import, and emits no wildcard ACAO.</done>
</task>

<task type="auto">
  <name>Task 2: Wire enrich-dish + invalidate-cache to the shared helper; type-check all three</name>
  <files>infra/supabase/functions/enrich-dish/index.ts, infra/supabase/functions/invalidate-cache/index.ts</files>
  <read_first>
    - infra/supabase/functions/enrich-dish/index.ts — re-read import block (~23-24), const (30-33), serve (107), OPTIONS (108-109), the service-role JWT check (112-118), and spreads (117,132,150,158,203,210,216)
    - infra/supabase/functions/invalidate-cache/index.ts — re-read import block (~15-17), const (19-22), serve (49), OPTIONS (50-51), the Redis logic (24-47) + feed:v2:* flush-all (80), and spreads (64,72,117,123)
    - infra/supabase/functions/_shared/cors.ts — the helper being imported
    - .planning/phases/02-cors-lockdown/02-RESEARCH.md — "Edit Inventory" → enrich-dish + invalidate-cache sections
    - .planning/phases/02-cors-lockdown/02-PATTERNS.md — `enrich-dish/index.ts` (auth = JWT not CORS, DO NOT TOUCH 112-118) + `invalidate-cache/index.ts` (PERF-03 boundary, leave Redis untouched) sections
  </read_first>
  <action>
    Apply the SAME three-edit swap as Task 1 to BOTH files (re-confirm line numbers against the files you just read):

    For `enrich-dish/index.ts`: DELETE the module-level wildcard corsHeaders declaration (the module-scope object-literal `corsHeaders` constant, currently 30-33); ADD `import { buildCorsHeaders } from '../_shared/cors.ts';` near the existing imports (~23-24); INSERT `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'));` as the FIRST line of the `serve` handler (current line 107), before the OPTIONS check. Leave the spread sites (117/132/150/158/203/210/216) and the OPTIONS line (108-109) as-is. CRITICAL: do NOT touch the service-role bearer-JWT check at lines 112-118 — that is the auth boundary (JWT, not CORS, per D-09); it stays exactly as written.

    For `invalidate-cache/index.ts`: DELETE the module-level wildcard corsHeaders declaration (the module-scope object-literal `corsHeaders` constant, currently 19-22); ADD `import { buildCorsHeaders } from '../_shared/cors.ts';` near the existing imports (~15-17); INSERT `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'));` as the FIRST line of the `serve` handler (current line 49), before the OPTIONS check. Leave the spread sites (64/72/117/123) and the OPTIONS line (50-51) as-is. CRITICAL: this phase touches ONLY the CORS object — do NOT touch the Redis `deleteByPattern`/SCAN logic (24-47), the `feed:v2:*` flush-all line (80), or any cache-event logic. That is Phase 7 / PERF-03 territory (F-21 co-ownership).

    In both files keep the existing `serve` import unchanged (Phase 4 owns the serve→Deno.serve swap). After editing both, type-check all three modified functions plus the shared helper.
  </action>
  <verify>
    <automated>for f in enrich-dish invalidate-cache feed; do test "$(grep -c 'const corsHeaders = {' infra/supabase/functions/$f/index.ts)" -eq 0 || { echo "FAIL $f"; exit 1; }; done; ~/.deno/bin/deno check infra/supabase/functions/_shared/cors.ts infra/supabase/functions/feed/index.ts infra/supabase/functions/enrich-dish/index.ts infra/supabase/functions/invalidate-cache/index.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const corsHeaders = {" infra/supabase/functions/enrich-dish/index.ts` returns 0
    - `grep -c "const corsHeaders = {" infra/supabase/functions/invalidate-cache/index.ts` returns 0
    - both files contain `import { buildCorsHeaders } from '../_shared/cors.ts'`
    - both files contain `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'))`
    - `enrich-dish/index.ts` still contains its service-role auth check (the `Bearer ` / `SUPABASE_SERVICE_ROLE_KEY` 401 path) UNCHANGED
    - `invalidate-cache/index.ts` still contains the `feed:v2:*` flush-all logic UNCHANGED (Redis untouched)
    - `grep -rc "'Access-Control-Allow-Origin': '\\*'" infra/supabase/functions/feed/index.ts infra/supabase/functions/enrich-dish/index.ts infra/supabase/functions/invalidate-cache/index.ts` returns 0 for all three
    - `~/.deno/bin/deno check` on all three functions + the shared helper exits 0
  </acceptance_criteria>
  <done>enrich-dish and invalidate-cache both compute corsHeaders per-request via the shared helper with no module-level const; the enrich-dish JWT auth check and the invalidate-cache Redis/flush-all logic are untouched; all three functions + the helper type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update README CORS Configuration section</name>
  <files>infra/supabase/functions/README.md</files>
  <read_first>
    - infra/supabase/functions/README.md lines 75-81 — the current "CORS Configuration" section (documents `Access-Control-Allow-Origin: *`)
    - .planning/phases/02-cors-lockdown/02-RESEARCH.md — "Common Pitfalls" → Pitfall 1 (editing cors.ts requires redeploying all importers because the shared module is bundled per-function)
    - .planning/phases/02-cors-lockdown/02-PATTERNS.md — `README.md (MODIFY)` section (what to replicate / what differs)
    - .planning/phases/02-cors-lockdown/02-CONTEXT.md — D-01, D-02, D-10 (allowlist source, initial origins, fail-closed)
  </read_first>
  <action>
    Rewrite the "CORS Configuration" section of `infra/supabase/functions/README.md` (currently lines 75-81). Replace the `Access-Control-Allow-Origin: *` bullet with documentation of the new allowlist behavior. The updated section MUST state, in prose/bullets (keep the existing section-header + bullet-list style):

    - CORS for `feed`, `enrich-dish`, and `invalidate-cache` is now allowlist-reflecting, not wildcard: `Access-Control-Allow-Origin` is set ONLY when the request `Origin` exactly matches an entry in the `ALLOWED_ORIGINS` env var; otherwise the header is omitted.
    - `ALLOWED_ORIGINS` is a comma-separated list configured as a Supabase function secret (operator-managed, no code edit — D-01). Initial value: `https://eat-me-v1-1-admin.vercel.app,http://localhost:3001` (D-02).
    - Fail-closed (D-10): if `ALLOWED_ORIGINS` is unset, no browser origin gets `Access-Control-Allow-Origin` (never falls back to `*`); requests with no `Origin` header (native mobile / curl) still succeed — auth is enforced by JWT, not CORS.
    - The allow-headers `authorization, x-client-info, apikey, content-type` and `Vary: Origin` + `Access-Control-Allow-Methods` are always emitted.
    - The logic lives in the shared module `_shared/cors.ts`. IMPORTANT (Pitfall 1): because Supabase bundles `_shared/` into each importing function at deploy time, editing `cors.ts` later requires REDEPLOYING ALL THREE importing functions (`feed`, `enrich-dish`, `invalidate-cache`) — a change to the shared file does not propagate to already-deployed functions until each is redeployed.

    Preserve the verbatim allow-headers string in the README exactly as `authorization, x-client-info, apikey, content-type`. Do not alter unrelated README sections.
  </action>
  <verify>
    <automated>grep -q "ALLOWED_ORIGINS" infra/supabase/functions/README.md && grep -q "redeploy" infra/supabase/functions/README.md && test "$(grep -c "Access-Control-Allow-Origin: \`\?\*" infra/supabase/functions/README.md 2>/dev/null || echo 0)" -ge 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README contains `ALLOWED_ORIGINS`
    - README contains the verbatim string `authorization, x-client-info, apikey, content-type`
    - README documents the redeploy-all-importers rule (contains the word "redeploy" and references the three functions or `_shared/cors.ts`)
    - the CORS Configuration section no longer advertises `Access-Control-Allow-Origin: *` as the behavior (the wildcard bullet is replaced by allowlist prose)
    - README mentions fail-closed / no-Origin-still-succeeds behavior
  </acceptance_criteria>
  <done>The README CORS Configuration section documents the allowlist + ALLOWED_ORIGINS env var + fail-closed + no-Origin happy path + the redeploy-all-importers warning, and no longer presents wildcard `*` as the behavior.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Operator deploy + smoke checklist (stage-don't-apply, prod cutover)</name>
  <action>Blocking human-action checkpoint (no CLI/API path in the agent loop — prod deploy + secrets + smoke calls are operator-only by the stage-don't-apply constraint). Halt execution and hand the operator the ordered deploy + smoke checklist in &lt;how-to-verify&gt;: set the `ALLOWED_ORIGINS` secret FIRST, then deploy all three functions, then run the three smoke calls (SC#1/SC#2/SC#3). Do not mark the phase complete until the operator confirms per &lt;resume-signal&gt;. The agent does NOT run any of these steps.</action>
  <what-built>
    The three SEC-01 functions (`feed`, `enrich-dish`, `invalidate-cache`) are wired to the shared `_shared/cors.ts` allowlist helper, the README is updated, and the four-case Deno test is green locally. The remaining steps require prod (set secret + deploy + smoke) — these are operator-owned (stage-don't-apply); the agent cannot deploy or run prod calls.
  </what-built>
  <how-to-verify>
    Operator deploy + smoke checklist — run IN THIS ORDER (order matters: a missing secret makes SC#3 falsely appear broken because the design is fail-closed):

    STEP 1 — Set the secret FIRST (deploy from `infra/supabase/`):
      `supabase secrets set ALLOWED_ORIGINS="https://eat-me-v1-1-admin.vercel.app,http://localhost:3001"`
      (or set it in Supabase Dashboard → Edge Functions → Secrets). Confirm it is set before deploying.

    STEP 2 — Deploy ALL THREE functions (each bundles its own copy of `_shared/cors.ts`):
      `supabase functions deploy feed`
      `supabase functions deploy enrich-dish`
      `supabase functions deploy invalidate-cache`

    STEP 3 — Run the three smoke calls (mirror the 3 success criteria) against a deployed function (use `feed`):
      SC#1 (no wildcard): `curl -i` with a disallowed or absent Origin → the response has NO `Access-Control-Allow-Origin: *`.
      SC#2 (no-Origin succeeds): `curl` with NO `Origin` header (simulates mobile) → still returns 200 / a normal body (auth via JWT unaffected).
      SC#3 (preflight matches): `curl -i -X OPTIONS -H 'Origin: https://eat-me-v1-1-admin.vercel.app' <feed-url>` → response echoes `Access-Control-Allow-Origin: https://eat-me-v1-1-admin.vercel.app`, includes `Access-Control-Allow-Headers: authorization, x-client-info, apikey, content-type`, and includes `Vary: Origin`.

    If SC#3 shows no ACAO even from the admin origin, the most likely cause is the secret was not set before deploy (fail-closed) — re-run STEP 1 then redeploy.
  </how-to-verify>
  <resume-signal>Type "approved" once all three smoke calls pass, or describe which SC failed and the observed headers.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser admin → 3 edge functions | Arbitrary-origin browser JS attempts to read responses from `feed` / `enrich-dish` / `invalidate-cache`; the per-request CORS header set (from the shared helper) is the only read-access gate. |
| native mobile / curl → feed | No `Origin` header; must keep succeeding (auth via JWT, not CORS — D-09 / SC#2). |
| operator runtime config → prod | `ALLOWED_ORIGINS` secret set at deploy; if forgotten, fail-closed degrades admin browser only, never mobile or security. |

## STRIDE Threat Register

ASVS L1, primary control V14.5 (CORS). Block-on: high. The header behavior is implemented in Plan 01's `buildCorsHeaders` and proven by `cors.test.ts`; this plan's risk is wiring correctness (every site uses the per-request object; no const left behind).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | a missed spread site still emitting a stale/wildcard const | mitigate | Delete the module-level const in all 3 files so the compiler flags any unmigrated reference (RESEARCH Pitfall 2). Grep gate `const corsHeaders = {` == 0 per file + no-`'*'`-literal grep. |
| T-02-07 | Tampering (cache poisoning) | preflight vs main response header drift | mitigate | Both OPTIONS and main responses read the one per-request `corsHeaders` from the single helper (D-13) → cannot drift; `Vary: Origin` always present. |
| T-02-08 | Repudiation / scope creep | accidental edit to auth or Redis logic | mitigate | enrich-dish JWT check (112-118) and invalidate-cache Redis/`feed:v2:*` logic explicitly out of scope; acceptance criteria assert both remain present and unchanged. |
| T-02-09 | Information Disclosure | operator deploys without setting `ALLOWED_ORIGINS` | accept (fail-closed) | Fail-closed (D-10) means a missing secret degrades admin browser access only; mobile/curl unaffected, no wildcard. Operator checklist orders secret-set BEFORE smoke so SC#3 is not misread as broken. |
| T-02-SC | Tampering | npm/jsr/deno installs in this plan | accept | No package installs in this plan (it edits existing files + README and imports the in-repo `_shared/cors.ts`). RESEARCH Package Legitimacy Audit recorded zero installed packages → no blocking-human supply-chain checkpoint required. |
</threat_model>

<verification>
- `grep -c "const corsHeaders = {"` returns 0 in each of feed/enrich-dish/invalidate-cache `index.ts` (no module-level wildcard const remains anywhere).
- Each function `index.ts` contains `import { buildCorsHeaders } from '../_shared/cors.ts'` and `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'))`.
- No `'Access-Control-Allow-Origin': '*'` literal remains in any of the 3 functions.
- enrich-dish service-role JWT check and invalidate-cache Redis/`feed:v2:*` logic remain present and unchanged.
- `~/.deno/bin/deno check` on all three functions + `_shared/cors.ts` exits 0.
- README CORS section documents the allowlist, `ALLOWED_ORIGINS`, fail-closed, no-Origin happy path, and the redeploy-all-importers rule; no longer presents `*`.
- The Plan-01 Deno test remains green: `~/.deno/bin/deno test --node-modules-dir=none -A infra/supabase/functions/_shared/cors.test.ts` exits 0.
- Operator checklist (Task 4) captured for stage-don't-apply: set `ALLOWED_ORIGINS` → deploy 3 functions → run SC#1/SC#2/SC#3 (NOT an agent-executed step).
</verification>

<success_criteria>
- All three SEC-01 functions emit allowlist-reflecting CORS (no wildcard) via the per-request shared helper; OPTIONS preflight and main response carry the same header set; `Vary: Origin` + verbatim allow-headers always present (locks SC#1 + SC#3 at the code level).
- The no-Origin mobile happy path is preserved (helper omits ACAO, proceeds; auth via JWT) — locks SC#2 at the code level.
- enrich-dish auth and invalidate-cache Redis logic untouched; only the 3 named functions changed (D-07); no migrations.
- README reflects the new allowlist model + the bundling/redeploy caveat.
- The operator deploy/smoke checklist is captured and ordered (secret → deploy → smoke) for the stage-don't-apply prod cutover.
</success_criteria>

<output>
Create `.planning/phases/02-cors-lockdown/02-02-SUMMARY.md` when done. Record: the three wired functions (with the confirmed per-request first line + relative import), the README update, the green Deno test + `deno check` results, and the operator deploy/smoke checklist status (handed off, pending operator). Note the Phase 4 forward-coordination flag (serve→Deno.serve must preserve the per-request corsHeaders line + the ../_shared/cors.ts import).
</output>