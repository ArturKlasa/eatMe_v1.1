---
phase: 04-edge-dependency-pinning-script-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/supabase/functions/app-config/index.ts
  - infra/supabase/functions/group-recommendations/index.ts
  - infra/supabase/functions/invalidate-cache/index.ts
  - infra/supabase/functions/enrich-dish/index.ts
  - infra/supabase/functions/feed/index.ts
  - infra/supabase/functions/batch-update-preference-vectors/index.ts
  - infra/supabase/functions/update-preference-vector/index.ts
  - infra/supabase/functions/menu-scan-worker/index.ts
  - infra/supabase/functions/_shared/cors.test.ts
  - infra/supabase/functions/menu-scan-worker/test.ts
  - infra/supabase/functions/deno-globals.d.ts
autonomous: true
requirements: [DEBT-05]
nyquist_compliant: true

must_haves:
  truths:
    - "No edge function imports serve from deno.land/std@0.168.0/http/server — all 7 run on native Deno.serve"
    - "Every supabase-js import across all 8 functions is exactly https://esm.sh/@supabase/supabase-js@2.39.3"
    - "Every @upstash/redis import is exactly https://esm.sh/@upstash/redis@1.38.0 (no @latest)"
    - "Both Deno test files import jsr:@std/assert@1.0.19 and pass"
    - "feed/enrich-dish/invalidate-cache keep their per-request buildCorsHeaders line, ../_shared/cors.ts import, and every ...corsHeaders response spread byte-for-byte (D-02)"
    - "deno-globals.d.ts shim keys match the new specifier strings exactly (no phantom editor errors)"
  artifacts:
    - path: "infra/supabase/functions/feed/index.ts"
      provides: "feed on Deno.serve + supabase-js@2.39.3, CORS seam intact"
      contains: "Deno.serve"
    - path: "infra/supabase/functions/_shared/cors.test.ts"
      provides: "CORS unit test on jsr:@std/assert"
      contains: "jsr:@std/assert@1.0.19"
    - path: "infra/supabase/functions/deno-globals.d.ts"
      provides: "IDE shim aligned to new specifiers"
      contains: "@supabase/supabase-js@2.39.3"
  key_links:
    - from: "infra/supabase/functions/feed/index.ts"
      to: "infra/supabase/functions/_shared/cors.ts"
      via: "import { buildCorsHeaders } from '../_shared/cors.ts' (must survive serve swap)"
      pattern: "buildCorsHeaders"
    - from: "infra/supabase/functions/_shared/cors.test.ts"
      to: "jsr:@std/assert@1.0.19"
      via: "test assertion imports"
      pattern: "jsr:@std/assert@1.0.19"
---

<objective>
Track A of Phase 4: pin and de-deprecate every edge-function dependency (DEBT-05). Swap the deprecated `serve` from `std@0.168.0/http/server.ts` for native `Deno.serve` in the 7 functions still using it; collapse all `@supabase/supabase-js` specifiers to the single exact pin `https://esm.sh/@supabase/supabase-js@2.39.3`; exact-pin `@upstash/redis@1.38.0`; migrate the 2 Deno test `asserts` imports to `jsr:@std/assert@1.0.19`; and update `deno-globals.d.ts` shim keys in lockstep.

Purpose: deterministic edge cold starts on non-deprecated, exactly-pinned dependencies — closes the F-22/F-23 supply-chain drift (a floating `@2`/`@latest`/`npm:@2` specifier can silently resolve to a breaking or compromised version at cold start).

Output: 8 edge functions on one canonical specifier set + native `Deno.serve`, 2 Deno tests green on jsr asserts, an aligned IDE shim file. No business logic changes anywhere; this is mechanical plumbing.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-edge-dependency-pinning-script-guard/04-RESEARCH.md
@.planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md
@.planning/phases/04-edge-dependency-pinning-script-guard/04-VALIDATION.md

# Reference functions already on the canonical specifiers (copy these forms exactly):
@infra/supabase/functions/app-config/index.ts   # supabase-js@2.39.3 reference (line 16) — LEAVE its import; copy its specifier
@infra/supabase/functions/feed/index.ts          # upstash@1.38.0 reference (line 15) + CORS seam (lines 702-703)
@infra/supabase/functions/menu-scan-worker/index.ts  # Deno.serve reference (line 1056) — already migrated, do NOT touch its serve wrapper
</context>

<deno_runtime_note>
Deno 2.8.1 is at `~/.deno/bin/deno` and is NOT on PATH. Before any `deno` command:
`export PATH="$HOME/.deno/bin:$PATH"`
Run tests with `deno test --node-modules-dir=none -A <path>`. First run fetches deps over network (jsr.io / esm.sh), then caches. `jsr:@std/assert@1.0.19` resolves cleanly and exports every symbol the two tests use (assert, assertEquals, assertFalse, assertArrayIncludes) — verified with local Deno 2.8.1.
</deno_runtime_note>

<tasks>

<task type="auto">
  <name>Task 1: Swap serve → Deno.serve in the 7 remaining functions (CORS seam preserved)</name>
  <files>infra/supabase/functions/app-config/index.ts, infra/supabase/functions/group-recommendations/index.ts, infra/supabase/functions/invalidate-cache/index.ts, infra/supabase/functions/enrich-dish/index.ts, infra/supabase/functions/feed/index.ts, infra/supabase/functions/batch-update-preference-vectors/index.ts, infra/supabase/functions/update-preference-vector/index.ts</files>
  <read_first>
    - infra/supabase/functions/menu-scan-worker/index.ts (line 1056 — the proven `Deno.serve(async req => {` form; DO NOT edit this file in this task)
    - infra/supabase/functions/feed/index.ts (lines 702-703 — the `serve(...)` wrapper + the per-request `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'))` seam line that must survive)
    - infra/supabase/functions/enrich-dish/index.ts (lines 103-104 — same seam)
    - infra/supabase/functions/invalidate-cache/index.ts (lines 45-46 — same seam)
    - .planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md (§ serve → Deno.serve swap + § CORS seam preservation)
  </read_first>
  <action>
    Per D-01: this is a mechanical two-touch edit per function — delete the std `serve` import line, rename the `serve(` call-site token to `Deno.serve(`. Handler bodies are byte-for-byte unchanged. The 7 functions and their RESEARCH-corrected line numbers (CONTEXT.md numbers were stale):

    For EACH of the 7 functions: (1) DELETE the import line `import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';` — it is identical in all 7; (2) change the call site `serve(` to `Deno.serve(`, leaving everything after the opening paren unchanged.

    Call-site lines (approximate, RESEARCH-corrected — locate by the `serve(` token, do not blindly edit by line number): app-config import 15 / call 32; group-recommendations import 15 / call 182; invalidate-cache import 15 / call 45; enrich-dish import 23 / call 103; feed import 12 / call 702; batch-update-preference-vectors import 15 / call 41; update-preference-vector import 21 / call 183.

    CRITICAL D-02 (preserve the Phase 2 CORS seam): in feed, enrich-dish, and invalidate-cache the line immediately after the wrapper is `const corsHeaders = buildCorsHeaders(req.headers.get('Origin'));`. Do NOT touch it, do NOT touch the `import { buildCorsHeaders } from '../_shared/cors.ts';` line, and do NOT touch any `...corsHeaders` response spread (enrich-dish ~8 spreads, feed ~6, invalidate-cache ~5). The ONLY change in these 3 files is the import-delete + the `serve(`→`Deno.serve(` token. The other 4 functions use a module-level wildcard `corsHeaders` const — leave it (locking those is the Deferred "other 4 wildcard functions" item, NOT this phase).

    `Deno` is already declared in `deno-globals.d.ts` (lines 6-11), so `Deno.serve` needs no new shim. Do NOT touch `menu-scan-worker/index.ts` in this task — it is already on `Deno.serve` at line 1056.

    After editing, run the diff-discipline check below: any hunk in feed/enrich-dish/invalidate-cache that is not exactly the import-delete + the `serve(`→`Deno.serve(` token rename is a regression of the SEC-01 CORS lockdown — revert and redo.
  </action>
  <acceptance_criteria>
    - `grep -rn "std@0.168.0/http/server" infra/supabase/functions/*/index.ts` returns 0 matches
    - `grep -rln "Deno.serve" infra/supabase/functions/*/index.ts` lists all 8 functions (the 7 swapped + menu-scan-worker)
    - `grep -c "buildCorsHeaders(req.headers.get('Origin'))" infra/supabase/functions/feed/index.ts infra/supabase/functions/enrich-dish/index.ts infra/supabase/functions/invalidate-cache/index.ts` each returns >= 1 (seam line intact)
    - `grep -c "from '../_shared/cors.ts'" infra/supabase/functions/feed/index.ts infra/supabase/functions/enrich-dish/index.ts infra/supabase/functions/invalidate-cache/index.ts` each returns 1 (import intact)
    - `git diff --stat infra/supabase/functions/menu-scan-worker/index.ts` shows no change (untouched in this task)
    - `export PATH="$HOME/.deno/bin:$PATH" && for f in app-config group-recommendations invalidate-cache enrich-dish feed batch-update-preference-vectors update-preference-vector; do deno check --node-modules-dir=none infra/supabase/functions/$f/index.ts; done` exits 0 for all 7
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.deno/bin:$PATH" && grep -rc "std@0.168.0/http/server" infra/supabase/functions/*/index.ts | grep -v ':0$' | grep -c . | grep -q '^0$' && deno test --node-modules-dir=none -A infra/supabase/functions/_shared/cors.test.ts</automated>
  </verify>
  <done>All 7 functions import-deleted + on `Deno.serve`; zero `std@0.168.0/http/server` matches in `*/index.ts`; the 3 CORS-seam functions keep their `buildCorsHeaders` line, cors.ts import, and spreads byte-for-byte; all 7 pass `deno check`; cors.test.ts still green.</done>
</task>

<task type="auto">
  <name>Task 2: Pin supabase-js to @2.39.3 (8 fns) and upstash to @1.38.0; align deno-globals shim</name>
  <files>infra/supabase/functions/invalidate-cache/index.ts, infra/supabase/functions/group-recommendations/index.ts, infra/supabase/functions/enrich-dish/index.ts, infra/supabase/functions/batch-update-preference-vectors/index.ts, infra/supabase/functions/update-preference-vector/index.ts, infra/supabase/functions/feed/index.ts, infra/supabase/functions/menu-scan-worker/index.ts, infra/supabase/functions/deno-globals.d.ts</files>
  <read_first>
    - infra/supabase/functions/app-config/index.ts (line 16 — the canonical `import { createClient } from 'https://esm.sh/@supabase/supabase-js@2.39.3';` to copy; LEAVE this file unchanged)
    - infra/supabase/functions/feed/index.ts (line 15 — the canonical `import { Redis } from 'https://esm.sh/@upstash/redis@1.38.0';` to copy)
    - infra/supabase/functions/deno-globals.d.ts (full file, 64 lines — shim blocks at 16-18, 20-23, 25-32, 34-37; openai/zod 39-63 stay)
    - .planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md (§ supabase-js pinning + § upstash pinning + § deno-globals.d.ts shim edits)
  </read_first>
  <action>
    Per D-05/D-06/D-04. Canonical supabase-js = `https://esm.sh/@supabase/supabase-js@2.39.3`; canonical upstash = `https://esm.sh/@upstash/redis@1.38.0`. One specifier style (esm.sh), one exact version, across ALL functions. Chosen over the roadmap's literal "JSR" wording per D-05 (prefer-incumbent: esm.sh is the dominant existing style; least churn, no JSR runtime-resolution risk) — this is a recorded deliberate deviation, not an oversight.

    supabase-js edits (rewrite the `@2` tail to `@2.39.3`, RESEARCH-corrected lines): invalidate-cache:16, group-recommendations:16, enrich-dish:24, batch-update-preference-vectors:16, update-preference-vector:22, feed:13 — each currently `https://esm.sh/@supabase/supabase-js@2` → `https://esm.sh/@supabase/supabase-js@2.39.3`. menu-scan-worker:10 is a SPECIFIER-STYLE change: `import { createClient } from 'npm:@supabase/supabase-js@2';` → `import { createClient } from 'https://esm.sh/@supabase/supabase-js@2.39.3';` (do NOT touch its `Deno.serve` wrapper at line 1056 or its `npm:openai@4` import — those are out of scope). app-config:16 is already `@2.39.3` — LEAVE it (it is the reference).

    upstash edit: invalidate-cache:17 `import { Redis } from 'https://esm.sh/@upstash/redis@latest';` → `https://esm.sh/@upstash/redis@1.38.0`. feed:15 is already `@1.38.0` — LEAVE it (reference).

    deno-globals.d.ts shim edits (D-04 — keys MUST match the new import strings exactly or VS Code "cannot find module" errors return): (a) lines 20-23 — change the module key `'https://esm.sh/@supabase/supabase-js@2'` → `'https://esm.sh/@supabase/supabase-js@2.39.3'` (keep the `createClient` body); (b) lines 25-32 — change the module key `'https://esm.sh/@upstash/redis@latest'` → `'https://esm.sh/@upstash/redis@1.38.0'` (keep the `Redis` class body); (c) DELETE lines 34-37 entirely — the `declare module 'npm:@supabase/supabase-js@2'` block (no function imports `npm:@2` after menu-scan-worker moves to esm.sh). LEAVE the openai/zod blocks (39-63). Do NOT delete the std serve shim block here — that is Task 3's job (paired with leaving it until the serve swap is confirmed; Task 1 already removed the imports, so deleting it in Task 3 keeps the shim change set with the asserts work). [If executing Tasks in order, the serve shim block deletion happens in Task 3.]
  </action>
  <acceptance_criteria>
    - `grep -rn "supabase-js@2'" infra/supabase/functions/*/index.ts` returns 0 matches (no bare `@2` specifier remains)
    - `grep -rn "npm:@supabase/supabase-js" infra/supabase/functions/*/index.ts` returns 0 matches (menu-scan-worker moved to esm.sh)
    - every line from `grep -rn "supabase-js" infra/supabase/functions/*/index.ts` contains `esm.sh/@supabase/supabase-js@2.39.3` (run `grep -rn "supabase-js" infra/supabase/functions/*/index.ts | grep -v "esm.sh/@supabase/supabase-js@2.39.3"` → 0 matches)
    - `grep -rn "upstash/redis@latest" infra/supabase/functions` returns 0 matches
    - `grep -c "@upstash/redis@1.38.0" infra/supabase/functions/invalidate-cache/index.ts` returns 1
    - `grep -c "@supabase/supabase-js@2.39.3" infra/supabase/functions/deno-globals.d.ts` returns 1 and `grep -c "@upstash/redis@1.38.0" infra/supabase/functions/deno-globals.d.ts` returns 1
    - `grep -c "npm:@supabase/supabase-js@2" infra/supabase/functions/deno-globals.d.ts` returns 0 (block deleted)
    - `export PATH="$HOME/.deno/bin:$PATH" && deno check --node-modules-dir=none infra/supabase/functions/menu-scan-worker/index.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.deno/bin:$PATH" && test "$(grep -rn 'supabase-js' infra/supabase/functions/*/index.ts | grep -vc 'esm.sh/@supabase/supabase-js@2.39.3')" = "0" && test "$(grep -rc 'upstash/redis@latest' infra/supabase/functions/*/index.ts infra/supabase/functions/deno-globals.d.ts | grep -vc ':0$')" = "0"</automated>
  </verify>
  <done>All 8 functions on `esm.sh/@supabase/supabase-js@2.39.3`; invalidate-cache on `@upstash/redis@1.38.0`; deno-globals.d.ts has exactly one supabase-js shim (`@2.39.3`), one upstash shim (`@1.38.0`), and no `npm:@supabase/supabase-js@2` block; menu-scan-worker compiles on esm.sh supabase-js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate test asserts to jsr:@std/assert@1.0.19 and delete the dead serve shim block</name>
  <files>infra/supabase/functions/_shared/cors.test.ts, infra/supabase/functions/menu-scan-worker/test.ts, infra/supabase/functions/deno-globals.d.ts</files>
  <read_first>
    - infra/supabase/functions/_shared/cors.test.ts (line 15 — the `from 'https://deno.land/std@0.168.0/testing/asserts.ts'` import; symbols used: assert, assertEquals, assertFalse)
    - infra/supabase/functions/menu-scan-worker/test.ts (line 13 — same import; symbols used: assertEquals, assertArrayIncludes)
    - infra/supabase/functions/deno-globals.d.ts (lines 16-18 — the now-dead `declare module 'https://deno.land/std@0.168.0/http/server.ts'` serve block to delete)
    - .planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md (§ Test asserts swap)
  </read_first>
  <behavior>
    - cors.test.ts: after the import swap, `deno test --node-modules-dir=none -A infra/supabase/functions/_shared/cors.test.ts` still passes all existing cases (the 4-case CORS allowlist suite from Phase 2) — the asserts source changes, the assertions and their results do not.
    - menu-scan-worker/test.ts: after the import swap, `deno test --node-modules-dir=none -A infra/supabase/functions/menu-scan-worker/test.ts` still passes all existing cases.
  </behavior>
  <action>
    Per D-03. Change ONLY the import source string in both test files; keep the destructured symbol list exactly as-is. `jsr:@std/assert@1.0.19` is the canonical non-deprecated replacement for std `testing/asserts.ts` (there is no esm.sh equivalent for Deno std *testing*; this is the one unavoidable `jsr:` specifier in the codebase). Version `1.0.19` is the exact pin verified against local Deno 2.8.1 — it exports assert, assertEquals, assertFalse, and assertArrayIncludes (the full symbol set both tests use).

    cors.test.ts:15 — change `from 'https://deno.land/std@0.168.0/testing/asserts.ts';` → `from 'jsr:@std/assert@1.0.19';` (the `{ assert, assertEquals, assertFalse }` destructure block is unchanged).
    menu-scan-worker/test.ts:13 — change `from 'https://deno.land/std@0.168.0/testing/asserts.ts';` → `from 'jsr:@std/assert@1.0.19';` (the `{ assertEquals, assertArrayIncludes }` destructure block is unchanged).

    deno-globals.d.ts: DELETE lines 16-18 — the `declare module 'https://deno.land/std@0.168.0/http/server.ts' { export function serve(...): void; }` block. It is dead after Task 1's serve swap (`Deno.serve` is covered by the `declare namespace Deno` at lines 6-11; no module shim needed). Do NOT add a `jsr:@std/assert` shim — deno-globals.d.ts is a VS Code shim only and the test files run under real Deno which resolves `jsr:` natively. Only add a 3-line `declare module 'jsr:@std/assert@1.0.19'` block IF an actual editor "cannot find module" squiggle appears after the change (judge after; do not add pre-emptively).
  </action>
  <acceptance_criteria>
    - `grep -rn "testing/asserts" infra/supabase/functions` returns 0 matches
    - `grep -c "jsr:@std/assert@1.0.19" infra/supabase/functions/_shared/cors.test.ts` returns 1
    - `grep -c "jsr:@std/assert@1.0.19" infra/supabase/functions/menu-scan-worker/test.ts` returns 1
    - the destructured symbol blocks are unchanged: `grep -c "assertFalse" infra/supabase/functions/_shared/cors.test.ts` returns >= 1 and `grep -c "assertArrayIncludes" infra/supabase/functions/menu-scan-worker/test.ts` returns >= 1
    - `grep -c "std@0.168.0/http/server" infra/supabase/functions/deno-globals.d.ts` returns 0 (dead serve shim deleted)
    - `export PATH="$HOME/.deno/bin:$PATH" && deno test --node-modules-dir=none -A infra/supabase/functions/_shared/cors.test.ts` exits 0
    - `export PATH="$HOME/.deno/bin:$PATH" && deno test --node-modules-dir=none -A infra/supabase/functions/menu-scan-worker/test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.deno/bin:$PATH" && test "$(grep -rc 'testing/asserts' infra/supabase/functions | grep -vc ':0$')" = "0" && deno test --node-modules-dir=none -A infra/supabase/functions/_shared/cors.test.ts && deno test --node-modules-dir=none -A infra/supabase/functions/menu-scan-worker/test.ts</automated>
  </verify>
  <done>Both test files import `jsr:@std/assert@1.0.19` with unchanged symbol lists and pass under Deno; zero `testing/asserts` references remain; the dead std serve shim block is removed from deno-globals.d.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge cold-start → remote dep registry (esm.sh / jsr.io) | The Deno runtime fetches each remote specifier when a function cold-starts; an unpinned/floating specifier resolves to whatever the registry serves at that moment |
| browser caller → feed/enrich-dish/invalidate-cache | Phase 2 CORS allowlist seam; must not regress during the serve swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-A1 | Tampering | edge dependency resolution at cold start | mitigate | Exact-pin every specifier: `@supabase/supabase-js@2.39.3`, `@upstash/redis@1.38.0`, `jsr:@std/assert@1.0.19`. A floating `@2`/`@latest`/`npm:@2` could resolve to a breaking or compromised version at cold start; exact pins make resolution deterministic (DEBT-05). Verified by the grep assertions in Tasks 1-3. |
| T-04-A2 | Information Disclosure / Elevation | feed / enrich-dish / invalidate-cache CORS seam | mitigate | Preserve the Phase 2 per-request `buildCorsHeaders` line + `../_shared/cors.ts` import + every response spread byte-for-byte during the serve swap (D-02). A careless replace that drops the seam reintroduces the SEC-01 wildcard-CORS regression. Verified by the seam greps + cors.test.ts in Task 1. |
| T-04-A3 | Tampering | dependency legitimacy | accept | `@supabase/supabase-js`, `@upstash/redis`, `@std/assert` are canonical packages from official sources (Supabase, Upstash, Deno std) already in the codebase — only their version strings change. No new packages introduced; no slopsquatting surface (RESEARCH § Security Domain). |
</threat_model>

<verification>
Run after all 3 tasks (export PATH first: `export PATH="$HOME/.deno/bin:$PATH"`):

Grep assertions (all must return zero matches):
- `grep -rn "std@0.168.0/http/server" infra/supabase/functions` → 0
- `grep -rn "testing/asserts" infra/supabase/functions` → 0
- `grep -rn "upstash/redis@latest" infra/supabase/functions` → 0
- `grep -rn "supabase-js" infra/supabase/functions/*/index.ts | grep -v "esm.sh/@supabase/supabase-js@2.39.3"` → 0

Deno tests (both must pass):
- `deno test --node-modules-dir=none -A infra/supabase/functions/_shared/cors.test.ts`
- `deno test --node-modules-dir=none -A infra/supabase/functions/menu-scan-worker/test.ts`

Compile-check the 5 functions WITHOUT tests (substitutes for a unit test per RESEARCH):
- `for f in app-config group-recommendations invalidate-cache enrich-dish batch-update-preference-vectors update-preference-vector; do deno check --node-modules-dir=none infra/supabase/functions/$f/index.ts; done` (feed compile is covered by Task 1; menu-scan-worker by Task 2)

Operator-gated boundary (NOT a Claude task — stage-don't-apply): after deploy from `infra/supabase/`, the operator smoke-tests one real call per migrated function (SC#4) to confirm each cold-starts on the new pinned deps in the live edge runtime.
</verification>

<success_criteria>
- ROADMAP SC#1: `std@0.168.0/http/server` import replaced with native `Deno.serve` across all edge functions; the `testing/asserts` test import migrated in the same pass — MET when both serve and asserts grep assertions return 0.
- ROADMAP SC#2: `@supabase/supabase-js` pinned to one exact version with a single specifier style across all functions; `@upstash/redis` exact-pinned everywhere incl. invalidate-cache — MET when the supabase-js and upstash grep assertions are clean.
- ROADMAP SC#4 (Claude's half): each migrated function compiles in the local edge runtime and its Deno tests pass — MET when both Deno tests pass and all `deno check` calls exit 0. (Operator smoke-test is the deploy-time half.)
- DEBT-05 satisfied for the edge-dependency-pinning track.
</success_criteria>

<output>
Create `.planning/phases/04-edge-dependency-pinning-script-guard/04-01-SUMMARY.md` when done. Record: confirmed 7-function serve set (NOT 8 — menu-scan-worker was already migrated), the canonical specifier strings used, that the CORS seam survived byte-for-byte in the 3 seam functions, and whether a `jsr:@std/assert` editor shim was needed. Note for the operator: deploy from `infra/supabase/` then smoke-test one real call per migrated function (SC#4).
</output>
