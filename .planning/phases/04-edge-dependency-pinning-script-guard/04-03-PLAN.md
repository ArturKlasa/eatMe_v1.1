---
phase: 04-edge-dependency-pinning-script-guard
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - infra/scripts/apply-phase6-flag-fixes.ts
  - infra/scripts/backfill-cuisine-from-dishes.ts
  - infra/scripts/backfill-cuisine-from-google.ts
  - infra/scripts/backfill-cuisine-types.ts
  - infra/scripts/backfill-open-hours.ts
  - infra/scripts/backfill-restaurant-currency.ts
  - infra/scripts/backfill-restaurant-timezone.ts
  - infra/scripts/seed-cold-start-vectors.ts
  - infra/scripts/batch-embed.ts
autonomous: true
requirements: [SEC-03]
nyquist_compliant: true

must_haves:
  truths:
    - "All 8 prod-write scripts import infra/scripts/lib/prod-guard.ts and gate every mutation behind the guard's dryRun"
    - "Every guarded script defaults to dry-run — run with no flag performs zero .insert/.update/.upsert/.delete and zero writing .rpc calls"
    - "Writing requires the explicit --apply flag on every guarded script"
    - "--dry-run is accepted as a no-op on every guarded script (never errors)"
    - "Every guarded script prints the target project ref (from SUPABASE_URL host) before any mutation"
    - "The existing --limit=N sampling still narrows the candidate set in either mode (dry-run → sample → full preserved)"
    - "Read-only scripts (replay-menu-scan-ab, preview-phase6-conversion, diagnose-*, verify-*, check-favorites-duplicates, generate-phase6-flag-checklist) are NOT touched"
  artifacts:
    - path: "infra/scripts/batch-embed.ts"
      provides: "net-new dry-run gate around run_analyze_dishes + update_restaurant_vector RPCs"
      contains: "prod-guard"
    - path: "infra/scripts/backfill-cuisine-from-dishes.ts"
      provides: "default-dry-run via shared guard (was LIVE-default)"
      contains: "prod-guard"
    - path: "infra/scripts/apply-phase6-flag-fixes.ts"
      provides: "inline guard replaced by shared prod-guard import"
      contains: "prod-guard"
  key_links:
    - from: "infra/scripts/backfill-cuisine-from-dishes.ts"
      to: "infra/scripts/lib/prod-guard.ts"
      via: "import { parseGuard, announceTarget } from './lib/prod-guard'"
      pattern: "lib/prod-guard"
    - from: "infra/scripts/batch-embed.ts"
      to: "infra/scripts/lib/prod-guard.ts"
      via: "dryRun gate around the two writing RPCs"
      pattern: "lib/prod-guard"
---

<objective>
Track B completion of Phase 4: wire all 8 confirmed prod-write scripts in `infra/scripts` to the shared `prod-guard.ts` from plan 04-02 (SEC-03). Flip the 7 LIVE-default scripts to default-dry-run, add a net-new gate to `batch-embed` (which has no dry-run path today), and refactor the already-conformant `apply-phase6-flag-fixes` to consume the shared helper instead of its inline copy. After this plan, no `infra/scripts` write path can reach a prod mutation without `--apply`, every run prints the target project ref before any write, and the dry-run → sample (`--limit`) → full workflow is preserved.

Purpose: closes SEC-03. The systemic risk is the whole directory of service-role scripts that hit LIVE prod — not just the 2 named in F-12. Centralizing on one audited guard (built in 04-02) makes "default dry-run, one `--apply`, always announce the target" uniform and tamper-evident.

Output: 8 scripts on the shared guard; 8 documented CLI-contract updates in script headers; zero read-only scripts touched. Authored + locally dry-run-verified only — the operator runs the real `--apply` writes when a backfill is next needed (operational, not a phase gate).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-edge-dependency-pinning-script-guard/04-RESEARCH.md
@.planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md
@.planning/phases/04-edge-dependency-pinning-script-guard/04-VALIDATION.md

# The shared guard built in plan 04-02 (this plan's dependency):
@infra/scripts/lib/prod-guard.ts
@.planning/phases/04-edge-dependency-pinning-script-guard/04-02-SUMMARY.md

# The reference script that already has the target shape (refactor it to import the shared guard):
@infra/scripts/apply-phase6-flag-fixes.ts
</context>

<write_set_note>
Per D-08: guard ALL write-path scripts (classified by actual mutation calls, NOT service-role usage). The 8 confirmed write scripts (RESEARCH B.1, all verified on HEAD; CONTEXT.md's "~11" candidate set was corrected — `replay-menu-scan-ab.ts` and `preview-phase6-conversion.ts` are read-only and EXCLUDED):

| Script | Mutation call (file:line) | Current dry-run mechanism | --limit? | Prints ref? |
|--------|---------------------------|---------------------------|----------|-------------|
| apply-phase6-flag-fixes.ts | .insert (495,513), .delete (524) | dry-run-DEFAULT (453-455, the reference) | no | YES (455) |
| backfill-cuisine-from-dishes.ts | .update (263) | LIVE-default (48) | yes (49-50) | no |
| backfill-cuisine-from-google.ts | .update (314) | LIVE-default (42) | yes | no |
| backfill-cuisine-types.ts | .update (226) | LIVE-default (35) | no | no |
| backfill-open-hours.ts | .update (174) | LIVE-default (47) | no | no |
| backfill-restaurant-currency.ts | .update (85) | LIVE-default (31) | yes (32-33) | no |
| backfill-restaurant-timezone.ts | .update (106) | LIVE-default (34) | yes (36-37), --all (35) | no |
| seed-cold-start-vectors.ts | .upsert (214) | LIVE-default (43) | yes (44-45) | no |
| batch-embed.ts | writing RPCs run_analyze_dishes (199), update_restaurant_vector (220) | NONE — no flag at all (net-new gate, D-11) | no | no (prints ENRICH_DISH_URL only, 123) |

EXCLUDE (read-only — verified zero mutation calls, do NOT touch): replay-menu-scan-ab.ts (self-documents "does NOT write anything"), preview-phase6-conversion.ts (.select only, emits ROLLBACK), verify-phase6.ts, verify-phase7.ts, check-favorites-duplicates.ts, generate-phase6-flag-checklist.ts, all diagnose-*.ts. Adding the guard to a read-only script needlessly changes its UX; classifying by service-role-key usage is the trap (all scripts use it).
</write_set_note>

<tasks>

<task type="auto">
  <name>Task 1: Flip the 7 LIVE-default scripts to default-dry-run via the shared guard</name>
  <files>infra/scripts/backfill-cuisine-from-dishes.ts, infra/scripts/backfill-cuisine-from-google.ts, infra/scripts/backfill-cuisine-types.ts, infra/scripts/backfill-open-hours.ts, infra/scripts/backfill-restaurant-currency.ts, infra/scripts/backfill-restaurant-timezone.ts, infra/scripts/seed-cold-start-vectors.ts</files>
  <read_first>
    - infra/scripts/lib/prod-guard.ts (the shared guard — its exact `parseGuard` return shape `{ dryRun, apply, projectRef, limit }` and `announceTarget` signature)
    - infra/scripts/backfill-cuisine-from-dishes.ts (lines 48-50 — `const DRY_RUN = process.argv.includes('--dry-run');` LIVE-default + the `--limit` parse to preserve; line 263 — the `.update` gated by `!DRY_RUN`)
    - infra/scripts/seed-cold-start-vectors.ts (lines 43-46 — same LIVE-default + `--limit`; line 214 — the `.upsert` to gate)
    - .planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md (§ Write-script retrofits — Group 1)
  </read_first>
  <action>
    <!-- planner-discipline-allow: process.argv.includes('--dry-run') -->
    Per D-07/D-09/D-11/D-12. For EACH of the 7 LIVE-default scripts, replace its inline `const DRY_RUN = process.argv.includes('--dry-run');` with the shared guard, inverting the default so dry-run is the default and writing requires `--apply`.

    For each script: (1) add `import { parseGuard, announceTarget } from './lib/prod-guard';` near the top (after the existing `import 'dotenv/config';`); (2) replace the inline `DRY_RUN` derivation with `const { dryRun, projectRef, limit } = parseGuard();` — IMPORTANT: the old `DRY_RUN` meant "skip writes when true" and the guard's `dryRun` has the SAME polarity (true = no writes), but the DEFAULT flips (old: writes unless `--dry-run`; new: dry-run unless `--apply`). Update every downstream reference: rename `DRY_RUN` → `dryRun` (or assign `const DRY_RUN = dryRun;` to minimize churn) so the existing `if (!DRY_RUN) { ...mutation... }` guards now correctly require `--apply` to write; (3) call `announceTarget({ dryRun, projectRef });` before the main loop / before any mutation; (4) preserve the existing `--limit=N` parsing — EITHER keep each script's own `--limit` parse untouched OR use the guard's returned `limit` (the guard does not consume `--limit`, so both work). Do NOT break `--limit` (Pitfall 5).

    Per-script mutation guard sites to confirm still wrapped by `!dryRun` (the writes that must NOT run absent `--apply`): backfill-cuisine-from-dishes `.update` (263); backfill-cuisine-from-google `.update` (314); backfill-cuisine-types `.update` (226); backfill-open-hours `.update` (174); backfill-restaurant-currency `.update` (85); backfill-restaurant-timezone `.update` (106) — note this one also has `--all` (35), leave that flag's behavior intact; seed-cold-start-vectors `.upsert` (214).

    Several scripts already log a `Mode: DRY RUN / LIVE` line (backfill-cuisine-types:157, backfill-open-hours:190, backfill-restaurant-currency:53) — reconcile it with the guard's `announceTarget` banner so there is one clear mode statement (remove the now-redundant inline line or have it read from `dryRun`).

    Document the CLI-contract change (D-09) in each script's header comment block: dry-run is now the DEFAULT; `--apply` is required to write; `--dry-run` is an accepted no-op affirmation. Describe this in plain prose — do NOT paste the literal grep-asserted banner string into a head comment that the negative greps key on; reference the new default by concept.
  </action>
  <acceptance_criteria>
    - all 7 scripts import the guard: `grep -lc "lib/prod-guard" infra/scripts/backfill-cuisine-from-dishes.ts infra/scripts/backfill-cuisine-from-google.ts infra/scripts/backfill-cuisine-types.ts infra/scripts/backfill-open-hours.ts infra/scripts/backfill-restaurant-currency.ts infra/scripts/backfill-restaurant-timezone.ts infra/scripts/seed-cold-start-vectors.ts` → each returns >= 1
    - no script still derives a LIVE-default: `grep -rn "process.argv.includes('--dry-run')" infra/scripts/backfill-*.ts infra/scripts/seed-cold-start-vectors.ts` returns 0 matches (the inline LIVE-default idiom is gone)
    - each script calls `announceTarget`: `grep -c "announceTarget" <each script>` returns >= 1
    - `--limit` preserved: `grep -c "limit" infra/scripts/backfill-cuisine-from-dishes.ts` and `... seed-cold-start-vectors.ts` each return >= 1
    - DEFAULT-DRY-RUN behavior (the SEC-03 gate), run with a throwaway/empty SUPABASE_URL so no real prod is reachable, no flag → the script prints a DRY RUN banner and exits without reaching any mutation. For at least backfill-cuisine-from-dishes and seed-cold-start-vectors: `cd infra/scripts && SUPABASE_URL='https://throwaway.supabase.co' SUPABASE_SERVICE_ROLE_KEY='x' OPENAI_API_KEY='x' npx ts-node backfill-cuisine-from-dishes.ts 2>&1 | grep -iq "dry run"` succeeds and the run performs zero writes
    - `--dry-run` is accepted (no error): the same script run with `--dry-run` exits without an arg-parse error
  </acceptance_criteria>
  <verify>
    <automated>cd infra/scripts && test "$(grep -rln 'lib/prod-guard' backfill-cuisine-from-dishes.ts backfill-cuisine-from-google.ts backfill-cuisine-types.ts backfill-open-hours.ts backfill-restaurant-currency.ts backfill-restaurant-timezone.ts seed-cold-start-vectors.ts | wc -l)" = "7" && test "$(grep -rln "process.argv.includes('--dry-run')" backfill-cuisine-from-dishes.ts backfill-cuisine-from-google.ts backfill-cuisine-types.ts backfill-open-hours.ts backfill-restaurant-currency.ts backfill-restaurant-timezone.ts seed-cold-start-vectors.ts 2>/dev/null | wc -l)" = "0"</automated>
  </verify>
  <done>All 7 LIVE-default scripts import + use the shared guard, default to dry-run (no inline `--dry-run` LIVE-default remains), call `announceTarget` before writes, keep `--limit`, and document the flipped CLI contract in their headers.</done>
</task>

<task type="auto">
  <name>Task 2: Add a net-new guard to batch-embed.ts and refactor apply-phase6-flag-fixes.ts onto the shared guard</name>
  <files>infra/scripts/batch-embed.ts, infra/scripts/apply-phase6-flag-fixes.ts</files>
  <read_first>
    - infra/scripts/lib/prod-guard.ts (the shared guard API)
    - infra/scripts/batch-embed.ts (line 123 — prints ENRICH_DISH_URL only, no dry-run today; line 199 — `supabase.rpc('run_analyze_dishes')`; line 220 — `supabase.rpc('update_restaurant_vector', ...)` — both writing RPCs to gate)
    - infra/scripts/apply-phase6-flag-fixes.ts (lines 453-456 — the inline `apply`/`dryRun`/`SUPABASE_URL` banner to replace with the shared import; lines 495,513 `.insert`, 524 `.delete` — the mutations that stay gated by `dryRun`)
    - .planning/phases/04-edge-dependency-pinning-script-guard/04-PATTERNS.md (§ Group 2 net-new gate + § Group 3 reference)
  </read_first>
  <action>
    Per D-07/D-11.

    batch-embed.ts (net-new gate — it has NO dry-run path today, only prints ENRICH_DISH_URL at 123): (1) add `import { parseGuard, announceTarget } from './lib/prod-guard';` after `import 'dotenv/config';`; (2) at the top of `main()` (or after env validation), `const { dryRun, projectRef } = parseGuard();` then `announceTarget({ dryRun, projectRef });`; (3) wrap BOTH writing RPC call sites behind `dryRun` — `run_analyze_dishes` (199) and `update_restaurant_vector` (220): when `dryRun`, log what WOULD be run (the dish/restaurant count) and skip the `.rpc(...)` call entirely; only execute the RPCs when not `dryRun` (i.e. `--apply`); (4) document in the header that the script now defaults to dry-run and requires `--apply` to run the embedding RPCs. This is a brand-new safety gate — the existing `ENRICH_DISH_URL` print stays but is now joined by the `announceTarget` project-ref banner. Note: the per-dish `fetch(ENRICH_DISH_URL)` enrich calls (82) also trigger writes via the edge function — gate them behind `dryRun` as well so a no-flag run is truly read-only (in dry-run, log the count of dishes that WOULD be enriched and skip the fetch loop's write-causing calls).

    <!-- planner-discipline-allow: process.argv.includes('--apply') -->
    apply-phase6-flag-fixes.ts (reference refactor — already conformant): replace the inline lines 453-455 (`const apply = process.argv.includes('--apply'); const dryRun = process.argv.includes('--dry-run') || !apply;` + the inline `SUPABASE_URL` banner) with `import { parseGuard, announceTarget } from './lib/prod-guard';` and `const { dryRun, projectRef } = parseGuard();` then `announceTarget({ dryRun, projectRef });`. Its existing mutation gates (`.insert` 495/513, `.delete` 524 behind `dryRun`) stay as-is. Behavior is unchanged (it was already default-dry-run + `--apply`); this only makes it consume the single shared source of truth (D-07). Verify the `FIXES.length` count in the original banner is preserved (either fold it into the announceTarget call or keep a follow-up log line for it).
  </action>
  <acceptance_criteria>
    - both scripts import the guard: `grep -c "lib/prod-guard" infra/scripts/batch-embed.ts` >= 1 and `grep -c "lib/prod-guard" infra/scripts/apply-phase6-flag-fixes.ts` >= 1
    - apply-phase6 no longer derives the guard inline: `grep -c "process.argv.includes('--apply')" infra/scripts/apply-phase6-flag-fixes.ts` returns 0 (now comes from the shared guard)
    - batch-embed gates its RPCs: the `run_analyze_dishes` and `update_restaurant_vector` `.rpc` calls are inside a `!dryRun`/`if (dryRun) { ...skip... }` branch — `grep -c "dryRun" infra/scripts/batch-embed.ts` returns >= 2
    - both call announceTarget: `grep -c "announceTarget" infra/scripts/batch-embed.ts` >= 1 and `grep -c "announceTarget" infra/scripts/apply-phase6-flag-fixes.ts` >= 1
    - batch-embed DEFAULT-DRY-RUN (net-new gate works), run with a throwaway URL, no flag → prints a DRY RUN banner with the project ref and reaches NO `.rpc` write: `cd infra/scripts && SUPABASE_URL='https://throwaway.supabase.co' SUPABASE_SERVICE_ROLE_KEY='x' ENRICH_DISH_URL='https://throwaway.supabase.co/functions/v1/enrich-dish' npx ts-node batch-embed.ts 2>&1 | grep -iq "dry run"` succeeds
    - `cd infra/scripts && npx ts-node apply-phase6-flag-fixes.ts 2>&1 | grep -iq "dry run"` succeeds (default dry-run preserved after refactor)
  </acceptance_criteria>
  <verify>
    <automated>cd infra/scripts && grep -q "lib/prod-guard" batch-embed.ts && grep -q "lib/prod-guard" apply-phase6-flag-fixes.ts && test "$(grep -c "process.argv.includes('--apply')" apply-phase6-flag-fixes.ts)" = "0" && SUPABASE_URL='https://throwaway.supabase.co' SUPABASE_SERVICE_ROLE_KEY='x' ENRICH_DISH_URL='https://throwaway.supabase.co/functions/v1/enrich-dish' npx ts-node batch-embed.ts 2>&1 | grep -iq "dry run"</automated>
  </verify>
  <done>batch-embed gates both writing RPCs (and the write-causing enrich fetch loop) behind `dryRun` as a net-new default-dry-run gate; apply-phase6-flag-fixes consumes the shared guard instead of its inline copy with behavior unchanged; both announce the project ref; both default to dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → prod Supabase (service-role) | Each of the 8 scripts runs via ts-node with the service-role key in `infra/scripts/.env`; a write path reaches LIVE prod and bypasses RLS |
| operator intent → which project | `.env` may point at the wrong project; a silent write to the wrong/prod ref is unrecoverable |
| historical CLI muscle-memory → flipped default | The 7 LIVE-default scripts used to write without a flag; an operator's old invocation now no-ops — a usability/expectation boundary that must be made loud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Repudiation | the 8 infra/scripts prod-write paths | mitigate | Every write path now requires `--apply`; absent it, the shared guard's default dry-run skips all `.insert/.update/.upsert/.delete` and writing `.rpc` calls. `announceTarget` prints the resolved project ref (from SUPABASE_URL host) before any mutation so the operator can abort if pointed at the wrong project. Verified by per-script no-flag dry-run smokes (zero writes, ref printed). |
| T-04-04 | Tampering | batch-embed.ts (had NO gate at all) | mitigate | Net-new dry-run gate (D-11): both writing RPCs and the write-causing enrich fetch loop are wrapped behind `dryRun`. Before this plan a bare `ts-node batch-embed.ts` wrote to prod immediately; after, it defaults to a no-write preview. |
| T-04-05 | Repudiation / Denial of Service (operational) | flipped CLI contract on 7 LIVE-default scripts | accept (with documented mitigation) | The inversion is intentional (D-09) but changes behavior — an old write invocation now no-ops. Mitigated by a loud `announceTarget` banner ("DRY RUN … re-run with --apply to write") and a header-comment note in each script; residual confusion risk accepted given the solo-operator posture and the strictly safer failure mode (no-op vs accidental write). |
| T-04-06 | Tampering | mis-scoping the guard (guarding a read-only script, or missing a writer) | mitigate | Write-set classified by ACTUAL mutation calls per RESEARCH B.1 (not service-role usage). Read-only scripts (replay, preview, diagnose-*, verify-*, etc.) are explicitly excluded and untouched; the 8 writers are exactly those with insert/update/upsert/delete/writing-rpc. |
</threat_model>

<verification>
Run after both tasks (all local, no prod path):

Wiring assertions:
- All 8 write scripts import the guard: `grep -lc "lib/prod-guard" infra/scripts/apply-phase6-flag-fixes.ts infra/scripts/backfill-cuisine-from-dishes.ts infra/scripts/backfill-cuisine-from-google.ts infra/scripts/backfill-cuisine-types.ts infra/scripts/backfill-open-hours.ts infra/scripts/backfill-restaurant-currency.ts infra/scripts/backfill-restaurant-timezone.ts infra/scripts/seed-cold-start-vectors.ts infra/scripts/batch-embed.ts` → all return >= 1
- No inline LIVE-default remains: `grep -rn "process.argv.includes('--dry-run')" infra/scripts/backfill-*.ts infra/scripts/seed-cold-start-vectors.ts` → 0
- Read-only scripts untouched: `git diff --stat infra/scripts/replay-menu-scan-ab.ts infra/scripts/preview-phase6-conversion.ts infra/scripts/verify-phase6.ts infra/scripts/verify-phase7.ts infra/scripts/check-favorites-duplicates.ts infra/scripts/generate-phase6-flag-checklist.ts infra/scripts/diagnose-empty-cuisine.ts infra/scripts/diagnose-no-dishes.ts infra/scripts/diagnose-open-now.ts infra/scripts/diagnose-s3-open-hours.ts infra/scripts/diagnose-scan-quality-trend.ts` shows NO changes

Default-dry-run behavior (point env at a throwaway URL so no real prod is reachable; assert each prints DRY RUN + performs zero writes). At minimum smoke backfill-cuisine-from-dishes, seed-cold-start-vectors, and batch-embed:
- `cd infra/scripts && SUPABASE_URL='https://throwaway.supabase.co' SUPABASE_SERVICE_ROLE_KEY='x' OPENAI_API_KEY='x' npx ts-node <script>.ts 2>&1 | grep -iq "dry run"`
- `--dry-run` accepted as no-op: same run with `--dry-run` exits without an arg error

Operator-gated boundary (NOT a Claude task): when a backfill is next needed, the operator runs the script with `--apply` and confirms the project-ref banner shows the correct target before any write (VALIDATION.md Manual-Only Verifications). This plan does NOT run any real `--apply` prod write.
</verification>

<success_criteria>
- ROADMAP SC#3: `infra/scripts` write paths default to dry-run, refuse to mutate prod without an explicit `--apply`, print the target project ref before any mutation, and preserve the dry-run → sample (`--limit`) → full workflow — MET when all 8 scripts import the shared guard, no inline LIVE-default remains, every script prints the ref via `announceTarget`, `--limit` still narrows, and the dry-run smokes show zero writes.
- SEC-03 satisfied: the whole directory of prod-write scripts is gated by one audited control; read-only scripts untouched.
</success_criteria>

<output>
Create `.planning/phases/04-edge-dependency-pinning-script-guard/04-03-SUMMARY.md` when done. Record: the 8 scripts wired (7 LIVE-default flipped + batch-embed net-new gate + apply-phase6 refactored to the shared helper), confirmation that the 11 read-only scripts were left untouched, the dry-run smoke results, and the documented CLI-contract change (default dry-run; `--apply` to write; `--dry-run` no-op). Note for the operator: the real `--apply` prod-write runs are operational (run when a backfill is next needed), not a phase gate — confirm the project-ref banner each time.
</output>
