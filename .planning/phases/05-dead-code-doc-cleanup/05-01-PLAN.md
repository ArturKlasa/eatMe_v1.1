---
phase: 05-dead-code-doc-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/stores/viewModeStore.ts
  - apps/mobile/src/components/map/ViewModeToggle.tsx
  - apps/mobile/src/components/map/RestaurantMarkers.tsx
  - apps/mobile/src/screens/BasicMapScreen.tsx
  - apps/mobile/src/components/map/DailyFilterModal.tsx
  - apps/mobile/src/components/map/index.ts
  - apps/mobile/src/styles/modalScreen.ts
  - apps/mobile/src/styles/modals.ts
  - apps/mobile/src/styles/index.ts
autonomous: true
requirements: [CLEAN-01]
nyquist_compliant: true

must_haves:
  truths:
    - "apps/mobile/src/stores/viewModeStore.ts is deleted"
    - "apps/mobile/src/components/map/ViewModeToggle.tsx is deleted"
    - "apps/mobile/src/components/map/RestaurantMarkers.tsx is deleted"
    - "BasicMapScreen.tsx renders only <DishMarkers .../> — no view-mode ternary, no RestaurantMarkers import, no `mode` selector, no mapPinRestaurants memo, no handleMarkerPress"
    - "DailyFilterModal.tsx no longer imports or renders ViewModeToggle"
    - "No viewModeToggleStyles or viewModeToggleContainer definition/export remains in apps/mobile/src/styles (modalScreen.ts, modals.ts, index.ts)"
    - "components/map/index.ts no longer re-exports RestaurantMarkers"
    - "apps/mobile typecheck passes: `pnpm check-types` exits 0"
    - "filteredRestaurants, mapPinDishes, handleDishMarkerPress, handleDishPress, and the DishMarkers import are PRESERVED unchanged in BasicMapScreen.tsx"
  artifacts:
    - path: "apps/mobile/src/screens/BasicMapScreen.tsx"
      provides: "Map screen with the dish-only marker path (view-mode branch removed)"
      contains: "DishMarkers dishes={mapPinDishes}"
    - path: "apps/mobile/src/components/map/DailyFilterModal.tsx"
      provides: "Daily-filter modal with the view-mode toggle render removed"
      excludes: "ViewModeToggle"
  key_links:
    - from: "apps/mobile/src/screens/BasicMapScreen.tsx"
      to: "apps/mobile/src/components/map/DishMarkers.tsx"
      via: "the surviving (formerly else-branch) <DishMarkers dishes={mapPinDishes} onMarkerPress={handleDishMarkerPress} />"
      pattern: "DishMarkers"
---

<objective>
CLEAN-01: fully remove the map restaurant-view-mode dead code from the mobile app. The `🏪 Places` toggle is genuinely user-reachable today (imported + rendered in DailyFilterModal.tsx), so this is a UI-VISIBLE change, not an internal-only sweep (D-01 — supersedes the inaccurate F-09 "not rendered" evidence). Delete the store, the toggle component, and the restaurant-markers component, and prune every artifact the toggle removal orphans, leaving the map's dish path exactly as-is. Zero residue (D-02). This unblocks Phase 9's `BasicMapScreen` decompose, which is explicitly gated on this code being gone.

Order matters: remove all consumers/usages first (Tasks 1–2), then delete the now-orphaned files (Task 3), then verify (Task 4) — so typecheck is clean at every step and no dangling import survives. One commit for the whole track (D-11) isolates the only behavior change in Phase 5 for clean on-device bisection.

Output: 3 deleted files, 6 edited files, `pnpm check-types` green, and a clean symbol grep. The daily-filter modal then renders without the Places tab; dish markers + dish-card navigation are untouched. The behavioral check is operator on-device (no emulator in the agent loop, D-03).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dead-code-doc-cleanup/05-CONTEXT.md
@.planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md
@.planning/phases/05-dead-code-doc-cleanup/05-VALIDATION.md
</context>

<cascade_note>
The orphan cascade is bounded and VERIFIED against current code (05-RESEARCH.md § CLEAN-01). Critical keep/remove facts:
- `mapPinRestaurants` (BasicMapScreen.tsx:160) depends on `filteredRestaurants`, which is ALSO consumed by the surviving `mapPinDishes` (line 177) — so `filteredRestaurants` STAYS; the cascade stops at `mapPinRestaurants`.
- `handleMarkerPress` (lines 295–300) feeds ONLY `RestaurantMarkers` → dead, remove. `handleDishMarkerPress` and `handleDishPress` feed the dish path → KEEP (D-03 "be conservative — only remove what's proven dead").
- Deleting the 3 files is safe: each component's only consumers are the usages removed in Tasks 1–2 (verified by repo-wide grep; no barrel consumers of RestaurantMarkers).
- `apps/mobile/src/styles/REFACTORING_SUMMARY.md` mentions `viewModeToggleStyles` — it is a historical refactoring snapshot, NOT live code and NOT in scope. Leave it (D-05 principle).
</cascade_note>

<tasks>

<task type="auto">
  <name>Task 1: Prune view-mode code from BasicMapScreen.tsx</name>
  <files>apps/mobile/src/screens/BasicMapScreen.tsx</files>
  <read_first>
    - apps/mobile/src/screens/BasicMapScreen.tsx (read the whole file — confirm current line numbers; the file is 608 lines and other edits may shift them)
    - .planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md (§ CLEAN-01 → "Files to EDIT → BasicMapScreen.tsx" — the exact remove list)
  </read_first>
  <action>
    Remove every view-mode artifact from `apps/mobile/src/screens/BasicMapScreen.tsx`, leaving the dish path intact. Make these exact removals (match by content, not just line number, since earlier deletions shift lines):

    1. Delete the import: `import { useViewModeStore } from '../stores/viewModeStore';` (currently line 12).
    2. Delete the import: `import { RestaurantMarkers } from '../components/map/RestaurantMarkers';` (currently line 30). KEEP the adjacent `import { DishMarkers } from '../components/map/DishMarkers';` (line 31).
    3. Delete the selector line: `const mode = useViewModeStore(state => state.mode);` (currently line 95).
    4. Delete the entire `mapPinRestaurants` memo INCLUDING its two comment lines (currently 158–169):
       ```
       // Convert ServerRestaurant[] from the Edge Function to the shape RestaurantMarkers expects.
       // Shows ALL filtered restaurants (not just ones with recommended dishes).
       const mapPinRestaurants = useMemo(() => {
         return filteredRestaurants
           .filter(r => r.location?.lat != null && r.location?.lng != null)
           .map(r => ({ id: r.id, name: r.name, coordinates: [r.location!.lng, r.location!.lat] as [number, number], isOpen: r.is_open ?? true }));
       }, [filteredRestaurants]);
       ```
       Do NOT touch `filteredRestaurants` itself or the `mapPinDishes` memo that follows it — both stay.
    5. Delete the `handleMarkerPress` useCallback (currently lines 295–300):
       ```
       const handleMarkerPress = useCallback(
         (restaurant: { id: string; name: string; coordinates: [number, number]; isOpen: boolean }) => {
           rootNavigation.navigate('RestaurantDetail', { restaurantId: restaurant.id });
         },
         [rootNavigation]
       );
       ```
       KEEP the `// Handler functions` section comment (line 294) and the `handleDishMarkerPress` (304) + `handleDishPress` (321) callbacks below it.
    6. Replace the view-mode conditional (currently lines 509–514) — drop the comment and the ternary, keep only the dish branch:
       FROM:
       ```
       {/* Conditional markers based on view mode */}
       {mode === 'restaurant' ? (
         <RestaurantMarkers restaurants={mapPinRestaurants} onMarkerPress={handleMarkerPress} />
       ) : (
         <DishMarkers dishes={mapPinDishes} onMarkerPress={handleDishMarkerPress} />
       )}
       ```
       TO:
       ```
       <DishMarkers dishes={mapPinDishes} onMarkerPress={handleDishMarkerPress} />
       ```
  </action>
  <acceptance_criteria>
    - `grep -c "viewModeStore\|useViewModeStore" apps/mobile/src/screens/BasicMapScreen.tsx` returns 0
    - `grep -c "RestaurantMarkers" apps/mobile/src/screens/BasicMapScreen.tsx` returns 0
    - `grep -c "mapPinRestaurants" apps/mobile/src/screens/BasicMapScreen.tsx` returns 0
    - `grep -c "handleMarkerPress" apps/mobile/src/screens/BasicMapScreen.tsx` returns 0
    - `grep -c "mode === 'restaurant'" apps/mobile/src/screens/BasicMapScreen.tsx` returns 0
    - `grep -c "DishMarkers dishes={mapPinDishes}" apps/mobile/src/screens/BasicMapScreen.tsx` returns 1 (surviving dish branch)
    - `grep -c "filteredRestaurants" apps/mobile/src/screens/BasicMapScreen.tsx` returns >= 1 (the data source is preserved for mapPinDishes)
    - `grep -c "handleDishMarkerPress\|handleDishPress" apps/mobile/src/screens/BasicMapScreen.tsx` returns >= 2 (dish handlers preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && pnpm check-types</automated>
  </verify>
  <done>BasicMapScreen.tsx has no view-mode imports/selector/memo/handler/ternary; the marker render is the single `<DishMarkers dishes={mapPinDishes} onMarkerPress={handleDishMarkerPress} />`; filteredRestaurants + mapPinDishes + the dish handlers are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ViewModeToggle from DailyFilterModal + orphaned styles + barrel export</name>
  <files>apps/mobile/src/components/map/DailyFilterModal.tsx, apps/mobile/src/components/map/index.ts, apps/mobile/src/styles/modalScreen.ts, apps/mobile/src/styles/modals.ts, apps/mobile/src/styles/index.ts</files>
  <read_first>
    - apps/mobile/src/components/map/DailyFilterModal.tsx (confirm the import at line 28 and the render at ~line 145–147)
    - apps/mobile/src/styles/index.ts (the 3 references at lines 24, 45, 62)
    - apps/mobile/src/styles/modalScreen.ts (the viewModeToggleStyles block at 115–138)
    - apps/mobile/src/styles/modals.ts (the viewModeToggleContainer block at 284–287)
    - apps/mobile/src/components/map/index.ts (the RestaurantMarkers re-export at line 9)
    - .planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md (§ CLEAN-01 → style-file boundaries)
  </read_first>
  <action>
    Remove all remaining consumers and orphaned style/barrel entries (so Task 3's deletes leave no dangling reference):

    1. `apps/mobile/src/components/map/DailyFilterModal.tsx`:
       - Delete the import (line 28): `import { ViewModeToggle } from './ViewModeToggle';`
       - Delete the render block (lines 145–147): the `{/* View Mode Toggle */}` comment + `<ViewModeToggle style={modals.viewModeToggleContainer} />` + the trailing blank line. It sits between a `</View>` and a `<ScrollView` — removal leaves the layout intact (no empty wrapper).

    2. `apps/mobile/src/components/map/index.ts`:
       - Delete line 9: `export { RestaurantMarkers } from './RestaurantMarkers';`

    3. `apps/mobile/src/styles/modalScreen.ts`:
       - Delete the entire `export const viewModeToggleStyles = StyleSheet.create({ ... });` block (lines 115–138) and the blank line separating it from the prior block (114).

    4. `apps/mobile/src/styles/modals.ts`:
       - Delete the `// View mode toggle container` comment and the `viewModeToggleContainer: { marginVertical: 16 } as ViewStyle,` entry (lines 284–287).

    5. `apps/mobile/src/styles/index.ts` — 3 edits:
       - Line 24: change `export { modalScreenStyles, viewModeToggleStyles } from './modalScreen';` → `export { modalScreenStyles } from './modalScreen';`
       - Line 45: change `import { modalScreenStyles, viewModeToggleStyles } from './modalScreen';` → `import { modalScreenStyles } from './modalScreen';`
       - Delete line 62: `viewModeToggleStyles,` (the entry inside the `commonStylesBase` object).
  </action>
  <acceptance_criteria>
    - `grep -c "ViewModeToggle" apps/mobile/src/components/map/DailyFilterModal.tsx` returns 0
    - `grep -c "RestaurantMarkers" apps/mobile/src/components/map/index.ts` returns 0
    - `grep -c "viewModeToggleStyles" apps/mobile/src/styles/modalScreen.ts` returns 0
    - `grep -c "viewModeToggleContainer" apps/mobile/src/styles/modals.ts` returns 0
    - `grep -c "viewModeToggleStyles" apps/mobile/src/styles/index.ts` returns 0
    - `grep -c "modalScreenStyles" apps/mobile/src/styles/index.ts` returns >= 2 (the sibling export/import are preserved, only viewModeToggleStyles removed)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && pnpm check-types</automated>
  </verify>
  <done>No file references `ViewModeToggle`, `viewModeToggleStyles`, `viewModeToggleContainer`, or the `RestaurantMarkers` barrel export; `modalScreenStyles` and the rest of the styles barrel are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the 3 orphaned files</name>
  <files>apps/mobile/src/stores/viewModeStore.ts, apps/mobile/src/components/map/ViewModeToggle.tsx, apps/mobile/src/components/map/RestaurantMarkers.tsx</files>
  <read_first>
    - apps/mobile/src/stores/viewModeStore.ts (confirm it is the store being removed)
    - apps/mobile/src/components/map/ViewModeToggle.tsx (confirm no other importer remains after Task 2)
    - apps/mobile/src/components/map/RestaurantMarkers.tsx (confirm no other importer remains after Task 1 + barrel edit in Task 2)
  </read_first>
  <action>
    After Tasks 1–2 have removed all references, delete these three now-orphaned files:
    - `rm apps/mobile/src/stores/viewModeStore.ts`
    - `rm apps/mobile/src/components/map/ViewModeToggle.tsx`
    - `rm apps/mobile/src/components/map/RestaurantMarkers.tsx`
    (Use `git rm` if the executor prefers staged deletion; either is fine — the commit is one per D-11.)
    Do NOT delete `DishMarkers.tsx` or any other map component.
  </action>
  <acceptance_criteria>
    - `test ! -f apps/mobile/src/stores/viewModeStore.ts` (file absent)
    - `test ! -f apps/mobile/src/components/map/ViewModeToggle.tsx` (file absent)
    - `test ! -f apps/mobile/src/components/map/RestaurantMarkers.tsx` (file absent)
    - `test -f apps/mobile/src/components/map/DishMarkers.tsx` (survivor present)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && pnpm check-types</automated>
  </verify>
  <done>The store, toggle, and restaurant-markers files are gone; DishMarkers and the rest of the map components remain; typecheck still passes (no dangling imports).</done>
</task>

<task type="auto">
  <name>Task 4: Verify clean removal (typecheck + symbol grep)</name>
  <files>apps/mobile/src/screens/BasicMapScreen.tsx</files>
  <read_first>
    - .planning/phases/05-dead-code-doc-cleanup/05-VALIDATION.md (§ Per-Task Verification Map — CLEAN-01 rows)
  </read_first>
  <action>
    Run the CLEAN-01 validation surface and confirm both signals are green:
    1. Typecheck: `cd apps/mobile && pnpm check-types` exits 0 (the SC#1 gate — catches any orphaned ref).
    2. Symbol sweep: `grep -rn "viewModeStore\|useViewModeStore\|ViewModeToggle\|RestaurantMarkers\|viewModeToggle" apps/mobile/src` returns ONLY `apps/mobile/src/styles/REFACTORING_SUMMARY.md` (the historical snapshot, intentionally left). Any other hit = an orphan to fix.
    If either fails, fix the residual reference and re-run before completing.
  </action>
  <acceptance_criteria>
    - `cd apps/mobile && pnpm check-types` exits 0
    - `grep -rn "viewModeStore\|useViewModeStore\|ViewModeToggle\|RestaurantMarkers\|viewModeToggle" apps/mobile/src | grep -v "REFACTORING_SUMMARY.md"` returns no lines
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && pnpm check-types && ! grep -rn "viewModeStore\|useViewModeStore\|ViewModeToggle\|RestaurantMarkers\|viewModeToggle" apps/mobile/src | grep -v "REFACTORING_SUMMARY.md"</automated>
  </verify>
  <done>Typecheck is green and no live view-mode symbol remains anywhere in apps/mobile/src (only the historical REFACTORING_SUMMARY.md mentions it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only deletes client-side React Native code and removes a UI toggle. No network, auth, data-flow, or trust boundary is added, changed, or crossed. The removed `RestaurantMarkers`/view-mode path read the same already-fetched, already-authorized feed data the dish path reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (regression) | BasicMapScreen dish path | accept | Risk is "deletion breaks the surviving dish render," not a security threat. Mitigated by the conservative keep-list (filteredRestaurants/mapPinDishes/dish handlers preserved), `pnpm check-types`, and the operator on-device check (VALIDATION.md Manual-Only). No exploitable surface. |
| T-05-02 | Information disclosure | removed restaurant markers | accept | Removing a UI surface cannot disclose data; it removes a render path. No data exposure created. |

No high/medium-severity threats. This is dead-code deletion with no new attack surface (security_block_on = high → nothing to block).
</threat_model>

<verification>
- `cd apps/mobile && pnpm check-types` exits 0 (SC#1 primary gate — proves no orphaned reference survived the cascade).
- `grep -rn "viewModeStore\|useViewModeStore\|ViewModeToggle\|RestaurantMarkers\|viewModeToggle" apps/mobile/src` → only REFACTORING_SUMMARY.md (zero live residue, D-02).
- The 3 target files are deleted; DishMarkers + the dish path are untouched.
- **Operator on-device (manual, D-03):** open the map → daily-filter modal renders with NO `🏪 Places` tab; tap a dish marker → opens the dish's restaurant; tap a footer dish card → opens restaurant with the dish featured. No emulator in the agent loop, so this is an operator sign-off, not an automated gate.
</verification>

<success_criteria>
- ROADMAP SC#1: the map restaurant-view-mode dead code is gone — `viewModeStore`, `ViewModeToggle`, and the dead branch in `BasicMapScreen` are removed and `pnpm check-types` passes. MET when Tasks 1–4 complete and typecheck is green.
- CLEAN-01 satisfied with zero residue (D-02) and the dish path behavior-preserved (D-03), unblocking the Phase 9 BasicMapScreen decompose.
</success_criteria>

<output>
Create `.planning/phases/05-dead-code-doc-cleanup/05-01-SUMMARY.md` when done. Record: the final files deleted/edited, that `pnpm check-types` passed, the symbol-grep result, and a clear note that the on-device verification is PENDING operator sign-off (the only behavior change in Phase 5). Commit as one commit for the CLEAN-01 track (D-11), straight to main.
</output>
