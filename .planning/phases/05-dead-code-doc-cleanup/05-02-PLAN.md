---
phase: 05-dead-code-doc-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - agent_docs/architecture.md
  - .github/copilot-instructions.md
  - INTEGRATION_COMPLETE_SUMMARY.md
  - agent_docs/conventions.md
  - agent_docs/commands.md
  - PROMPT.md
autonomous: true
requirements: [CLEAN-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Zero live `apps/web-portal` imports exist anywhere in apps/, packages/, infra/ (re-confirmed, excluding web-portal-v2)"
    - "CLAUDE.md no longer describes apps/web-portal as a current app/test target (lines 8, 21, 29-30 rewritten), EXCEPT line 62's DishKind-shim mention which is intentionally DEFERRED to Phase 6 (D-07)"
    - "agent_docs/architecture.md structure tree + dependency graph reflect current reality (apps/admin as the portal/admin surface, web-portal-v2 on ice) with no structural gap"
    - ".github/copilot-instructions.md no longer presents apps/web-portal as the live owner portal"
    - "INTEGRATION_COMPLETE_SUMMARY.md, agent_docs/conventions.md, agent_docs/commands.md, and PROMPT.md no longer reference apps/web-portal (v1) as a present/current path"
    - "web-portal-v2 references are PRESERVED (on ice, not deleted); the two code-comment provenance refs (menu-scan-worker/index.ts, backfill-cuisine-from-google.ts) are LEFT UNTOUCHED (D-05)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Session-loaded project guide with an accurate app inventory (no live web-portal claim)"
      excludes: "apps/web-portal/`): Next.js"
    - path: "agent_docs/architecture.md"
      provides: "Architecture doc whose structure tree matches the real apps/ layout"
      excludes: "web-portal/          # Next.js 16 + React 19 restaurant owner portal"
  key_links: []
---

<objective>
CLEAN-02: purge residual `apps/web-portal` references from agent-facing docs. The app was deleted AND committed (`c1a7e3f`, 2026-06-18) and the workspace/build config is already clean — so SC#2's deletion is already done; this plan is residual-DOC cleanup only (per FINDINGS F-03/F-04/F-08 and the ROADMAP scope note). Scope is all 7 agent-facing docs that still describe web-portal as a current app, not just the 4 named in the roadmap (D-04) — leaving the extra 3 (`conventions.md`, `commands.md`, `PROMPT.md`) would defeat the "stop misleading readers" goal.

Edit style depends on the doc (D-06): for docs that describe LIVE architecture (CLAUDE.md, architecture.md, copilot-instructions.md), rewrite to current reality — `apps/admin` is the portal/admin surface, `apps/web-portal-v2` is on ice — don't gut a line and leave a structural gap (CLAUDE.md loads every agent session, so its accuracy matters most). For purely historical records (INTEGRATION_COMPLETE_SUMMARY.md), scrubbing/removing the ref is acceptable.

Two explicit guardrails: (1) PRESERVE every `web-portal-v2` reference — that app is on ice, not deleted (memory: v2 paused not abandoned). (2) DEFER `CLAUDE.md:62` to Phase 6 — its web-portal mention is inside the DishKind-shim guidance that Phase 6 (DEBT-03) will rewrite when the shims are removed (D-07); half-editing it now is wrong. Leave the two code-comment provenance refs untouched (D-05). Documentation-only; no code or behavior change.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dead-code-doc-cleanup/05-CONTEXT.md
@.planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md
@.planning/phases/05-dead-code-doc-cleanup/05-VALIDATION.md
</context>

<current_reality_note>
When rewriting (not scrubbing) a doc, the accurate current state is:
- `apps/mobile/` — Expo/React Native consumer app (live).
- `apps/admin/` — the sole admin/portal surface (port 3001), Next.js. The legacy `apps/web-portal/` owner portal was DELETED + committed `c1a7e3f` on 2026-06-18.
- `apps/web-portal-v2/` — owner portal rebuild, ON ICE (paused, not abandoned — do NOT describe it as active, do NOT delete its references).
- `packages/` — database, shared, tokens (unchanged).
Per-file dispositions are enumerated in 05-RESEARCH.md § CLEAN-02 (a line-by-line inventory of all web-portal hits in each of the 7 files). Read it before editing.
</current_reality_note>

<tasks>

<task type="auto">
  <name>Task 1: Confirm build-clean (SC#2 evidence — zero live web-portal imports)</name>
  <files>.planning/phases/05-dead-code-doc-cleanup/05-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-dead-code-doc-cleanup/05-CONTEXT.md (D-10 — grep is the build-clean evidence; full `turbo build` only if grep surfaces something)
    - .planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md (§ CLEAN-02 → "Build-clean evidence")
  </read_first>
  <action>
    Re-run the build-clean confirmation that proves the `apps/web-portal` deletion is clean against the workspace (SC#2 first clause). This is a confirmation step, not a removal (deletion already committed `c1a7e3f`):
    Run: `grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2`
    Expected: ZERO matches. If it returns ZERO, SC#2's build-clean clause holds via grep and a full `turbo build` is unnecessary (D-10). If it returns ANY live import, STOP and escalate to a full `turbo build` to assess — do not proceed to doc edits until the import situation is understood (it would mean the deletion was not actually clean).
    Record the grep result (and that it was zero) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2` returns no lines (zero live imports)
  </acceptance_criteria>
  <verify>
    <automated>! grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2</automated>
  </verify>
  <done>Zero live `apps/web-portal` imports confirmed across apps/packages/infra (excluding web-portal-v2) — SC#2 build-clean evidence established by grep; no full build needed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the live-architecture docs to current reality</name>
  <files>CLAUDE.md, agent_docs/architecture.md, .github/copilot-instructions.md</files>
  <read_first>
    - CLAUDE.md (lines 8, 21, 29-30 to rewrite; line 62 to LEAVE — deferred to Phase 6)
    - agent_docs/architecture.md (lines 9, 24, 38, 61)
    - .github/copilot-instructions.md (all ~20 web-portal hits — the heaviest file; see RESEARCH inventory)
    - .planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md (§ CLEAN-02 per-file disposition table)
  </read_first>
  <action>
    Rewrite (do not gut) the web-portal references in the three live-architecture docs to reflect current reality (see current_reality_note). Concrete edits:

    **CLAUDE.md:**
    - Line 8 (`- **Web Portal** (apps/web-portal/): Next.js 16 + React 19, shadcn/ui, ...`): retarget to the live admin surface — e.g. `- **Admin Portal** (apps/admin/): Next.js admin/portal surface (port 3001).` and, if the tech-stack list should note the owner-portal rebuild, add that `apps/web-portal-v2/` is the owner-portal rebuild currently on ice. Do not leave a bare gap in the stack list.
    - Lines 21 & 29-30 (the `turbo test` "web-portal Vitest suite" comment and the `cd apps/web-portal && npx vitest` app-specific block): these describe a deleted test target. Retarget to the real current test surface (e.g. the web-portal-v2 Vitest suite if that is where tests now live, or remove the web-portal-specific lines if no equivalent live target exists). Match whatever `turbo test` actually runs today — read the root `turbo.json`/`package.json` test wiring if unsure; do NOT invent a command.
    - Line 62: LEAVE UNCHANGED. Its web-portal mention is inside the DishKind-shim sentence that Phase 6 (DEBT-03) rewrites when the shims are removed (D-07).

    **agent_docs/architecture.md:**
    - Line 9 (structure tree `└── web-portal/  # Next.js 16 + React 19 restaurant owner portal`): replace with the real current entry — `admin/` (and/or `web-portal-v2/  # owner portal rebuild — on ice`). Keep the tree well-formed (no dangling branch).
    - Line 24 (dep graph `apps/web-portal → @eatme/database, @eatme/shared, @eatme/tokens`): retarget to `apps/admin` (and/or note web-portal-v2). Keep the graph consistent with the tree.
    - Line 38 (`Pattern: apps/web-portal/app/onboard/...`): replace the example path with a live analog, or generalize the pattern description so it no longer points at a deleted path.
    - Line 61 (`docs/project/04-web-portal.md — Web portal architecture details`): if that doc still exists and describes the deleted app, either drop the line or annotate it as historical; if it describes the current portal, retarget the label. Read the referenced doc's first lines to decide.

    **.github/copilot-instructions.md (heaviest — ~20 hits):**
    - This file describes `apps/web-portal` as the live owner portal throughout (overview line 8; onboarding/storage/geo patterns; commands `turbo dev --filter=web-portal`, `cd apps/web-portal`; form/localStorage/components/ingredients/auth refs; test count "49 Vitest test files"). Rewrite the live-architecture statements to current reality (admin surface; v2 on ice) and remove/retarget the v1-specific path examples (`apps/web-portal/lib/...`, `apps/web-portal/app/onboard/...`, `apps/web-portal/components/...`). Where a pattern is generic (e.g. PostGIS POINT format, LocalStorage auto-save) keep the guidance but drop the dead `apps/web-portal/...` path. Note: several refs (ingredients.ts, AllergenWarnings, allergen lookup) point at ABANDONED allergen/ingredient features — drop those entirely rather than retargeting (allergens are abandoned; see CLAUDE.md "Allergens & Dietary Tags — Abandoned").

    Throughout: PRESERVE any `web-portal-v2` mention. Do NOT touch CLAUDE.md:62.
  </action>
  <acceptance_criteria>
    - `grep -n "web-portal" CLAUDE.md | grep -v "web-portal-v2" | grep -v "DISH_KIND_META"` returns no lines (every live-app web-portal ref is gone; the only surviving one is the deferred DishKind-shim line, identified by its `DISH_KIND_META` content — NOT by line number, since rewriting lines 8/21/29-30 shifts the line numbers; web-portal-v2 mentions are allowed). NOTE: use the trailing-slash form `apps/web-portal/` or an explicit `grep -v web-portal-v2` — a bare `apps/web-portal\b` ALSO matches `apps/web-portal-v2` (the `-` is a word boundary) and would wrongly flag the preserved v2 refs.
    - `grep -c "apps/web-portal/" agent_docs/architecture.md` returns 0 (the trailing slash matches only v1 paths, not `apps/web-portal-v2/`)
    - `grep -c "restaurant owner portal" agent_docs/architecture.md` returns 0 (the structure-tree label no longer describes web-portal as the owner portal)
    - `grep -c "apps/web-portal/" .github/copilot-instructions.md` returns 0 (no live v1 path remains; the trailing slash leaves web-portal-v2 untouched)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -n "web-portal" CLAUDE.md | grep -v 'web-portal-v2' | grep -v 'DISH_KIND_META' | wc -l)" = "0"</automated>
  </verify>
  <done>CLAUDE.md (except the deferred DishKind-shim line), architecture.md, and copilot-instructions.md describe the current app reality (admin surface; v2 on ice) with no structural gaps; no live `apps/web-portal/` path remains; every web-portal-v2 reference is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Scrub the historical/minor docs</name>
  <files>agent_docs/conventions.md, agent_docs/commands.md, INTEGRATION_COMPLETE_SUMMARY.md, PROMPT.md</files>
  <read_first>
    - agent_docs/conventions.md (line 6 — the kebab-case example)
    - agent_docs/commands.md (lines 10, 15 — the test command + Web Portal section)
    - INTEGRATION_COMPLETE_SUMMARY.md (lines 57, 200-203, 207-208 — historical record)
    - PROMPT.md (the v1-only lines 13, 58, 179; everything else is web-portal-v2 → KEEP)
    - .planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md (§ CLEAN-02 per-file disposition — esp. the PROMPT.md v1-vs-v2 distinction)
  </read_first>
  <action>
    Scrub the residual v1 web-portal references from the four remaining docs. Edit style = remove/retarget the dead ref (these are minor or historical; D-06 allows scrubbing):

    **agent_docs/conventions.md:**
    - Line 6 (`Directories: kebab-case (menu-scan/, web-portal/)`): swap the `web-portal/` example for a live directory (e.g. `admin/` or `mobile/`). One-token change; keep the convention intact.

    **agent_docs/commands.md:**
    - Line 10 (`| turbo test | Run test suites (web-portal Vitest) |`): retarget the parenthetical to the real current test surface (match what `turbo test` runs today; do not invent).
    - Line 15 (`## Web Portal (apps/web-portal/)` section header + its commands): either retarget the section to `apps/admin` / `apps/web-portal-v2` if those have equivalent commands, or remove the dead section. Do not leave commands that `cd apps/web-portal`.

    **INTEGRATION_COMPLETE_SUMMARY.md (historical record — scrubbing fine):**
    - Line 57 (`cd apps/web-portal`) and lines 200-203, 207-208 (the `apps/web-portal/lib/ingredients.ts`, `components/IngredientAutocomplete.tsx`, `AllergenWarnings.tsx`, `DietaryTagBadges.tsx`, `forms/DishFormDialog.tsx`, `app/onboard/menu/page.tsx` file list): these reference both the deleted app AND abandoned allergen/ingredient features. Remove the dead `apps/web-portal/...` lines (or the whole stale file-list block, which is cleaner). This is a point-in-time integration record; scrubbing the dead refs is acceptable.

    **PROMPT.md (mostly web-portal-v2 — surgical):**
    - This is a v2 planning snapshot; the BULK of its `web-portal` hits are `web-portal-v2` → KEEP. Edit ONLY the three lines that reference the v1 path `apps/web-portal/` as still-present:
      - Line 13 (`at apps/web-portal/ stays untouched until DNS cutover`)
      - Line 58 (`apps/web-portal/ (v1). No unlisted changes...`)
      - Line 179 (`No edits to apps/web-portal/ — v1 is out of scope`)
      These are moot v2-build constraints about not touching v1; since v1 is deleted, **remove the literal `apps/web-portal/` path** — rewrite each sentence to past tense ("v1 was deleted 2026-06-18") or drop the now-irrelevant constraint clause, such that no bare `apps/web-portal/` v1 path string survives. Do NOT alter any `apps/web-portal-v2/` or `apps/admin/` line.

    Leave the two code-comment provenance refs (menu-scan-worker/index.ts, backfill-cuisine-from-google.ts) untouched (D-05) — they are NOT in this plan's files_modified.
  </action>
  <acceptance_criteria>
    - `grep -c "web-portal/" agent_docs/conventions.md` returns 0 (the kebab-case example no longer uses `web-portal/`)
    - `grep -c "apps/web-portal/" agent_docs/commands.md` returns 0 (trailing slash = v1-only; the dead command section is removed/retargeted)
    - `grep -c "apps/web-portal/" INTEGRATION_COMPLETE_SUMMARY.md` returns 0
    - `grep -c "apps/web-portal/" PROMPT.md` returns 0 (no v1 path survives — the trailing slash matches only v1, never `apps/web-portal-v2/`)
    - `grep -c "web-portal-v2" PROMPT.md` returns >= 8 (every v2 ref preserved)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c "apps/web-portal/" INTEGRATION_COMPLETE_SUMMARY.md)" = "0" && grep -q "web-portal-v2" PROMPT.md</automated>
  </verify>
  <done>conventions.md, commands.md, and INTEGRATION_COMPLETE_SUMMARY.md no longer reference the deleted apps/web-portal; PROMPT.md's only edits are to its three v1-present lines (all web-portal-v2 content preserved).</done>
</task>

<task type="auto">
  <name>Task 4: Verify residual-doc cleanup</name>
  <files>.planning/phases/05-dead-code-doc-cleanup/05-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-dead-code-doc-cleanup/05-VALIDATION.md (§ Per-Task Verification Map — CLEAN-02 rows)
  </read_first>
  <action>
    Confirm the cleanup is complete and the guardrails held:
    1. No live web-portal (v1) path remains across the 7 target docs EXCEPT the intentionally-deferred CLAUDE.md:62:
       `grep -rn "apps/web-portal/" CLAUDE.md agent_docs/architecture.md .github/copilot-instructions.md INTEGRATION_COMPLETE_SUMMARY.md agent_docs/conventions.md agent_docs/commands.md PROMPT.md | grep -v "web-portal-v2"` → only acceptable residue (CLAUDE.md:62 if it still uses a path form; PROMPT.md historical annotations if you chose to annotate rather than remove).
    2. web-portal-v2 references survived: `grep -rc "web-portal-v2" PROMPT.md` returns >= 8.
    3. Build-clean still holds: `grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2` → zero (unchanged from Task 1; docs edits can't affect this, but re-confirm as the SC#2 anchor).
    Record all three results in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2` returns no lines
    - `grep -n "web-portal" CLAUDE.md | grep -v 'web-portal-v2' | grep -v 'DISH_KIND_META'` returns no lines (the only surviving CLAUDE.md web-portal ref is the deferred DishKind-shim line)
    - `grep -c "web-portal-v2" PROMPT.md` returns >= 8 (v2 preserved)
  </acceptance_criteria>
  <verify>
    <automated>! grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2</automated>
  </verify>
  <done>Across all 7 docs, the only surviving web-portal (v1) reference is the deferred CLAUDE.md:62 (and any chosen PROMPT.md historical annotations); web-portal-v2 refs intact; build-clean grep still zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits Markdown documentation only. No code, config, dependency, network, or data path is modified. No trust boundary exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information disclosure (doc accuracy) | agent-facing docs | accept | The only "risk" is the inverse: stale docs MISLEAD readers. This plan reduces that. No secret, credential, or sensitive path is added — the removed refs point at a deleted app. CLAUDE.md is the highest-impact file (loads every session); its rewrite is verified by grep to match current reality. |

No security threats — documentation-only change with no executable surface (security_block_on = high → nothing to block).
</threat_model>

<verification>
- `grep -rn "from ['\"].*apps/web-portal" apps packages infra | grep -v web-portal-v2` → ZERO (SC#2 build-clean anchor; D-10 — no full `turbo build` needed unless this surfaces something).
- Across the 7 target docs, no live `apps/web-portal/` path remains except the deferred CLAUDE.md:62.
- Every `web-portal-v2` reference is preserved (PROMPT.md still has its v2 content); the two code-comment provenance refs are untouched (out of scope, D-05).
- CLAUDE.md, architecture.md, copilot-instructions.md read as current reality (admin surface; v2 on ice) — no structural gaps.
</verification>

<success_criteria>
- ROADMAP SC#2: the `apps/web-portal` deletion is verified clean against workspace/build (grep = zero live imports), and residual doc references are removed from CLAUDE.md, agent_docs/architecture.md, .github/copilot-instructions.md, and INTEGRATION_COMPLETE_SUMMARY.md — plus the 3 additional agent-facing docs (D-04). MET when Tasks 1–4 pass.
- CLEAN-02 satisfied: docs stop misleading readers, web-portal-v2 (on ice) preserved, CLAUDE.md:62 correctly deferred to Phase 6.
</success_criteria>

<output>
Create `.planning/phases/05-dead-code-doc-cleanup/05-02-SUMMARY.md` when done. Record: the zero-live-imports grep result, the per-file edits made (and that CLAUDE.md:62 + web-portal-v2 + provenance refs were intentionally left), and any doc where the "current reality" retarget required reading the live test wiring (turbo.json) to avoid inventing a command. Commit as one commit for the CLEAN-02 track (D-11), straight to main.
</output>
