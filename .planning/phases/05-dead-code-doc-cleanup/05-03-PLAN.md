---
phase: 05-dead-code-doc-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/supabase/functions/enrich-dish/index.ts
autonomous: true
requirements: [CLEAN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "enrich-dish/index.ts header (lines ~8-21) no longer mentions `ingredient`, `parent dish`, `parent ingredients`, or `variant`"
    - "The corrected header retains `option_group` (its trigger trg_enrich_on_option_group_change is a migration-151 keep) and the `_trg_after_dish_embedded` centroid line (D-09)"
    - "The header's load description matches the ACTUAL load logic: dish + option groups + restaurant cuisine (verified at lines 135-164, F-06)"
    - "enrich-dish/index.ts still type-checks: `deno check` exits 0"
    - "No runtime code changed — only header comment lines"
  artifacts:
    - path: "infra/supabase/functions/enrich-dish/index.ts"
      provides: "enrich-dish edge function with a header comment that accurately describes its current load logic"
      excludes: "parent ingredients"
  key_links: []
---

<objective>
CLEAN-03: correct the stale header comment block in `infra/supabase/functions/enrich-dish/index.ts` so it describes what the function ACTUALLY loads today, not the retired ingredient/parent-dish pipeline. Match the comment to the code (D-08), not just "less wrong": the verified current load (lines 135-164, per F-06) is **dish + option groups + restaurant cuisine** — no ingredients, no parent dish. Two stale spots: line 9 (`dish/ingredient/option_group writes`) and lines 14-15 (`Load dish + ingredients + option groups + restaurant cuisine + parent dish (when this is a variant) + parent ingredients`).

Keep `option_group` in the wording — its trigger `trg_enrich_on_option_group_change` is one of the three triggers migration 151 explicitly kept alive (`trg_enrich_on_dish_change`, `after_dish_embedded`, `trg_enrich_on_option_group_change`). Keep the `_trg_after_dish_embedded` centroid line (lines 20-21). Drop every `ingredient` / `parent dish` / `parent ingredients` / `variant` reference (D-09). This is documentation-only — no runtime change, no behavior change. Edge functions are Deno (cannot import workspace packages); validate with `deno check`, not tsc.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dead-code-doc-cleanup/05-CONTEXT.md
@.planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md
@.planning/phases/05-dead-code-doc-cleanup/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the enrich-dish header comment</name>
  <files>infra/supabase/functions/enrich-dish/index.ts</files>
  <read_first>
    - infra/supabase/functions/enrich-dish/index.ts (read the header lines 1-33 AND the load logic at lines 130-175 — confirm it loads dishes.select(...), option_groups.select('name, options(name)'), restaurants.select('cuisine_types'), and NO ingredient/parent query, so the corrected comment is accurate per D-08)
    - infra/supabase/migrations/151_retire_ingredient_triggers.sql (lines 44-46 — confirms the 3 surviving triggers; ingredient triggers were retired here)
    - .planning/phases/05-dead-code-doc-cleanup/05-RESEARCH.md (§ CLEAN-03 → the exact before/after rewrite)
  </read_first>
  <action>
    Edit ONLY the header comment in `infra/supabase/functions/enrich-dish/index.ts`. Make exactly these two changes; touch nothing else in the file.

    1. Line 9 — drop `ingredient`, keep `dish` and `option_group`:
       FROM: `//   - _trg_notify_enrich_dish trigger on dish/ingredient/option_group writes`
       TO:   `//   - _trg_notify_enrich_dish trigger on dish/option_group writes`

    2. Lines 14-15 — collapse the two-line load description to a single accurate line (drop `ingredients`, `parent dish (when this is a variant)`, and `parent ingredients`):
       FROM:
       ```
       //   1. Load dish + ingredients + option groups + restaurant cuisine
       //      + parent dish (when this is a variant) + parent ingredients
       ```
       TO:
       ```
       //   1. Load dish + option groups + restaurant cuisine
       ```
       (The pipeline steps stay numbered 1-4; only step 1's content shrinks. Delete the wrapped continuation line entirely.)

    KEEP unchanged: lines 4-6 (embedding-generation summary), lines 16-18 (pipeline steps 2-4), and lines 20-21 (the `_trg_after_dish_embedded` centroid note — D-09). Do NOT alter any code below the header (imports, getEmbedding, the load logic, the writes). This is a comment-only edit.
  </action>
  <acceptance_criteria>
    - `sed -n '8,21p' infra/supabase/functions/enrich-dish/index.ts | grep -c "ingredient"` returns 0
    - `sed -n '8,21p' infra/supabase/functions/enrich-dish/index.ts | grep -ci "parent"` returns 0
    - `sed -n '8,21p' infra/supabase/functions/enrich-dish/index.ts | grep -c "option_group\|option groups"` returns >= 1 (option_group retained)
    - `grep -c "_trg_after_dish_embedded" infra/supabase/functions/enrich-dish/index.ts` returns >= 1 (centroid line retained)
    - `grep -c "Load dish + option groups + restaurant cuisine" infra/supabase/functions/enrich-dish/index.ts` returns 1
    - `grep -c "createClient\|getEmbedding\|EMBEDDING_MODEL" infra/supabase/functions/enrich-dish/index.ts` is unchanged (no code touched)
  </acceptance_criteria>
  <verify>
    <automated>~/.deno/bin/deno check infra/supabase/functions/enrich-dish/index.ts</automated>
  </verify>
  <done>The enrich-dish header describes dish + option groups + restaurant cuisine, retains option_group and the _trg_after_dish_embedded centroid line, and contains no ingredient/parent/variant reference; `deno check` passes; no runtime code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Validate enrich-dish (deno check + grep)</name>
  <files>infra/supabase/functions/enrich-dish/index.ts</files>
  <read_first>
    - .planning/phases/05-dead-code-doc-cleanup/05-VALIDATION.md (§ Per-Task Verification Map — CLEAN-03 rows)
    - .planning/phases/05-dead-code-doc-cleanup/05-CONTEXT.md (code_context — enrich-dish is Deno; validate with deno check, not tsc)
  </read_first>
  <action>
    Confirm both CLEAN-03 signals. Deno is installed at `~/.deno` and is NOT on PATH — invoke it by full path (`~/.deno/bin/deno`). Edge functions live under `infra/supabase/functions/`.
    1. Type-check: `~/.deno/bin/deno check infra/supabase/functions/enrich-dish/index.ts` exits 0. (A comment-only change cannot break types, but this confirms nothing else moved.)
    2. Header accuracy grep: lines 8-21 contain no `ingredient` and no `parent`; `option_group` and `_trg_after_dish_embedded` are still present.
    If `deno` is unreachable at that path, locate it (`command -v deno || ls ~/.deno/bin/deno`) and use the resolved path; do not skip the check.
  </action>
  <acceptance_criteria>
    - `~/.deno/bin/deno check infra/supabase/functions/enrich-dish/index.ts` exits 0
    - `sed -n '8,21p' infra/supabase/functions/enrich-dish/index.ts | grep -ciE "ingredient|parent|variant"` returns 0
    - `sed -n '8,21p' infra/supabase/functions/enrich-dish/index.ts | grep -c "option_group"` returns >= 1
  </acceptance_criteria>
  <verify>
    <automated>~/.deno/bin/deno check infra/supabase/functions/enrich-dish/index.ts && test "$(sed -n '8,21p' infra/supabase/functions/enrich-dish/index.ts | grep -ciE 'ingredient|parent|variant')" = "0"</automated>
  </verify>
  <done>`deno check` is green and the header carries zero ingredient/parent/variant tokens while retaining option_group + the centroid line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a comment block in one Deno edge function. No code, control flow, query, auth check, or env handling is modified. No trust boundary is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (accidental code edit) | enrich-dish runtime | accept | The only risk is fat-fingering a code line while editing the comment. Mitigated by the comment-only scope, the "code unchanged" grep acceptance criterion (createClient/getEmbedding/EMBEDDING_MODEL counts unchanged), and `deno check`. No new attack surface — the function's service-role auth and OpenAI call are untouched. |

No security threats — comment-only change in a function whose auth/data paths are not modified (security_block_on = high → nothing to block).
</threat_model>

<verification>
- `~/.deno/bin/deno check infra/supabase/functions/enrich-dish/index.ts` exits 0 (CLEAN-03 static gate; Deno, not tsc — edge functions can't import workspace packages).
- Header lines 8-21 carry zero `ingredient`/`parent`/`variant` tokens; `option_group` and `_trg_after_dish_embedded` are retained (D-09).
- The corrected load line reads `Load dish + option groups + restaurant cuisine`, matching the verified actual logic (lines 135-164, F-06).
- No runtime code changed (createClient/getEmbedding/EMBEDDING_MODEL grep counts unchanged) — documentation-only, no deploy/behavior impact.
</verification>

<success_criteria>
- ROADMAP SC#3: enrich-dish header comments are corrected to drop ingredient/parent-dish references while preserving language for the triggers migration 151 explicitly kept alive (trg_enrich_on_dish_change, after_dish_embedded, trg_enrich_on_option_group_change). MET when Tasks 1–2 pass.
- CLEAN-03 satisfied: the comment matches reality (D-08), option_group + centroid retained, no runtime change.
</success_criteria>

<output>
Create `.planning/phases/05-dead-code-doc-cleanup/05-03-SUMMARY.md` when done. Record: the exact corrected header wording, that `deno check` passed via the full `~/.deno/bin/deno` path (deno is not on PATH), and confirmation that no code below the header was touched. Commit as one commit for the CLEAN-03 track (D-11), straight to main.
</output>
