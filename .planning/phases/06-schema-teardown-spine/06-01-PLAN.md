---
phase: 06-schema-teardown-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/scripts/verify-phase6-teardown.ts
autonomous: true
requirements: [DEBT-01, DEBT-02]
must_haves:
  truths:
    - "A read-only REST script exists that reports GONE/STILL-EXISTS for every ingredient table + dead column the teardown drops"
    - "The script reuses the verify-phase7.ts pattern (service-role select(col) → error means GONE) and writes nothing"
  artifacts:
    - path: "infra/scripts/verify-phase6-teardown.ts"
      provides: "Post-apply REST verification of the ingredient teardown (DEBT-01/02 gate)"
      contains: "verify-phase6-teardown"
      min_lines: 40
  key_links:
    - from: "infra/scripts/verify-phase6-teardown.ts"
      to: "@supabase/supabase-js"
      via: "createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) — read-only select probes"
      pattern: "createClient\\("
---

<objective>
Author the Wave-0 verification asset for the schema teardown: a read-only REST script `infra/scripts/verify-phase6-teardown.ts` that the operator (and the post-apply gate) runs to confirm every dropped ingredient table, trigger-orphaned column, and the `options.canonical_ingredient_id` FK column is GONE in prod after the migrations are applied.

Purpose: The phase is stage-don't-apply — there is no Supabase CLI in this env (REST-only), so the only automated post-apply check is a read-only `@supabase/supabase-js` probe. The Per-Task Verification Map (06-VALIDATION.md) flags this script as the MISSING DEBT-02 automated command; this plan creates it so later plans can reference it. It mirrors the existing `infra/scripts/verify-phase7.ts` exactly.
Output: `infra/scripts/verify-phase6-teardown.ts` (read-only; no prod-guard needed — guard is for write scripts only).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-teardown-spine/06-RESEARCH.md
@.planning/phases/06-schema-teardown-spine/06-PATTERNS.md
@infra/scripts/verify-phase7.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author verify-phase6-teardown.ts (read-only REST probe, mirror verify-phase7.ts)</name>
  <files>infra/scripts/verify-phase6-teardown.ts</files>
  <read_first>
    - infra/scripts/verify-phase7.ts (the exact analog — copy header, client init, the select(col)→error?'GONE':'STILL EXISTS' loop, and the closing read-only banner)
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md § "infra/scripts/verify-phase6-teardown.ts" (lines 133-162 — the column-gone and table-gone probe snippets)
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md § "Pattern: REST-only verify script" and § "Authoritative reconciled drop list"
  </read_first>
  <action>
    Create `infra/scripts/verify-phase6-teardown.ts` as a READ-ONLY ts-node script that mirrors `infra/scripts/verify-phase7.ts` structure verbatim (per PATTERNS.md). Do NOT wire prod-guard — it is read-only and writes nothing (PATTERNS.md § "prod-guard (reference only)" explicitly warns against this).

    Header + client (copy verify-phase7.ts lines 1-16 form): shebang `#!/usr/bin/env ts-node`, a JSDoc banner naming the file `verify-phase6-teardown.ts — READ-ONLY. Confirms the ingredient teardown (Phase B triggers + Phase C tables/columns) is live in prod.`, the usage line `cd infra/scripts && pnpm exec ts-node verify-phase6-teardown.ts`, `import 'dotenv/config';`, `import { createClient } from '@supabase/supabase-js';`, and `const sb = createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!) as any;`.

    In `main()`:
    (1) Print a `=== Phase 6 teardown verification (read-only) — ${process.env.SUPABASE_URL} ===` banner.
    (2) Dead-column probe — for each of `allergens_override`, `dietary_tags_override`: `const { error } = await sb.from('dishes').select(col).limit(1);` then log `dishes.${col.padEnd(20)}: ${error ? 'GONE ✓' : 'STILL EXISTS ✗'}`. Then the FK-sever column: `const { error: optErr } = await sb.from('options').select('canonical_ingredient_id').limit(1);` log `options.canonical_ingredient_id: ${optErr ? 'GONE ✓' : 'STILL EXISTS ✗'}`.
    (3) Table-gone probe — loop over the full reconciled drop list (per RESEARCH § "Authoritative reconciled drop list"): `['dish_ingredients','canonical_ingredients','canonical_ingredient_allergens','canonical_ingredient_dietary_tags','ingredient_aliases','ingredient_aliases_v2','ingredient_concepts','ingredient_variants','concept_translations','variant_translations']`. For each: `const { error } = await sb.from(t).select('id').limit(1);` log `table ${t.padEnd(34)}: ${error ? 'GONE ✓' : 'STILL EXISTS ✗'}`.
    (4) Close with `console.log('\n(read-only — nothing written)');` and call `main();` at the end (no top-level await; verify-phase7.ts calls `main();` bare).

    Use `select('id')` for tables and `select(col)` for columns — selecting a dropped object errors via PostgREST, which is the GONE signal. The script must compile under the infra/scripts ts-node setup (CJS, `as any` on the client to avoid generated-type friction, exactly as verify-phase7.ts does).
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && npx tsc --noEmit --skipLibCheck infra/scripts/verify-phase6-teardown.ts 2>&1 | grep -v "Cannot find module '@supabase" | grep -E "error TS" ; test $? -ne 0 && echo "TS_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/scripts/verify-phase6-teardown.ts` succeeds
    - `grep -c "GONE ✓" infra/scripts/verify-phase6-teardown.ts` returns ≥ 2 (column probe + table probe both present)
    - `grep -q "createClient(process.env.SUPABASE_URL" infra/scripts/verify-phase6-teardown.ts` (uses service-role REST client)
    - `grep -q "canonical_ingredient_id" infra/scripts/verify-phase6-teardown.ts` (FK-sever column covered)
    - `grep -qE "variant_translations|concept_translations" infra/scripts/verify-phase6-teardown.ts` (full 10-table list covered)
    - `grep -q "read-only — nothing written" infra/scripts/verify-phase6-teardown.ts` (read-only banner present)
    - `grep -cE "prod-guard|parseGuard|announceTarget" infra/scripts/verify-phase6-teardown.ts` returns 0 (no write-guard wired into a read-only script)
  </acceptance_criteria>
  <done>The read-only verify script exists, compiles (modulo the dotenv/supabase module-resolution noise the grep filters), covers all 10 reconciled tables + the 2 dead columns + the FK-sever column, and writes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → prod Postgres (service-role REST) | The verify script holds the service-role key and reads prod; it must never mutate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | verify-phase6-teardown.ts | mitigate | Script is read-only (`select(...).limit(1)` only); no insert/update/delete/rpc-write; acceptance grep asserts zero prod-guard wiring because there is nothing to guard (a write path would require the guard) |
| T-06-02 | Information Disclosure | service-role key in env | accept | Reuses the established `infra/scripts` pattern (verify-phase7.ts) — service-role key is read from `.env`, never logged; only `SUPABASE_URL` is printed (same as verify-phase7) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — only an `@supabase/supabase-js` import already pinned in Phase 4; no new dependency, no legitimacy checkpoint needed |
</threat_model>

<verification>
- `infra/scripts/verify-phase6-teardown.ts` exists and is read-only (no write calls, no prod-guard).
- Covers the full reconciled drop universe: 10 ingredient tables + `dishes.allergens_override` / `dishes.dietary_tags_override` + `options.canonical_ingredient_id`.
- TS compiles modulo module-resolution noise (the script runs under ts-node at apply time, exactly like verify-phase7.ts).
</verification>

<success_criteria>
The DEBT-02 post-apply automated command named in 06-VALIDATION.md (`verify-phase6-teardown.ts`) now exists and matches the verify-phase7.ts pattern; downstream plans (06-03, 06-06) can reference it as the post-apply GONE check.
</success_criteria>

<output>
Create `.planning/phases/06-schema-teardown-spine/06-01-SUMMARY.md` when done
</output>
