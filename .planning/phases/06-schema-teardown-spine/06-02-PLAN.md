---
phase: 06-schema-teardown-spine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql
  - infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql
autonomous: true
requirements: [DEBT-01]
must_haves:
  truths:
    - "A new Phase B migration (171) drops the ingredient triggers + helper functions using IF EXISTS, idempotent regardless of whether 151 was applied"
    - "A git-grep pre-flight proves zero live references to the dropped objects in apps/ and infra/supabase/functions/"
    - "171's REVERSE is degenerate/marker-only — it does NOT recreate compute_dish_dietary_tags or refresh_dish_dietary (which reference 156-dropped objects)"
  artifacts:
    - path: "infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql"
      provides: "Reconciled Phase B trigger/function drop (DEBT-01)"
      contains: "DROP TRIGGER IF EXISTS"
    - path: "infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql"
      provides: "Degenerate reverse (no recreation of dropped-object-referencing functions)"
      contains: "REVERSE"
  key_links:
    - from: "171_retire_ingredient_triggers_reconciled.sql"
      to: "151_retire_ingredient_triggers.sql"
      via: "supersedes 151 (D-06 reconciliation); reuses the IF EXISTS forward-drop set, NOT 151's broken REVERSE"
      pattern: "DROP (TRIGGER|FUNCTION) IF EXISTS"
---

<objective>
Author the reconciled Phase B migration that drops the inert ingredient-pipeline triggers and helper functions (DEBT-01), superseding the stale `151_retire_ingredient_triggers.sql` per D-06. The forward drop is `IF EXISTS`-idempotent (correct whether or not 151 already ran); the REVERSE is degenerate/marker-only because `151_REVERSE` recreates functions that query objects migration 156 already dropped — copying it verbatim would fail to apply.

Purpose: SC1 requires Phase B (the trigger drop) to be authored with a clean `git grep` pre-flight (zero hits in `apps/` and `infra/supabase/functions/`) and a paired REVERSE. Migration 156 (abandon-allergens, applied) already dropped `dietary_tags`/`canonical_ingredient_dietary_tags`/`allergens` + the override columns, so 151's REVERSE references nonexistent objects (D-06 drift). This plan authors a NEW migration 171 (next number after 170) that is safe regardless of live state — the operator probe (handed off in 06-06) confirms which triggers actually still exist.
Output: `171_retire_ingredient_triggers_reconciled.sql` + `171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql`. Authored + dry-run only — NOT applied (stage-don't-apply).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-teardown-spine/06-RESEARCH.md
@.planning/phases/06-schema-teardown-spine/06-PATTERNS.md
@.planning/phases/06-schema-teardown-spine/06-CONTEXT.md
@infra/supabase/migrations/151_retire_ingredient_triggers.sql
@infra/supabase/migrations/152_REVERSE_ONLY_drop_ingredient_pipeline.sql
@infra/supabase/migrations/156_drop_dietary_allergen_columns.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: git-grep Phase B pre-flight + author migration 171 forward (reconciled trigger/function drop)</name>
  <files>infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql</files>
  <read_first>
    - infra/supabase/migrations/151_retire_ingredient_triggers.sql (the forward-drop set to reuse: triggers lines 59-61, functions lines 64-72 — reconcile, do NOT copy the REVERSE)
    - infra/supabase/migrations/156_drop_dietary_allergen_columns.sql (header convention lines 1-42; the source of the D-06 drift — it dropped dietary_tags/allergens/canonical_ingredient_dietary_tags)
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md § "The D-06 trigger/function drift" and § "Migration header + pairing convention"
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md § "<N>_retire_ingredient_triggers_reconciled.sql (Phase B — DEBT-01)"
  </read_first>
  <action>
    FIRST run the SC1 git-grep pre-flight and record the result in the SUMMARY (it must show zero LIVE hits in apps/ and edge functions; the only acceptable hits are historical migration comments + web-portal-v2 — which has no ingredient-trigger references):
    `git grep -nE "dish_ingredients|trg_dish_ingredients_refresh|trg_dishes_override_refresh|refresh_dish_dietary|compute_dish_allergens|compute_dish_dietary_tags|trg_enrich_on_ingredient_change|dish_ingredients_refresh|dishes_override_refresh" -- 'apps/**' 'infra/supabase/functions/**'`
    Expect ZERO hits in apps/ and infra/supabase/functions/ (these objects are DB-only and dead per 151's safety note). If any hit appears, STOP and surface it — it is a live consumer that blocks the drop.

    Then author `infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql` following the 156 header convention (PATTERNS.md): a `-- 171_retire_ingredient_triggers_reconciled.sql` first line, `-- Created: 2026-06-20`, a prose block stating it SUPERSEDES 151 (D-06) and why (151's REVERSE references 156-dropped objects; 156's CASCADE may have already removed some of 151's targets, so the forward drops are now possibly-partial no-ops — `IF EXISTS` makes that harmless), a `-- Pre-apply checklist:` noting the operator must run the LIVE-STATE PROBE first (handed off in 06-06) and that 151 must NOT also be applied, and a `-- Reverse:` note pointing at the degenerate reverse.

    Body wrapped `BEGIN; ... COMMIT;` with `── (n) ──` banner comments, reusing 151's forward-drop set verbatim (all `IF EXISTS`, triggers before the functions they call, `refresh_dish_dietary` before its `compute_*` callees):
    - `DROP TRIGGER IF EXISTS dish_ingredients_refresh ON public.dish_ingredients;`
    - `DROP TRIGGER IF EXISTS dishes_override_refresh ON public.dishes;`
    - `DROP TRIGGER IF EXISTS trg_enrich_on_ingredient_change ON public.dish_ingredients;`
    - `DROP FUNCTION IF EXISTS public.trg_dish_ingredients_refresh();`
    - `DROP FUNCTION IF EXISTS public.trg_dishes_override_refresh();`
    - `DROP FUNCTION IF EXISTS public.refresh_dish_dietary(uuid);`
    - `DROP FUNCTION IF EXISTS public.compute_dish_allergens(uuid);`
    - `DROP FUNCTION IF EXISTS public.compute_dish_dietary_tags(uuid);`

    PRESERVE-section comment (copy 151 lines 41-46): explicitly note that `trg_enrich_on_dish_change`, `after_dish_embedded`, and `trg_enrich_on_option_group_change` are NOT touched (still wanted). Do NOT use CASCADE anywhere in this file.
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && grep -cE "CASCADE" infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql | grep -q "^0$" && grep -c "DROP TRIGGER IF EXISTS" infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql | grep -qE "^[1-9]" && echo "FORWARD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql` succeeds
    - `grep -c "DROP TRIGGER IF EXISTS" infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql` returns 3
    - `grep -c "DROP FUNCTION IF EXISTS" infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql` returns 5
    - `grep -cE "\bCASCADE\b" infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql` returns 0
    - `grep -qE "trg_enrich_on_dish_change|after_dish_embedded" infra/supabase/migrations/171_retire_ingredient_triggers_reconciled.sql` (PRESERVE section present — does not drop the still-wanted triggers)
    - `git grep -nE "dish_ingredients_refresh|trg_dish_ingredients_refresh|refresh_dish_dietary" -- 'apps/**' 'infra/supabase/functions/**'` returns empty (SC1 pre-flight clean)
  </acceptance_criteria>
  <done>Migration 171 forward exists, drops 3 triggers + 5 functions with IF EXISTS, contains zero CASCADE, preserves the 3 still-wanted enrich triggers, and the git-grep pre-flight shows zero live consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 171 REVERSE (degenerate/marker-only — do NOT recreate 156-broken functions)</name>
  <files>infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql</files>
  <read_first>
    - infra/supabase/migrations/152_REVERSE_ONLY_drop_ingredient_pipeline.sql (the degenerate/marker-only reverse precedent — header note + no-op BEGIN/COMMIT placeholder)
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md § "Pitfall 3: Re-using 151's REVERSE" and § "Schema-only REVERSE"
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md § "REVERSE — use the 152 minimal/degenerate reverse precedent, NOT 151's"
  </read_first>
  <action>
    Author `infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql` using the `152_REVERSE` degenerate/marker-only precedent — NOT 151's REVERSE.

    Header: `-- 171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql`, `-- Reverses 171_retire_ingredient_triggers_reconciled.sql.` Then a prose note (mirroring 152_REVERSE lines 1-20) explaining: this reverse is intentionally degenerate. The trigger FUNCTIONS cannot be recreated as-is because `compute_dish_dietary_tags` queries `public.dietary_tags` + `public.canonical_ingredient_dietary_tags` and `refresh_dish_dietary` writes `dishes.allergens` / `dishes.dietary_tags` — ALL of which migration 156 already dropped. `dishes_override_refresh` fires `AFTER UPDATE OF allergens_override, dietary_tags_override` — columns migration 174 (this phase) drops. Recreating any of them would fail to apply. To genuinely restore the triggers you would first have to un-apply 156 and re-add those columns/tables, then re-run the original function bodies from migrations 092/105/135 — out of scope and not desired (allergens/dietary are abandoned per CLAUDE.md).

    Body: a `BEGIN; ... COMMIT;` with a single `-- No-op placeholder. See header note.` comment (the 152_REVERSE form). Do NOT include any `CREATE FUNCTION`, `CREATE TRIGGER`, `UPDATE public.dishes SET allergens`, or references to `compute_dish_dietary_tags` / `dietary_tags` in executable SQL.
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && test -f infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql && grep -v '^--' infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql | grep -cE "CREATE FUNCTION|CREATE TRIGGER" | grep -q "^0$" && echo "REVERSE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql` succeeds
    - `grep -v '^--' infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql | grep -cE "CREATE FUNCTION|CREATE TRIGGER"` returns 0 (degenerate — no recreation in executable SQL)
    - `grep -v '^--' infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql | grep -cE "UPDATE public.dishes SET allergens|compute_dish_dietary_tags"` returns 0 (no references to 156-dropped objects in executable SQL)
    - `grep -qiE "BEGIN" infra/supabase/migrations/171_REVERSE_ONLY_retire_ingredient_triggers_reconciled.sql` (transaction wrapper present, matching pairing convention)
  </acceptance_criteria>
  <done>The REVERSE exists as a degenerate marker-only file (152 precedent), explains why recreation is impossible post-156, and contains no executable CREATE FUNCTION/TRIGGER or references to dropped objects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration → prod Postgres (operator-applied) | The DDL is authored here but applied by the operator; the migration must be idempotent and never CASCADE-cascade into a still-wanted object. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service | dropping a still-live trigger | mitigate | SC1 git-grep pre-flight proves zero live consumers in apps/ + edge functions; PRESERVE comment + acceptance grep assert the 3 still-wanted enrich triggers are NOT dropped; `IF EXISTS` means a no-op if already gone |
| T-06-04 | Tampering | broken REVERSE silently fails on rollback | mitigate | Degenerate REVERSE (152 precedent) documents irreversibility instead of authoring CREATE statements that reference 156-dropped objects (which would error); acceptance grep asserts zero executable CREATE FUNCTION/TRIGGER |
| T-06-05 | Elevation/Integrity | CASCADE silently removing unknown dependents | mitigate | Forward migration uses only `DROP ... IF EXISTS` (no CASCADE); acceptance grep asserts zero CASCADE tokens |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure SQL authoring) — no legitimacy gate applicable |
</threat_model>

<verification>
- `git grep` Phase B pre-flight returns zero hits in `apps/` and `infra/supabase/functions/` (SC1).
- Migration 171 forward drops 3 triggers + 5 functions with `IF EXISTS`, zero CASCADE, preserves the still-wanted enrich triggers.
- Migration 171 REVERSE is degenerate/marker-only — no executable recreation of 156-broken functions.
- Authored + dry-run only — the agent does NOT apply to prod (operator handoff in 06-06).
</verification>

<success_criteria>
DEBT-01 is satisfied at the authoring level: a reconciled, idempotent Phase B trigger/function drop migration (171) supersedes 151, with a clean grep pre-flight and a correct degenerate reverse. Real-world application is gated on the operator handoff (06-06).
</success_criteria>

<output>
Create `.planning/phases/06-schema-teardown-spine/06-02-SUMMARY.md` when done
</output>
