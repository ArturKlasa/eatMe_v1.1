---
phase: 06-schema-teardown-spine
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - infra/supabase/migrations/172_snapshot_ingredient_archive.sql
  - infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql
  - infra/supabase/migrations/173_REVERSE_ONLY_drop_ingredient_tables_restrict.sql
  - infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql
  - infra/supabase/migrations/174_REVERSE_ONLY_drop_ingredient_columns_restrict.sql
autonomous: true
requirements: [DEBT-02]
must_haves:
  truths:
    - "A separate snapshot migration (172) creates the non-public ingredient_archive schema with CREATE TABLE AS SELECT, authored to land BEFORE any drop (D-02)"
    - "The tables-drop migration (173) severs options.canonical_ingredient_id FIRST, then drops the ingredient tables child-to-parent using RESTRICT (zero CASCADE)"
    - "The columns-drop migration (174) drops dishes.allergens_override and dietary_tags_override with IF EXISTS"
    - "Every forward drop migration has a paired REVERSE (173/174); 172 is one-way archive (it only creates, so no reverse is authored)"
  artifacts:
    - path: "infra/supabase/migrations/172_snapshot_ingredient_archive.sql"
      provides: "In-DB archive snapshot in a non-public schema (D-01/D-02/D-03)"
      contains: "CREATE SCHEMA IF NOT EXISTS ingredient_archive"
    - path: "infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql"
      provides: "RESTRICT child-to-parent table drop with options FK sever first (DEBT-02, SC2)"
      contains: "RESTRICT"
    - path: "infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql"
      provides: "Dead dish override column drop (DEBT-02)"
      contains: "DROP COLUMN IF EXISTS"
  key_links:
    - from: "173_drop_ingredient_tables_restrict.sql"
      to: "172_snapshot_ingredient_archive.sql"
      via: "snapshot must land BEFORE the drop (D-02); the drop header references the archive landing-check"
      pattern: "ingredient_archive"
    - from: "173_drop_ingredient_tables_restrict.sql"
      to: "options.canonical_ingredient_id"
      via: "ALTER TABLE public.options DROP COLUMN first statement severs the FK so canonical_ingredients can drop under RESTRICT"
      pattern: "options DROP COLUMN IF EXISTS canonical_ingredient_id"
---

<!-- planner-discipline-allow: RESTRICT -->
<!-- planner-discipline-allow: canonical_ingredient_id -->

<objective>
Author the Phase C teardown migrations (DEBT-02) in the locked spine order: (172) an in-DB `ingredient_archive` snapshot in a non-public schema, authored as a SEPARATE migration that lands BEFORE any drop (D-01/D-02/D-03); (173) the table drop that severs `options.canonical_ingredient_id` FIRST then drops the ingredient tables child-to-parent using `RESTRICT` (never CASCADE — SC2); (174) the dead-column drop for `dishes.allergens_override` / `dietary_tags_override`. Each forward drop migration gets a paired schema-only REVERSE (D-05); 172 is a one-way archive (it only creates, so no reverse is authored — documented in its header).

Purpose: SC2 mandates that Phase C drops tables then columns in explicit child-to-parent order using `RESTRICT` (so an unexpected dependent fails loudly), preceded by a `pg_depend` dependency audit and a data snapshot, and is treated as one-way. The existing `152`/`153` migrations use CASCADE and must be SUPERSEDED, not reused. The one carve-out to "tables before columns": `options.canonical_ingredient_id`'s FK points into `canonical_ingredients`, so it MUST be severed before the table can drop under RESTRICT — handled as the first statement of 173 (research Pitfall 2). The `pg_depend` + FK dependency-audit query and the LIVE-STATE PROBE are run by the operator (handed off in 06-06) before applying; this plan authors the migrations idempotent (`IF EXISTS`) so they are correct regardless of live state.
Output: 172, 173 (+REVERSE), 174 (+REVERSE). Authored + dry-run only — NOT applied (stage-don't-apply).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-teardown-spine/06-RESEARCH.md
@.planning/phases/06-schema-teardown-spine/06-PATTERNS.md
@.planning/phases/06-schema-teardown-spine/06-CONTEXT.md
@infra/supabase/migrations/156_drop_dietary_allergen_columns.sql
@infra/supabase/migrations/152_REVERSE_ONLY_drop_ingredient_pipeline.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 172 snapshot (ingredient_archive) + 173 table RESTRICT drop with FK-sever carve-out + 173 REVERSE</name>
  <files>infra/supabase/migrations/172_snapshot_ingredient_archive.sql, infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql, infra/supabase/migrations/173_REVERSE_ONLY_drop_ingredient_tables_restrict.sql</files>
  <read_first>
    - infra/supabase/migrations/156_drop_dietary_allergen_columns.sql (header convention lines 1-42, BEGIN/COMMIT + banner form)
    - infra/supabase/migrations/152_REVERSE_ONLY_drop_ingredient_pipeline.sql (degenerate-reverse precedent for the data-restoration note)
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md sections "Provably-safe RESTRICT child→parent DROP order", "Note on options.canonical_ingredient_id", "Code Examples — RESTRICT drop block", "pg_depend / FK audit pre-flight", "Authoritative reconciled drop list"
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md sections "<N>_snapshot_ingredient_archive.sql" and "<N>_drop_ingredient_tables_restrict.sql + REVERSE"
  </read_first>
  <action>
    (A) Author `infra/supabase/migrations/172_snapshot_ingredient_archive.sql` (D-01/D-02/D-03). Header (156 convention): a `-- 172_snapshot_ingredient_archive.sql` first line, `-- Created: 2026-06-20`, prose stating this is the pre-drop data snapshot for DEBT-02, runs as a SEPARATE migration that the operator applies AND verifies lands BEFORE migration 173 (D-02), into a non-`public` schema so it is not PostgREST-exposed and needs no RLS (D-03), retained indefinitely (D-04). A `-- Reverse:` note: NONE authored — this migration only creates; rolling back is a future-cleanup `DROP SCHEMA ingredient_archive` (deferred per D-04).
    Body: `CREATE SCHEMA IF NOT EXISTS ingredient_archive;` then, for EACH table in the snapshot-then-drop list (RESEARCH "Authoritative reconciled drop list"): `canonical_ingredients`, `ingredient_aliases`, `ingredient_aliases_v2`, `ingredient_concepts`, `ingredient_variants`, `concept_translations`, `variant_translations`, `canonical_ingredient_allergens`, and `dish_ingredients` — emit a conditional archive statement guarded by `to_regclass` so it no-ops if the table is already gone. Use a `DO $$` block per table following this shape: `DO $$ BEGIN IF to_regclass('public.canonical_ingredients') IS NOT NULL THEN EXECUTE 'CREATE TABLE IF NOT EXISTS ingredient_archive.canonical_ingredients AS SELECT * FROM public.canonical_ingredients'; END IF; END $$;`. Add a comment: `-- Operator: the to_regclass guards make any table the LIVE-STATE PROBE reported absent a silent no-op.`. Append the operator landing-check as a trailing comment: `-- Landing check (run before applying 173): SELECT table_name FROM information_schema.tables WHERE table_schema = 'ingredient_archive';`

    (B) Author `infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql` — SUPERSEDES `152` (CASCADE to RESTRICT). Header (156 convention): note it supersedes 152, that 152 used CASCADE which violates SC2, that this uses RESTRICT so an unknown dependent fails loudly, and a `-- Pre-apply checklist:` requiring the operator to (1) run the LIVE-STATE PROBE + pg_depend DEPENDENCY-AUDIT (06-RESEARCH audit query) and confirm `external_fks_into_set` + `dependent_objects` are empty, (2) confirm 172 snapshot landed. `-- Reverse:` points at the schema-only REVERSE (data restored manually from ingredient_archive).
    Body — one `BEGIN; ... COMMIT;`. The FIRST statement is the FK sever (carve-out, research Pitfall 2): `ALTER TABLE public.options DROP COLUMN IF EXISTS canonical_ingredient_id;` with a `-- FK sever, MUST precede the canonical_ingredients drop` comment. Then the child-to-parent RESTRICT drops in this exact order, each `DROP TABLE IF EXISTS public.<t> RESTRICT;`:
    1=variant_translations, 2=concept_translations, 3=ingredient_aliases_v2, 4=ingredient_variants, 5=canonical_ingredient_dietary_tags (comment `-- if present; 156 may have dropped it`), 6=canonical_ingredient_allergens, 7=ingredient_aliases, 8=dish_ingredients, 9=ingredient_concepts, 10=canonical_ingredients.
    Do NOT use CASCADE anywhere. Add `── (n) ──` banner comments grouping the FK sever and the drops.

    (C) Author `infra/supabase/migrations/173_REVERSE_ONLY_drop_ingredient_tables_restrict.sql` (D-05 schema-only). Use the 152_REVERSE degenerate approach (research endorses NOT inlining ~600 lines of inter-migration DDL): a header note that structural reversal means re-running migrations 099-106 in order plus re-adding `options.canonical_ingredient_id` and its FK, after which data is restored manually via `INSERT ... SELECT FROM ingredient_archive.<t>` (never baked into the reverse). Body: `BEGIN; -- No-op placeholder. See header note. COMMIT;`.
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && CAS=$(grep -cE "\bCASCADE\b" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql); RES=$(grep -c "RESTRICT" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql); test "$CAS" -eq 0 && test "$RES" -ge 10 && grep -q "CREATE SCHEMA IF NOT EXISTS ingredient_archive" infra/supabase/migrations/172_snapshot_ingredient_archive.sql && echo TABLES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/supabase/migrations/172_snapshot_ingredient_archive.sql` succeeds and `grep -q "CREATE SCHEMA IF NOT EXISTS ingredient_archive" infra/supabase/migrations/172_snapshot_ingredient_archive.sql`
    - `grep -c "to_regclass" infra/supabase/migrations/172_snapshot_ingredient_archive.sql` returns 9 (one guarded archive block per snapshot-then-drop table)
    - `grep -q "ingredient_archive" infra/supabase/migrations/172_snapshot_ingredient_archive.sql` AND the schema is NOT `public` (no `CREATE TABLE public.` archive targets)
    - `grep -c "DROP TABLE IF EXISTS public" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql` returns 10
    - `grep -cE "RESTRICT" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql` returns at least 10
    - `grep -cE "\bCASCADE\b" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql` returns 0
    - The FIRST non-comment DDL statement in 173 is `ALTER TABLE public.options DROP COLUMN IF EXISTS canonical_ingredient_id;` — verify: `grep -n "options DROP COLUMN IF EXISTS canonical_ingredient_id" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql` returns a line BEFORE the first `DROP TABLE` line
    - `grep -n "canonical_ingredients" infra/supabase/migrations/173_drop_ingredient_tables_restrict.sql` shows `DROP TABLE IF EXISTS public.canonical_ingredients RESTRICT;` as the LAST table drop (child-to-parent order)
    - `test -f infra/supabase/migrations/173_REVERSE_ONLY_drop_ingredient_tables_restrict.sql` and `grep -v '^--' infra/supabase/migrations/173_REVERSE_ONLY_drop_ingredient_tables_restrict.sql | grep -cE "CREATE TABLE|DROP TABLE"` returns 0 (degenerate reverse)
  </acceptance_criteria>
  <done>172 creates the non-public ingredient_archive schema with 9 to_regclass-guarded CREATE TABLE AS SELECT blocks; 173 severs the options FK first then RESTRICT-drops 10 tables child-to-parent with zero CASCADE; 173 REVERSE is degenerate schema-only per the 152 precedent.</done>
</task>

<task type="auto">
  <name>Task 2: Author 174 column drop (dishes.*_override) + 174 REVERSE (schema-only)</name>
  <files>infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql, infra/supabase/migrations/174_REVERSE_ONLY_drop_ingredient_columns_restrict.sql</files>
  <read_first>
    - infra/supabase/migrations/156_drop_dietary_allergen_columns.sql (forward DROP COLUMN form lines 45-54; header convention)
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md section "<N>_drop_ingredient_columns_restrict.sql + REVERSE" (the ADD COLUMN IF NOT EXISTS reverse form lines 124-129)
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md section "Authoritative reconciled drop list" (columns: dishes.allergens_override, dishes.dietary_tags_override — options.canonical_ingredient_id is handled in 173, NOT here)
  </read_first>
  <action>
    Author `infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql`. Header (156 convention): `-- 174_drop_ingredient_columns_restrict.sql`, `-- Created: 2026-06-20`, prose stating this drops the two dead ingredient-pipeline override columns on `dishes` (the trigger that maintained them, `dishes_override_refresh`, is dropped by migration 171; the columns have been direct-default-only and empty). NOTE explicitly: `options.canonical_ingredient_id` is NOT dropped here — it was severed as the FIRST statement of migration 173 because its FK blocks the `canonical_ingredients` table drop under RESTRICT (the documented carve-out). `-- Pre-apply checklist:` apply AFTER 173. `-- Reverse:` schema-only re-add.
    Body: one `BEGIN; ... COMMIT;`:
    `ALTER TABLE public.dishes DROP COLUMN IF EXISTS allergens_override, DROP COLUMN IF EXISTS dietary_tags_override;`
    Do NOT include `options.canonical_ingredient_id` (already severed in 173). Do NOT use CASCADE.

    Author `infra/supabase/migrations/174_REVERSE_ONLY_drop_ingredient_columns_restrict.sql` (D-05 schema-only, mirror 156_REVERSE ADD COLUMN form). Header note: structural restore only; the columns were always empty/default so no data restore is meaningful. Body `BEGIN; ... COMMIT;`:
    `ALTER TABLE public.dishes ADD COLUMN IF NOT EXISTS allergens_override text[] DEFAULT ARRAY[]::text[], ADD COLUMN IF NOT EXISTS dietary_tags_override text[] DEFAULT ARRAY[]::text[];`
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && grep -q "allergens_override" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql && grep -q "dietary_tags_override" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql && OPT=$(grep -c "canonical_ingredient_id" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql); CAS=$(grep -cE "\bCASCADE\b" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql); test "$OPT" -eq 0 && test "$CAS" -eq 0 && grep -q "ADD COLUMN IF NOT EXISTS allergens_override" infra/supabase/migrations/174_REVERSE_ONLY_drop_ingredient_columns_restrict.sql && echo COLUMNS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql` succeeds
    - `grep -c "DROP COLUMN IF EXISTS" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql` returns 2 (allergens_override + dietary_tags_override)
    - `grep -c "canonical_ingredient_id" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql` returns 0 (severed in 173, NOT duplicated here)
    - `grep -cE "\bCASCADE\b" infra/supabase/migrations/174_drop_ingredient_columns_restrict.sql` returns 0
    - `test -f infra/supabase/migrations/174_REVERSE_ONLY_drop_ingredient_columns_restrict.sql` and `grep -c "ADD COLUMN IF NOT EXISTS" infra/supabase/migrations/174_REVERSE_ONLY_drop_ingredient_columns_restrict.sql` returns 2 (schema-only restore)
  </acceptance_criteria>
  <done>174 drops the two dead dishes override columns with IF EXISTS (zero CASCADE, does not touch options.canonical_ingredient_id); 174 REVERSE re-adds both columns with their original text[] defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migrations → prod Postgres (operator-applied) | Irreversible DDL drops authored here, applied by the operator; one-way (data only recoverable from the in-DB archive). |
| ingredient_archive schema → PostgREST | Archive holds copies of dropped data; must not become an API surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering (data loss) | RESTRICT drop in wrong order / CASCADE | mitigate | Snapshot-first (172, D-01/D-02) precedes any drop; child-to-parent RESTRICT order (173) so a forgotten dependent fails loudly instead of silently cascading; acceptance grep asserts zero CASCADE + 10 RESTRICT drops; operator runs pg_depend audit before applying (06-06) |
| T-06-07 | Integrity | canonical_ingredients drop rejected by live options FK | mitigate | `options.canonical_ingredient_id` severed as the FIRST statement of 173 (carve-out); acceptance grep asserts the sever precedes the first DROP TABLE |
| T-06-08 | Information Disclosure | ingredient_archive exposed via PostgREST | accept/mitigate | Archive lives in a non-`public` schema (D-03) → PostgREST only exposes `public` by default, so no API surface and no RLS needed; acceptance asserts archive targets are not `public.` |
| T-06-09 | Repudiation | irreversible drop with no recovery path | mitigate | In-DB `ingredient_archive` retains all snapshot-then-drop tables (D-01/D-04 indefinite retention); schema-only REVERSE files (173/174) recreate structure; data restore is a documented manual `INSERT ... SELECT FROM ingredient_archive` step |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure SQL authoring) — no legitimacy gate applicable |
</threat_model>

<verification>
- 172 snapshot creates a non-public `ingredient_archive` schema with `to_regclass`-guarded `CREATE TABLE AS SELECT` for the 9 snapshot-then-drop tables, authored to land BEFORE 173 (D-02).
- 173 severs `options.canonical_ingredient_id` FIRST, then RESTRICT-drops the 10 ingredient tables child-to-parent, zero CASCADE.
- 174 drops `dishes.allergens_override` + `dietary_tags_override` with `IF EXISTS`, zero CASCADE, does NOT re-drop the options FK column.
- Every forward drop migration (173, 174) has a paired REVERSE; 172 is one-way (documented).
- Authored + dry-run only — the agent does NOT apply to prod. The operator pg_depend audit + apply order is handed off in 06-06.
</verification>

<success_criteria>
DEBT-02 is satisfied at the authoring level: snapshot-first, child-to-parent RESTRICT table drop with the options FK-sever carve-out, dead-column drop, all idempotent and paired with reverses — superseding the CASCADE-based 152/153. Real-world application + the pg_depend audit are gated on the operator handoff (06-06).
</success_criteria>

<output>
Create `.planning/phases/06-schema-teardown-spine/06-03-SUMMARY.md` when done
</output>