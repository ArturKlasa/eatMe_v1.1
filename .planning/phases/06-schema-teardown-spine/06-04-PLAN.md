---
phase: 06-schema-teardown-spine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web-portal-v2/src/components/menu/KindSelector.tsx
  - apps/web-portal-v2/src/components/menu/DishForm.tsx
  - apps/web-portal-v2/src/components/menu/MenuManager.tsx
  - packages/shared/src/types/restaurant.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/constants/menu.ts
  - packages/shared/src/__tests__/dish-kinds.test.ts
autonomous: true
requirements: [DEBT-03]
must_haves:
  truths:
    - "apps/web-portal-v2 no longer imports or references the DishKind type or DISH_KIND_META (v2 severed FIRST per SC3)"
    - "The DishKind type, the legacy dish_kind? field, DISH_KIND_META, the re-export, and dish-kinds.test.ts are deleted from @eatme/shared"
    - "A consumer grep shows zero remaining DishKind/DISH_KIND_META importers in apps/ + packages/ (ignoring .next/ build artifacts)"
    - "turbo check-types passes across apps/admin + apps/web-portal-v2"
  artifacts:
    - path: "apps/web-portal-v2/src/components/menu/DishForm.tsx"
      provides: "Flat dish form with no kind discriminator (D-07)"
    - path: "packages/shared/src/constants/menu.ts"
      provides: "Menu constants without DISH_KIND_META (DINING_FORMATS preserved)"
  key_links:
    - from: "apps/web-portal-v2/src/components/menu/DishForm.tsx"
      to: "@eatme/shared"
      via: "import keeps DishV2Input + PRIMARY_PROTEINS, drops type DishKind"
      pattern: "import.*DishV2Input.*@eatme/shared"
---

<objective>
Remove the now-isolated `DishKind` / `DISH_KIND_META` shims (DEBT-03) in the locked SC3 / D-08 removal order (v2 importers first, then `@eatme/shared`): sever the `apps/web-portal-v2` importers FIRST (delete `KindSelector.tsx`, strip the `dish_kind` field + kind-switch transform from `DishForm.tsx`, and reconcile the `MenuManager.tsx` `input.dish_kind` reads), THEN delete the shims from `@eatme/shared` (`DishKind` type + legacy `dish_kind?` field in `types/restaurant.ts`, the `DishKind` re-export in `types/index.ts`, `DISH_KIND_META` in `constants/menu.ts`, and `__tests__/dish-kinds.test.ts`). Verify with a consumer grep showing zero importers and `turbo check-types` green.

Purpose: Per CLAUDE.md the DishKind shims "survive only for the retired apps; remove them when that app is deleted" — and web-portal-v2's `KindSelector` is the last live importer. `apps/web-portal-v2` is on-ice (edit in place, do NOT delete the app — D-07). `DishV2Input` is Zod-derived from `dishSchemaV2` (which has its own inline `dish_kind: z.enum([...]).optional()` literal, independent of the `DishKind` type symbol), so omitting `dish_kind` from `buildDishInput` is type-safe (RESEARCH verified). This track is TS-only with no DB dependency — it runs in parallel with the schema-authoring track.
Output: KindSelector deleted, DishForm flattened, MenuManager reconciled, shims + test deleted from @eatme/shared, zero-importer grep green, check-types green.

OUT OF SCOPE (do NOT touch — RESEARCH/PATTERNS scope boundary): the inline `dish_kind: z.enum([...])` literals at `validation/dish.ts:56`, `validation/menuScan.ts:41/97`, `validation/restaurant.ts:65`, and `functions/menu-scan-worker/index.ts:101`. They are independent of the `DishKind` symbol and relate to migration 163, not the ingredient pipeline.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-teardown-spine/06-RESEARCH.md
@.planning/phases/06-schema-teardown-spine/06-PATTERNS.md
@.planning/phases/06-schema-teardown-spine/06-CONTEXT.md
@apps/web-portal-v2/src/components/menu/DishForm.tsx
@apps/web-portal-v2/src/components/menu/MenuManager.tsx
@packages/shared/src/constants/menu.ts
@packages/shared/src/types/restaurant.ts
@packages/shared/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sever web-portal-v2 (delete KindSelector, flatten DishForm, reconcile MenuManager) — v2 FIRST per SC3</name>
  <files>apps/web-portal-v2/src/components/menu/KindSelector.tsx, apps/web-portal-v2/src/components/menu/DishForm.tsx, apps/web-portal-v2/src/components/menu/MenuManager.tsx</files>
  <read_first>
    - apps/web-portal-v2/src/components/menu/DishForm.tsx (current state: import line 5, DishFormValues.dish_kind line 19, DEFAULT_VALUES.dish_kind line 60, buildDishInput switch lines 71-111, watch('dish_kind') line 134, KindSelector render + kind-conditional sections lines 223-254)
    - apps/web-portal-v2/src/components/menu/MenuManager.tsx (input.dish_kind reads at lines 167, 170-173, 207, 209-210 — these read the lowercase property off DishV2Input)
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md sections "Exact live-code consumers" and "v2 buildDishInput flattened"
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md section "DishKind shim removal (DEBT-03 / SC3)"
  </read_first>
  <action>
    Sever web-portal-v2 in place (do NOT delete the app — D-07). Three files:

    (1) DELETE `apps/web-portal-v2/src/components/menu/KindSelector.tsx` entirely (it imports both `DishKind` and `DISH_KIND_META`).

    (2) `apps/web-portal-v2/src/components/menu/DishForm.tsx`:
    - Line 5 import: drop `type DishKind` only — keep `PRIMARY_PROTEINS` and `type DishV2Input`. Result: `import { PRIMARY_PROTEINS, type DishV2Input } from '@eatme/shared';`
    - Remove the `import { KindSelector } from './KindSelector';` line (line 6).
    - `DishFormValues` (line 19): remove `dish_kind: DishKind;`.
    - `DEFAULT_VALUES` (line 60): remove `dish_kind: 'standard',`.
    - `buildDishInput` (lines 71-111): collapse the `switch (v.dish_kind)` to a flat return matching RESEARCH "v2 buildDishInput flattened" — return `{ ...base, is_template: values.is_template, modifier_groups: values.slots ?? [], bundled_items: values.bundle_items ?? [] }` (omit `dish_kind` entirely; `dishSchemaV2.dish_kind` is `.optional()` so this validates). Remove the `const v = values as any;` and the whole switch.
    - Line 134 `const dishKind = watch('dish_kind');`: remove.
    - Lines 223-229 (the "Dish kind" `<label>` + `<KindSelector>` block): remove entirely (D-07 cuts the kind selector).
    - The kind-conditional sections (lines 231-254): D-07 says the form must still produce a flat dish. Replace the `dishKind === 'configurable'` / `'bundle'` / `'course_menu'` conditionals with unconditional rendering of the modifier sections that v2 still needs — render `BundleItemsSection`, `ConfigurableSlotsSection` (with `control`/`register`), and the `is_template` checkbox WITHOUT the kind gate (the form now always exposes modifier_groups + bundled_items). Drop the `CourseEditorSection` render and its import if courses are no longer reachable without a kind, OR keep all three sections unconditionally — planner's call within D-07; pick the option that keeps `turbo check-types` green and leaves no unused import. Document the choice in the SUMMARY.

    (3) `apps/web-portal-v2/src/components/menu/MenuManager.tsx`: `buildDishInput` no longer emits `dish_kind`, so the `input.dish_kind` reads at lines 167, 170-173, 207, 209-210 would silently evaluate against `undefined`. Reconcile so the optimistic-update shape stays coherent: replace `dish_kind: input.dish_kind ?? 'standard'` with `dish_kind: 'standard'` (the local dishes[] row keeps a literal default since the form no longer carries a kind), and replace the `input.dish_kind === 'configurable' ? (input.is_template ?? false) : false` expressions with `input.is_template ?? false` (is_template now flows directly from the flat form). Do NOT introduce a `DishKind` import — these are plain string/boolean assignments on the local optimistic row.

    Run the v2 type gate after the edits: `cd apps/web-portal-v2 && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && test ! -f apps/web-portal-v2/src/components/menu/KindSelector.tsx && V2=$(git grep -nE "\bDishKind\b" -- 'apps/web-portal-v2/**' ':!**/.next/**' | wc -l); test "$V2" -eq 0 && cd apps/web-portal-v2 && npx tsc --noEmit && echo V2_SEVERED</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f apps/web-portal-v2/src/components/menu/KindSelector.tsx` (file deleted)
    - `git grep -nE "\bDishKind\b" -- 'apps/web-portal-v2/**' ':!**/.next/**'` returns empty
    - `git grep -nE "import.*KindSelector" -- 'apps/web-portal-v2/**'` returns empty (no dangling import)
    - `grep -c "dish_kind" apps/web-portal-v2/src/components/menu/DishForm.tsx` returns 0 (form no longer tracks kind)
    - `cd apps/web-portal-v2 && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>KindSelector.tsx is deleted, DishForm.tsx produces a flat DishV2Input with no dish_kind, MenuManager.tsx optimistic rows use literal defaults, no dangling KindSelector import, and web-portal-v2 type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the DishKind/DISH_KIND_META shims from @eatme/shared + dish-kinds.test.ts, then prove zero importers</name>
  <files>packages/shared/src/types/restaurant.ts, packages/shared/src/types/index.ts, packages/shared/src/constants/menu.ts, packages/shared/src/__tests__/dish-kinds.test.ts</files>
  <read_first>
    - packages/shared/src/types/restaurant.ts (DishKind type def with @deprecated JSDoc lines 18-26; legacy `dish_kind?: DishKind;` field lines 165-171)
    - packages/shared/src/types/index.ts (the `DishKind,` re-export line 3)
    - packages/shared/src/constants/menu.ts (DISH_KIND_META + @deprecated JSDoc lines 14-31; DINING_FORMATS begins line 33 — KEEP it)
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md section "DishKind shim removal" (the exact line targets + "keep DINING_FORMATS/DINING_FORMAT_META")
  </read_first>
  <action>
    Delete the shims from `@eatme/shared` (run ONLY after Task 1 — v2 importers are gone; SC3 / D-08 order is locked: v2 first, then @eatme/shared). Four files:

    (1) `packages/shared/src/types/restaurant.ts`: delete the `export type DishKind = 'standard' | 'bundle' | 'configurable' | 'course_menu' | 'buffet';` line (line 26) WITH its `@deprecated` JSDoc block (lines 18-25). Delete the legacy `dish_kind?: DishKind;` field (line 171) WITH its `@deprecated` comment (lines 165-170). Do NOT touch the sibling legacy fields `parent_dish_id`/`is_parent`/`is_template`/`variants` — they are out of DEBT-03 scope (migration 163 cleanup, not this phase).

    (2) `packages/shared/src/types/index.ts`: delete the `DishKind,` line from the `export type { ... }` block (line 3).

    (3) `packages/shared/src/constants/menu.ts`: delete `export const DISH_KIND_META = { ... } as const;` (lines 25-31) WITH its `@deprecated` JSDoc (lines 14-24). KEEP `DINING_FORMATS` and everything below (line 33+) untouched.

    (4) DELETE `packages/shared/src/__tests__/dish-kinds.test.ts` entirely.

    Then run the SC3 zero-importer proof and `turbo check-types`. The grep MUST return zero lines (the only remaining `dish_kind` references in the repo are the out-of-scope inline `z.enum` literals and historical migration comments, which the `\b...\b` symbol grep below does not match):
    `git grep -nE "\bDishKind\b|\bDISH_KIND_META\b" -- 'apps/**' 'packages/**' ':!**/.next/**' ':!**/__tests__/**'`
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && test ! -f packages/shared/src/__tests__/dish-kinds.test.ts && IMP=$(git grep -nE "\bDishKind\b|\bDISH_KIND_META\b" -- 'apps/**' 'packages/**' ':!**/.next/**' | wc -l); test "$IMP" -eq 0 && turbo check-types && echo SHIMS_GONE</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f packages/shared/src/__tests__/dish-kinds.test.ts` (test deleted)
    - `grep -c "DISH_KIND_META" packages/shared/src/constants/menu.ts` returns 0
    - `grep -c "DINING_FORMATS" packages/shared/src/constants/menu.ts` returns at least 1 (DINING_FORMATS preserved — NOT collateral-deleted)
    - `grep -cE "export type DishKind" packages/shared/src/types/restaurant.ts` returns 0
    - `grep -c "dish_kind" packages/shared/src/types/restaurant.ts` returns 0 (legacy field gone)
    - `grep -c "DishKind" packages/shared/src/types/index.ts` returns 0 (re-export gone)
    - `git grep -nE "\bDishKind\b|\bDISH_KIND_META\b" -- 'apps/**' 'packages/**' ':!**/.next/**'` returns ZERO lines (SC3 zero-importer proof)
    - `turbo check-types` exits 0 (apps/admin + apps/web-portal-v2 compile against the slimmed @eatme/shared)
  </acceptance_criteria>
  <done>DishKind type, legacy dish_kind? field, DISH_KIND_META, the re-export, and dish-kinds.test.ts are all deleted; DINING_FORMATS is preserved; zero-importer grep is empty; turbo check-types passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @eatme/shared → consuming apps (admin, web-portal-v2) | Deleting an exported symbol can break any consumer at compile time; the grep + check-types gate is the proof there are none left. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Denial of Service | deleting a still-imported shim breaks app compile | mitigate | SC3-locked order (v2 severed in Task 1 BEFORE shim deletion in Task 2); zero-importer grep + `turbo check-types` gate both run after deletion; RESEARCH verified the 11 exact consumers, all in v2 + @eatme/shared |
| T-06-11 | Integrity | collateral deletion of DINING_FORMATS while removing DISH_KIND_META | mitigate | Acceptance grep asserts `DINING_FORMATS` still present in menu.ts; PATTERNS pins the exact line range (25-31) to delete |
| T-06-12 | Integrity | silent behavior change in MenuManager when input.dish_kind becomes undefined | mitigate | Task 1 explicitly reconciles the `input.dish_kind` reads (lines 167/170-173/207/209-210) to literal defaults so the optimistic-update shape stays coherent post-sever |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (deletions + edits only) — no legitimacy gate applicable |
</threat_model>

<verification>
- v2 severed FIRST: KindSelector deleted, DishForm flattened (no dish_kind), MenuManager reconciled, web-portal-v2 tsc clean.
- Shims deleted from @eatme/shared: DishKind type, legacy dish_kind? field, DISH_KIND_META, re-export, dish-kinds.test.ts.
- DINING_FORMATS preserved.
- SC3 zero-importer grep returns empty; `turbo check-types` green.
- Out-of-scope inline z.enum literals (validation/*, menu-scan-worker) untouched.
</verification>

<success_criteria>
DEBT-03 is satisfied: DishKind/DISH_KIND_META usage is removed from web-portal-v2 first, then the shims + test are deleted from @eatme/shared, proven by a zero-importer consumer grep and a green turbo check-types.
</success_criteria>

<output>
Create `.planning/phases/06-schema-teardown-spine/06-04-SUMMARY.md` when done
</output>
