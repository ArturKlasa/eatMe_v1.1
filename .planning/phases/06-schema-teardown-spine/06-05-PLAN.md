---
phase: 06-schema-teardown-spine
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - packages/database/src/types.ts
autonomous: true
requirements: [DEBT-04]
must_haves:
  truths:
    - "packages/database/src/types.ts contains zero references to any dropped ingredient table, the *_override columns, options.canonical_ingredient_id, or dish_kind (verified by grep)"
    - "No edge function in infra/supabase/functions/* carries an inline enum/type copy of a dropped ingredient DB object (D-10 verified — no-op for ingredients)"
    - "turbo check-types passes across apps/admin + apps/web-portal-v2 after the teardown authoring + DishKind removal"
  artifacts:
    - path: "packages/database/src/types.ts"
      provides: "Generated DB types confirmed in sync with the slimmed schema (DEBT-04)"
  key_links:
    - from: "packages/database/src/types.ts"
      to: "the slimmed schema (post-teardown)"
      via: "residue grep proves no dropped-object types remain; check-types is the backstop"
      pattern: "dish_ingredients|canonical_ingredient|allergens_override|dish_kind"
---

<!-- planner-discipline-allow: dish_ingredients|canonical_ingredient|ingredient_aliases|ingredient_concepts|ingredient_variants|concept_translations|variant_translations|allergens_override|dietary_tags_override|canonical_ingredient_id|dish_kind -->

<objective>
Confirm the generated DB types are in sync with the slimmed (post-teardown) schema (DEBT-04). RESEARCH verified that `packages/database/src/types.ts` (3226 lines) was ALREADY regenerated to a post-152/153/156/157/163 schema and carries ZERO residue (no ingredient tables, no `*_override` columns, no `options.canonical_ingredient_id`, no `dish_kind`) — this OVERTURNS FINDINGS F-07/F-15. So DEBT-04 is a VERIFICATION task, not a hand-edit (RESEARCH Pitfall 4: editing an already-correct types.ts introduces real drift). This plan: (1) re-greps types.ts for residue (expect zero — only edit if found); (2) verifies the D-10 inline edge-function enum reconciliation is a no-op for ingredients (no edge function inline-copies a dropped ingredient DB object); (3) runs `turbo check-types` as the SC4 cross-app backstop.

Purpose: SC4 requires the slimmed types.ts to no longer contain the dropped tables/columns, inline edge-function enum copies reconciled, and `turbo check-types` passing across all apps. Because the types are already slimmed and the edge functions carry no ingredient enum copies (RESEARCH verified — only retirement comments + the out-of-scope migration-163 `dish_kind` worker enum), this plan asserts cleanliness rather than performing edits. There is no Supabase CLI in this env, so D-09's "hand-edit types" path is only triggered if the residue grep finds something (it won't).
Output: types.ts confirmed residue-free (or minimally edited if the grep surprises us); D-10 documented as verified no-op; `turbo check-types` green.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-teardown-spine/06-RESEARCH.md
@.planning/phases/06-schema-teardown-spine/06-PATTERNS.md
@.planning/phases/06-schema-teardown-spine/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify types.ts residue-free + D-10 edge enum no-op + turbo check-types (SC4)</name>
  <files>packages/database/src/types.ts</files>
  <read_first>
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md sections "Type Regen Mechanics — types.ts is ALREADY slimmed", "Inline edge-function enum reconciliation (D-10) — essentially a no-op for ingredients", "Pitfall 4: Treating DEBT-04 as an edit task"
    - .planning/phases/06-schema-teardown-spine/06-PATTERNS.md section "packages/database/src/types.ts (DEBT-04) — VERIFY, do NOT edit" (the exact residue grep)
    - packages/database/src/types.ts (only if the residue grep below returns a hit — otherwise no edit; do NOT diff/rewrite large blocks per Pitfall 4)
  </read_first>
  <action>
    This is a VERIFICATION task (RESEARCH Pitfall 4) — do NOT hand-edit types.ts unless the residue grep returns a hit.

    (1) Residue grep on the generated types (expect ZERO):
    `grep -nE "dish_ingredients|canonical_ingredient|ingredient_aliases|ingredient_concepts|ingredient_variants|concept_translations|variant_translations|allergens_override|dietary_tags_override|canonical_ingredient_id|dish_kind" packages/database/src/types.ts`
    - If ZERO: types.ts is already in sync with the slimmed schema (DEBT-04 satisfied) — record this in the SUMMARY. Do NOT edit.
    - If NON-ZERO: surgically remove ONLY the matched dropped-object type blocks (the corresponding Row/Insert/Update/Relationships entries for the dropped table, or the dropped column key in the dishes/options blocks). Keep the edit minimal and reviewable; re-run the grep to confirm zero.

    (2) D-10 edge-function inline-enum verification — confirm no edge function inline-copies a dropped INGREDIENT DB object (expect this to be a documented no-op per RESEARCH):
    `git grep -nE "canonical_ingredient|dish_ingredients|ingredient_aliases|ingredient_concepts|ingredient_variants|allergens_override|dietary_tags_override" -- 'infra/supabase/functions/**'`
    - Expect only retirement COMMENTS (feed/index.ts, menu-scan-worker/index.ts) — no executable inline enum/type duplicating a dropped ingredient object. Record "D-10 verified — no ingredient enum copies in edge functions" in the SUMMARY.
    - Do NOT touch the migration-163-leftover `dish_kind: z.enum([...])` literal at `menu-scan-worker/index.ts:101` — it is out of this phase's scope (RESEARCH/PATTERNS boundary).
    - If any executable inline copy of a dropped ingredient object is found (unexpected), reconcile it to match the slimmed schema and run `deno check` on the touched function (`deno` at `~/.deno`; `deno test --node-modules-dir=none -A <path>`).

    (3) Run the SC4 cross-app backstop: `turbo check-types` (covers apps/admin + apps/web-portal-v2; @eatme/shared + @eatme/database are type-checked transitively). Must exit 0. Note: edge functions are Deno, NOT covered by turbo check-types — that is why step (2) uses git grep + (conditional) deno check.
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && RES=$(grep -cE "dish_ingredients|canonical_ingredient|ingredient_aliases|ingredient_concepts|ingredient_variants|concept_translations|variant_translations|allergens_override|dietary_tags_override|canonical_ingredient_id|dish_kind" packages/database/src/types.ts); test "$RES" -eq 0 && turbo check-types && echo TYPES_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "dish_ingredients|canonical_ingredient|ingredient_aliases|ingredient_concepts|ingredient_variants|concept_translations|variant_translations|allergens_override|dietary_tags_override|canonical_ingredient_id|dish_kind" packages/database/src/types.ts` returns 0 (no dropped-object residue)
    - `git grep -nE "canonical_ingredient|dish_ingredients|ingredient_aliases|ingredient_concepts|ingredient_variants|allergens_override|dietary_tags_override" -- 'infra/supabase/functions/**'` returns only comment lines (no executable inline enum/type copy of a dropped ingredient object)
    - `turbo check-types` exits 0
    - SUMMARY records "D-10 verified — no ingredient enum copies in edge functions" and whether types.ts needed any edit (expected: none)
  </acceptance_criteria>
  <done>types.ts is confirmed residue-free (no edit needed per RESEARCH, or minimally edited if the grep surprised us), the D-10 edge-function inline-enum reconciliation is documented as a verified no-op for ingredients, and turbo check-types passes across all apps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated types.ts → all TS consumers | A drifted or hand-mangled types.ts can mask a real schema mismatch or break compilation across both apps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Integrity | hand-editing an already-correct types.ts introduces drift | mitigate | Treated as a VERIFICATION task (RESEARCH Pitfall 4) — grep-first, edit only on a hit; no large-block diff/rewrite; `turbo check-types` is the backstop |
| T-06-14 | Integrity | a dropped object's type silently lingering in types.ts | mitigate | Residue grep across the full dropped-object set asserts zero; if non-zero, surgical removal + re-grep |
| T-06-15 | Integrity | out-of-scope edge enum edit (migration-163 dish_kind) sneaking in | accept | Explicitly out of scope per RESEARCH/PATTERNS; action forbids touching menu-scan-worker:101; D-10 scoped to ingredient objects only |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (verification + at most surgical type edits) — no legitimacy gate applicable |
</threat_model>

<verification>
- types.ts residue grep returns 0 across the full dropped-object set (already slimmed per RESEARCH; overturns F-07/F-15).
- D-10 edge-function inline-enum reconciliation verified as a no-op for ingredients (only retirement comments; the migration-163 dish_kind worker enum is out of scope).
- `turbo check-types` passes across apps/admin + apps/web-portal-v2 (SC4 backstop).
</verification>

<success_criteria>
DEBT-04 is satisfied: the slimmed types.ts is confirmed to contain none of the dropped tables/columns, the inline edge-function enum reconciliation is documented as a verified no-op for ingredients, and turbo check-types passes across all apps.
</success_criteria>

<output>
Create `.planning/phases/06-schema-teardown-spine/06-05-SUMMARY.md` when done
</output>