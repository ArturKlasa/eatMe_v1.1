---
phase: 06-schema-teardown-spine
plan: 06
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-05"]
files_modified:
  - .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md
autonomous: false
requirements: [DEBT-01, DEBT-02]
must_haves:
  truths:
    - "The operator receives the bundled LIVE-STATE PROBE + pg_depend DEPENDENCY-AUDIT SQL to run BEFORE any drop"
    - "The operator receives the exact apply order: probe + audit (paste-back) → 171 triggers → 172 snapshot (+landing check) → 173 tables RESTRICT → 174 columns"
    - "The operator receives the post-apply verification: verify-phase6-teardown.ts + re-run probe, expecting all-GONE"
    - "The phase is NOT marked complete until the operator pastes back a clean post-apply probe (this handoff is the load-bearing real-world gate, replacing the CLI schema-push)"
  artifacts:
    - path: ".planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md"
      provides: "The operator apply-and-verify runbook (stage-don't-apply handoff)"
      contains: "LIVE-STATE PROBE"
  key_links:
    - from: ".planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md"
      to: "infra/scripts/verify-phase6-teardown.ts"
      via: "post-apply verification step references the verify script + the re-run probe"
      pattern: "verify-phase6-teardown"
---

<objective>
Assemble and hand the operator the apply-and-verify runbook for the authored teardown migrations (the stage-don't-apply gate that REPLACES the standard CLI schema-push, because there is no Supabase CLI in this env — REST-only). The runbook bundles: (a) the LIVE-STATE PROBE + pg_depend DEPENDENCY-AUDIT SQL (from RESEARCH) the operator pastes-and-runs in the Supabase dashboard SQL editor BEFORE any drop; (b) the exact apply order of migrations 171/172/173/174 with the 172-snapshot landing check between 172 and 173; (c) the post-apply verification (`verify-phase6-teardown.ts` + re-running the probe, expecting all-GONE). The phase CANNOT be verified complete without the operator's clean post-apply paste-back — build/type checks pass without applying the drops (types come from the already-slimmed types.ts, not the live DB), so this handoff + paste-back is the load-bearing real-world gate.

Purpose: The phase modifies schema-relevant `infra/supabase/migrations/*.sql`. The standard GSD schema-push gate would inject a `supabase db push` task — WRONG here (no CLI; applying drops from the agent would be an irreversible prod mutation violating stage-don't-apply). This BLOCKING, autonomous:false task delivers the operator the safe manual path instead. It depends on every migration being authored (06-02, 06-03) and the verify script existing (06-01), plus types confirmed (06-05).
Output: `06-OPERATOR-HANDOFF.md` runbook + a blocking human checkpoint. Phase completion is gated on the operator's clean paste-back.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-teardown-spine/06-RESEARCH.md
@.planning/phases/06-schema-teardown-spine/06-CONTEXT.md
@infra/scripts/verify-phase6-teardown.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the operator apply-and-verify runbook (06-OPERATOR-HANDOFF.md)</name>
  <files>.planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md</files>
  <read_first>
    - .planning/phases/06-schema-teardown-spine/06-RESEARCH.md sections "Operator paste-back probe (single bundled SELECT)" and "pg_depend / FK audit pre-flight query" (copy BOTH SQL blocks verbatim into the runbook)
    - infra/scripts/verify-phase6-teardown.ts (the post-apply read-only check the runbook tells the operator to run)
    - The five authored migration files from 06-02/06-03 (171, 172, 173, 174 + reverses) so the runbook references their exact filenames
  </read_first>
  <action>
    Author `.planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` — a step-by-step runbook the operator follows in the Supabase dashboard SQL editor. Sections:

    (1) PRE-FLIGHT (run BEFORE any drop, paste-back required): copy the LIVE-STATE PROBE SQL (single `jsonb_build_object` SELECT, RESEARCH "Operator paste-back probe") and the pg_depend DEPENDENCY-AUDIT SQL (RESEARCH "pg_depend / FK audit pre-flight query") VERBATIM into fenced sql blocks. Instruct: paste each into the dashboard SQL editor, run, paste the JSON results back. STOP CONDITIONS — do NOT apply any drop if: `external_fks_into_set` is non-empty OR `dependent_objects` is non-empty (a hard blocker — an unknown view/FK depends on an ingredient object; bring it back to planning). Note that `archive_row_counts` of `-1` means a table is already absent (its drop is a no-op) and `>= 0` means it has rows worth archiving.

    (2) APPLY ORDER (exact, one migration at a time): 
    Step A — apply `171_retire_ingredient_triggers_reconciled.sql` (drops triggers/functions; idempotent IF EXISTS).
    Step B — apply `172_snapshot_ingredient_archive.sql` (creates ingredient_archive). Then run the LANDING CHECK: `SELECT table_name FROM information_schema.tables WHERE table_schema = 'ingredient_archive';` — confirm the archive tables (the ones the probe reported present) landed BEFORE proceeding (D-02). Do NOT proceed to 173 until the archive is confirmed.
    Step C — apply `173_drop_ingredient_tables_restrict.sql` (severs options.canonical_ingredient_id FIRST, then RESTRICT-drops the tables child-to-parent). If a RESTRICT drop ERRORS with a dependency, STOP — that is the loud-failure signal SC2 wants; an unexpected dependent exists. Bring it back to planning.
    Step D — apply `174_drop_ingredient_columns_restrict.sql` (drops dishes.allergens_override + dietary_tags_override).
    Note for each: the paired `*_REVERSE_ONLY_*.sql` exists if rollback is ever needed (schema-only; data restored manually from ingredient_archive).

    (3) SUPERSEDED-MIGRATION NOTE: explicitly tell the operator that `151_retire_ingredient_triggers.sql`, `152_drop_ingredient_pipeline.sql`, and `153_drop_ingredient_columns.sql` (the older CASCADE-based / broken-REVERSE versions) MUST NOT be applied — they are superseded by 171/173/174 and left in the repo as historical-but-unapplied. Applying 152/153 (CASCADE) would violate SC2; applying 151's REVERSE would fail post-156.

    (4) POST-APPLY VERIFICATION (paste-back required): run `cd infra/scripts && pnpm exec ts-node verify-phase6-teardown.ts` — expect every table + column line to read `GONE ✓`. Then RE-RUN the LIVE-STATE PROBE from section (1) — expect `ingredient_tables_present`=[], `ingredient_triggers_present`=[], `ingredient_functions_present`=[], `dead_columns_present`={}. Paste both results back. The phase is NOT complete until this paste-back is clean.

    Keep all SQL in fenced ```sql blocks with `--` comment prefixes (so any static-safety gate strips forbidden tokens). Reference the authored migration filenames exactly.
  </action>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && test -f .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md && grep -q "LIVE-STATE PROBE" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md && grep -q "phase6_depaudit" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md && grep -q "verify-phase6-teardown" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md && grep -qE "171_retire_ingredient_triggers_reconciled|173_drop_ingredient_tables_restrict" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md && echo HANDOFF_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` succeeds
    - `grep -q "phase6_probe" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` (LIVE-STATE PROBE copied verbatim)
    - `grep -q "phase6_depaudit" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` (DEPENDENCY-AUDIT copied verbatim)
    - `grep -q "ingredient_archive" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` (172 landing check present)
    - `grep -qE "MUST NOT be applied|superseded" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` (151/152/153 superseded note present)
    - `grep -q "verify-phase6-teardown" .planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md` (post-apply verification step present)
    - Apply order 171 → 172 (+landing check) → 173 → 174 appears in sequence in the file
  </acceptance_criteria>
  <done>06-OPERATOR-HANDOFF.md contains the verbatim LIVE-STATE PROBE + pg_depend DEPENDENCY-AUDIT, the exact apply order with the 172-snapshot landing check, the 151/152/153-superseded note, and the post-apply verify-script + re-run-probe step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING operator apply-and-verify gate (replaces the CLI schema-push)</name>
  <what-built>The teardown migrations (171 triggers, 172 snapshot, 173 tables RESTRICT, 174 columns) are authored + dry-run only, the read-only verify-phase6-teardown.ts is written, and 06-OPERATOR-HANDOFF.md gives the full apply-and-verify runbook. NOTHING has been applied to prod — that is the operator's manual step (stage-don't-apply; no CLI in this env).</what-built>
  <how-to-verify>
    1. Open `.planning/phases/06-schema-teardown-spine/06-OPERATOR-HANDOFF.md`.
    2. PRE-FLIGHT: paste the LIVE-STATE PROBE and the pg_depend DEPENDENCY-AUDIT into the Supabase dashboard SQL editor, run each, and confirm `external_fks_into_set` and `dependent_objects` are BOTH empty (if either has rows, STOP and report back — do not apply).
    3. APPLY in order: 171 → 172 → (run the ingredient_archive landing check; confirm the archive tables landed) → 173 → 174. If any RESTRICT drop errors, STOP and report the dependency.
    4. Do NOT apply 151 / 152 / 153 (superseded — see the runbook note).
    5. POST-APPLY: run `cd infra/scripts && pnpm exec ts-node verify-phase6-teardown.ts` (expect all `GONE ✓`) and re-run the LIVE-STATE PROBE (expect all-empty / `{}`).
    6. Paste back the post-apply probe + verify-script output.
  </how-to-verify>
  <action>Present `06-OPERATOR-HANDOFF.md` to the operator and PAUSE. Do NOT apply any migration from the agent (stage-don't-apply; no CLI). Wait for the operator's paste-back of the clean post-apply probe + verify-script output before marking the phase complete. If the operator reports a STOP-condition (non-empty dep audit, a RESTRICT failure, an unexpected STILL-EXISTS), surface it back to planning rather than proceeding.</action>
  <resume-signal>Paste back the clean post-apply probe + verify-script output (all GONE / empty), or report any STOP-condition (non-empty dep audit, a RESTRICT failure, or an unexpected STILL-EXISTS).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (authored migrations) → operator → prod Postgres | The agent never touches prod; the operator is the sole apply path. The handoff must make the safe order + STOP conditions unmissable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering (irreversible data loss) | applying drops before the snapshot lands | mitigate | Runbook enforces 172 snapshot + landing check BEFORE 173 drop (D-02); blocking human checkpoint; operator pastes back the landing check |
| T-06-17 | Tampering | applying a dependent-blocked or wrong-ordered drop | mitigate | Pre-flight pg_depend audit with explicit STOP conditions (non-empty external_fks/dependent_objects); RESTRICT fails loudly; runbook says STOP on a RESTRICT error |
| T-06-18 | Integrity | operator applies the superseded CASCADE migrations (152/153) or 151's broken REVERSE | mitigate | Runbook explicitly lists 151/152/153 as MUST-NOT-apply / superseded, with the reason (CASCADE violates SC2; 151_REVERSE fails post-156) |
| T-06-19 | Repudiation | phase marked complete without real-world confirmation | mitigate | autonomous:false blocking-human checkpoint; phase completion gated on the operator's clean post-apply paste-back (this replaces the CLI schema-push gate) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (doc authoring + operator handoff) — no legitimacy gate applicable |
</threat_model>

<verification>
- 06-OPERATOR-HANDOFF.md contains the verbatim LIVE-STATE PROBE + pg_depend DEPENDENCY-AUDIT, the exact apply order with the snapshot landing check, the 151/152/153-superseded note, and the post-apply verify-script + re-run-probe step.
- The blocking-human checkpoint gates phase completion on the operator's clean post-apply paste-back.
- Nothing is applied to prod by the agent (stage-don't-apply).
</verification>

<success_criteria>
The phase's real-world gate is in place: the operator has a complete, safe apply-and-verify runbook (replacing the CLI schema-push), and the phase cannot be marked complete until the operator pastes back a clean post-apply probe + verify-script output confirming the ingredient teardown is live and GONE.
</success_criteria>

<output>
Create `.planning/phases/06-schema-teardown-spine/06-06-SUMMARY.md` when done
</output>
