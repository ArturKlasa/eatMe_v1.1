---
phase: 07-performance-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json
  - infra/supabase/functions/feed/__tests__/tiered-loop.test.ts
  - infra/supabase/functions/feed/__tests__/precap-behavior.test.ts
  - infra/supabase/functions/invalidate-cache/__tests__/delete-path.test.ts
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
must_haves:
  truths:
    - "A representative multi-restaurant candidate fixture exists (≥3 restaurants, ≥8 dishes each) usable by the byte/row + behavior-preserving harnesses"
    - "A Deno test harness exists that stubs supabase.rpc and asserts the tiered loop breaks at POOL_TARGET and returns a Candidate[]-shaped pool"
    - "A Deno test harness exists that parses a DELETE webhook body {type:'DELETE', old_record:{...}, record:null} and asserts the per-restaurant key resolves from old_record"
    - "A behavior-preserving harness exists that runs the JS diversity path on old-200 vs new-K-capped input and asserts an identical dish-ID list + reduced byte length"
  artifacts:
    - path: "infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json"
      provides: "Representative Candidate[] fixture: ≥3 restaurants × ≥8 dishes each, with modifier_groups JSON populated"
      contains: "restaurant_id"
    - path: "infra/supabase/functions/feed/__tests__/tiered-loop.test.ts"
      provides: "Deno test: tiered-loop break-at-POOL_TARGET + response-shape parity, stubbing supabase.rpc"
      contains: "POOL_TARGET"
    - path: "infra/supabase/functions/feed/__tests__/precap-behavior.test.ts"
      provides: "Deno test: byte/row-count delta + behavior-preserving diversified-output assertion on the fixture"
      contains: "applyDiversity"
    - path: "infra/supabase/functions/invalidate-cache/__tests__/delete-path.test.ts"
      provides: "Deno test: DELETE webhook body parsing resolves per-restaurant key from old_record"
      contains: "old_record"
  key_links:
    - from: "infra/supabase/functions/feed/__tests__/precap-behavior.test.ts"
      to: "infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json"
      via: "imports the fixture JSON as the representative candidate pool"
      pattern: "multi-restaurant-pool"
---

<objective>
Author the Wave 0 validation scaffolding that every later Phase 7 plan depends on: a representative multi-restaurant candidate fixture and three targeted Deno test harnesses (tiered-loop break logic, per-restaurant pre-cap behavior-preservation, and the invalidate-cache DELETE path). None of these exist today (F-26 — no existing Deno suite under `feed/`).

Purpose: nyquist_validation is enabled (Dimension 8). The behavior-preserving seams in this phase (tiered loop, SQL pre-cap, DELETE-path key resolution) can only be validated WITHOUT prod access by stubbing `supabase.rpc` and running the JS pipeline against a fixed fixture. These harnesses are the automated `<verify>` backing for Plans 02, 03, and 04.
Output: 1 JSON fixture + 3 Deno test files. They reference helper logic (loop break, pre-cap diversity) that the later plans wire into the real source; in Wave 0 the harnesses encode the EXPECTED contract so the later plans turn them green.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-cache/07-RESEARCH.md
@.planning/phases/07-performance-cache/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the representative multi-restaurant candidate fixture</name>
  <read_first>
    - infra/supabase/functions/feed/index.ts (lines 168-211 — the `interface Candidate` shape the fixture rows MUST match; lines 603-650 — `applyDiversity`; lines 940-1014 — how `pool` rows flow into `dishResult` build, so the fixture carries the heavy `modifier_groups` field)
    - infra/supabase/migrations/169_generate_candidates_pushdown.sql (lines 56-89 — the 32-column RETURNS TABLE shape that `generate_candidates` produces; the fixture rows mirror this projection)
  </read_first>
  <files>infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json</files>
  <action>
    Create a JSON array fixture representing a realistic Stage-1 candidate pool. Requirements:
    - At least 3 distinct `restaurant_id` values, each with at least 8 dishes (≥24 rows total) so the per-restaurant pre-cap (K=8) and the JS max-3 diversity cap both have something to bite on. Make ONE restaurant dominate (e.g. 12 dishes) so the row-count drop from the K=8 pre-cap is measurable.
    - Each row carries the fields the Candidate interface declares: `id` (uuid string), `restaurant_id` (uuid string), `name`, `description`, `price` (number), `primary_protein`, `vector_distance` (number, ascending across the pool — a realistic ANN order), `distance_m` (number), `popularity_score` (number), `protein_families` (array), `restaurant_name`, `restaurant_cuisines` (array), `restaurant_currency_code`, and a populated `modifier_groups` JSON array (the heaviest field — include 2-3 option_groups with 3-4 options each on most rows so the serialized-byte delta is non-trivial). Include the migration-167 fold columns `open_hours`, `timezone`, `country_code`.
    - Give `vector_distance` values monotonically increasing across the array (so it reads as a valid ANN-ordered pool) and vary `popularity_score`/`distance_m` so the pre-cap window ORDER BY (vector_distance, popularity_score DESC, distance_m ASC) is exercised.
    - Keep it deterministic (no random) — the harnesses assert exact dish-ID lists.
    Do NOT inline this fixture into the test files; it is shared by `tiered-loop.test.ts` and `precap-behavior.test.ts`.
  </action>
  <verify>
    <automated>test -f infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json && node -e "const a=require('./infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json'); const r=new Set(a.map(x=>x.restaurant_id)); if(a.length<24) throw new Error('need >=24 rows, got '+a.length); if(r.size<3) throw new Error('need >=3 restaurants, got '+r.size); const counts={}; a.forEach(x=>counts[x.restaurant_id]=(counts[x.restaurant_id]||0)+1); if(Math.max(...Object.values(counts))<8) throw new Error('need a restaurant with >=8 dishes'); if(!a.every(x=>'modifier_groups' in x && 'vector_distance' in x)) throw new Error('rows missing modifier_groups/vector_distance'); console.log('fixture OK:',a.length,'rows,',r.size,'restaurants');"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/supabase/functions/feed/__tests__/fixtures/multi-restaurant-pool.json` exists and is valid JSON
    - The fixture is an array of ≥24 objects spanning ≥3 distinct `restaurant_id` values
    - At least one restaurant has ≥8 dishes (so K=8 pre-cap + max-3 cap are exercised)
    - Every row contains `modifier_groups` and `vector_distance` keys
    - `vector_distance` is monotonically non-decreasing across the array
  </acceptance_criteria>
  <done>The fixture exists, parses, and satisfies the ≥3-restaurant / ≥8-dish / dominant-restaurant / populated-modifier_groups shape.</done>
</task>

<task type="auto">
  <name>Task 2: Author the tiered-loop break-logic Deno harness</name>
  <read_first>
    - infra/supabase/functions/feed/index.ts (lines 881-917 — the exact RPC call site the loop will wrap, the `const pool = candidates ?? []` shape at :903, and the empty-pool early return at :906-915 that must stay untouched)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§2 — the loop design: TIER_FRACTIONS=[0.25,0.5,1.0], POOL_TARGET=100, replace-don't-merge, break-on-POOL_TARGET)
    - .planning/phases/07-performance-cache/07-PATTERNS.md (the feed/index.ts pattern block — the named-constant loop excerpt)
    - infra/supabase/functions/_shared/cors.test.ts (existing in-repo Deno test style — Deno.test + assert imports from jsr:@std/assert@1.0.19)
  </read_first>
  <files>infra/supabase/functions/feed/__tests__/tiered-loop.test.ts</files>
  <action>
    Write a Deno test that encodes the tiered-loop contract independent of the full edge function. Export (or inline) a small pure helper `runTieredFetch(rpcStub, requestedRadiusM, TIER_FRACTIONS, POOL_TARGET)` that reproduces the loop logic from RESEARCH §2: for each fraction it calls `rpcStub(Math.round(requestedRadiusM * frac))`, assigns `pool = result ?? []` (REPLACE, not merge), and breaks once `pool.length >= POOL_TARGET`. Use the named constants `TIER_FRACTIONS = [0.25, 0.5, 1.0]` and `POOL_TARGET = 100`.
    Assert THREE behaviors with a stubbed rpc:
    1. Break-at-POOL_TARGET: stub returns 40 rows at 0.25, 120 rows at 0.5 → loop stops after the 0.5 tier (does NOT call the 1.0 tier). Track call count on the stub; assert it equals 2 and the radius passed on call 2 is `Math.round(requestedRadiusM * 0.5)`.
    2. Sparse fall-through: stub returns 5 rows at every tier → loop runs all 3 tiers, final radius == `requestedRadiusM` (the 1.0 tier == requested radius, never exceeds it — D-03), final pool == the last tier's result (replace semantics).
    3. Shape parity: the returned value is an array (the same `Candidate[]` shape `:903` produces) — assert `Array.isArray(pool)` and that a representative row from the fixture round-trips unchanged through the helper.
    Reference POOL_TARGET and TIER_FRACTIONS by those exact names so a grep gate can confirm they are present.
    Run command form (deno at ~/.deno per MEMORY): `~/.deno/bin/deno test --node-modules-dir=none -A infra/supabase/functions/feed/__tests__/tiered-loop.test.ts`.
  </action>
  <verify>
    <automated>~/.deno/bin/deno test --node-modules-dir=none -A infra/supabase/functions/feed/__tests__/tiered-loop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tiered-loop.test.ts` exists and references the literal `POOL_TARGET` and the literal array `[0.25, 0.5, 1.0]`
    - The test asserts the loop breaks after the tier that first reaches POOL_TARGET (call-count assertion)
    - The test asserts the sparse case runs all 3 tiers and the final radius equals the requested radius (never exceeds it)
    - `~/.deno/bin/deno test --node-modules-dir=none -A infra/supabase/functions/feed/__tests__/tiered-loop.test.ts` exits 0
  </acceptance_criteria>
  <done>The Deno test passes and locks the break-at-POOL_TARGET, sparse-fall-through-to-requested-radius, and shape-parity contract for the tiered loop.</done>
</task>

<task type="auto">
  <name>Task 3: Author the pre-cap behavior-preserving harness and the DELETE-path harness</name>
  <read_first>
    - infra/supabase/functions/feed/index.ts (lines 603-650 — `applyDiversity(dishes, maxPerRestaurant)`; line 951 — `applyDiversity(scored, 3)`; lines 980-1014 — the `dishResult` builder used for the byte measurement)
    - infra/supabase/functions/invalidate-cache/index.ts (line 54 — `const record = body.record ?? {}`; lines 79-100 — per-restaurant key resolution; line 76 — the unconditional flush-all that does NOT depend on record)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§3 "Measuring measurably reduced" — byte/row delta method; §4 "DELETE path" — old_record fallback)
  </read_first>
  <files>infra/supabase/functions/feed/__tests__/precap-behavior.test.ts, infra/supabase/functions/invalidate-cache/__tests__/delete-path.test.ts</files>
  <action>
    File A — `precap-behavior.test.ts`: import the fixture from Task 1. Implement a pure `precap(pool, K)` helper that mirrors the SQL `ROW_NUMBER() OVER (PARTITION BY restaurant_id ORDER BY vector_distance ASC NULLS LAST, popularity_score DESC, distance_m ASC)` then `WHERE rn <= K` — i.e. group by `restaurant_id`, sort each group by that key tuple, keep the first K. Assert:
    1. Row-count delta: `precap(pool, 8).length < pool.length` on the fixture (the dominant restaurant loses rows). Record both counts.
    2. Byte delta: `JSON.stringify(precap(pool, 8)).length < JSON.stringify(pool).length` (serialized-byte reduction — the modifier_groups JSON dropped with the trimmed rows). This is the SC#3 "measurably reduced" automated gate.
    3. Behavior-preserving: replicate the JS diversity cap as `applyDiversity(sorted, 3)` (max-3-per-restaurant by score order). Build a representative scored ordering (sort the pool, and the pre-capped pool, by the same key), run a local `applyDiversity(_, 3)` over BOTH the full-200-equivalent pool and the K=8-capped pool, and assert the resulting dish-ID list is IDENTICAL. (K=8 > 2.5× the max-3 cap, so the proxy pre-cap must not change which 3/restaurant survive.) Reference `applyDiversity` by name.
    File B — `delete-path.test.ts`: implement a pure `resolveRecord(body)` mirroring the planned `body.record ?? body.old_record ?? {}` fallback. Assert:
    1. On `{type:'DELETE', table:'restaurants', record:null, old_record:{id:'rest-1'}}` → `resolveRecord(body).id === 'rest-1'` (resolves from old_record).
    2. On `{type:'UPDATE', table:'menus', record:{restaurant_id:'rest-2'}, old_record:{...}}` → resolves from record (unchanged behavior).
    3. The flush-all is event-independent: assert a documented constant/comment that `deleteByPattern('feed:v2:*')` runs regardless of type (encode it as a boolean the helper returns or a comment-asserted invariant). Reference `old_record` by name.
    Run both with `~/.deno/bin/deno test --node-modules-dir=none -A <path>`.
  </action>
  <verify>
    <automated>~/.deno/bin/deno test --node-modules-dir=none -A infra/supabase/functions/feed/__tests__/precap-behavior.test.ts infra/supabase/functions/invalidate-cache/__tests__/delete-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `precap-behavior.test.ts` exists, imports `multi-restaurant-pool.json`, references `applyDiversity`, and asserts both a row-count drop and a byte-length drop for K=8
    - `precap-behavior.test.ts` asserts the diversified dish-ID list is identical for full-pool vs K=8-capped pool
    - `delete-path.test.ts` exists, references `old_record`, and asserts DELETE resolves the record from `old_record` while UPDATE resolves from `record`
    - `~/.deno/bin/deno test --node-modules-dir=none -A` over both files exits 0
  </acceptance_criteria>
  <done>Both harnesses pass: the pre-cap byte/row/behavior-preservation contract (SC#3) and the DELETE-path old_record fallback contract (SC#4) are locked as automated gates.</done>
</task>

</tasks>

<verification>
- All three Deno test files pass under `~/.deno/bin/deno test --node-modules-dir=none -A`.
- The fixture satisfies the ≥3-restaurant / ≥8-dish-per-restaurant / dominant-restaurant / populated-modifier_groups shape (Task 1 node assertion).
- The harnesses encode contracts that Plans 02 (pre-cap), 03 (tiered loop), and 04 (DELETE path) make true against the real source — they are the Wave-0 dependency for those plans' `<verify>` blocks.
- After this plan, update `.planning/phases/07-performance-cache/07-VALIDATION.md`: set `wave_0_complete: true` once all three harnesses are green (the Per-Task Verification Map is finalized in the later plans).
</verification>

<success_criteria>
- 1 fixture + 3 Deno test files exist and pass.
- No source file under `feed/` or `invalidate-cache/` (outside `__tests__/`) is modified by this plan.
- The harnesses run in < 15s total (validation latency budget).
</success_criteria>

<output>
Create `.planning/phases/07-performance-cache/07-01-SUMMARY.md` when done.
</output>
