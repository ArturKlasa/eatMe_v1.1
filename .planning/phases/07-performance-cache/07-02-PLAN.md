---
phase: 07-performance-cache
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql
  - infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "generate_candidates has the hnsw.iterative_scan GUC (relaxed_order) scoped to it via ALTER FUNCTION, with companion max_scan_tuples / scan_mem_multiplier / ef_search GUCs (D-04/D-05)"
    - "generate_candidates returns at most K=8 rows per restaurant, ordered by vector_distance then popularity_score then distance_m, before the LIMIT p_limit (D-06/D-07)"
    - "The migration-167 open-hours fold and the single round-trip LATERAL modifier-JSON build are preserved (not reversed)"
    - "A REVERSE pair restores the migration-169 function body verbatim and RESETs all four GUCs"
  artifacts:
    - path: "infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql"
      provides: "Coordinated CREATE OR REPLACE generate_candidates (pre-cap) + ALTER FUNCTION SET iterative_scan GUCs"
      contains: "hnsw.iterative_scan"
    - path: "infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql"
      provides: "REVERSE: RESET the 4 GUCs + restore migration-169 body verbatim (no DROP, signature unchanged)"
      contains: "RESET hnsw.iterative_scan"
  key_links:
    - from: "infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql"
      to: "infra/supabase/migrations/136_hnsw_dishes_embedding.sql"
      via: "the iterative_scan GUC tunes the HNSW index scan on dishes.embedding inside the materialized CTE"
      pattern: "hnsw\\.iterative_scan"
    - from: "infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql"
      to: "infra/supabase/migrations/169_generate_candidates_pushdown.sql"
      via: "extends the 169 candidates CTE with a ROW_NUMBER pre-cap; 32-column shape unchanged (CREATE OR REPLACE)"
      pattern: "ROW_NUMBER\\(\\) OVER \\(\\s*PARTITION BY"
---

<objective>
Author migration 175 — the ONE coordinated `generate_candidates` change set (D-11): (a) scope the `hnsw.iterative_scan = 'relaxed_order'` GUC plus companion bounds to the function via `ALTER FUNCTION ... SET` (D-04/D-05, PERF-01 SC#2), and (b) inject a per-restaurant `ROW_NUMBER()` pre-cap (K=8) into migration 169's materialized `candidates` CTE before the `LIMIT p_limit` (D-06/D-07, PERF-02 SC#3). Plus the `175_REVERSE_ONLY_*` pair. Both changes edit the same function, so they land together (D-11).

Purpose: iterative_scan fixes filtered-ANN under-return (the durable half of PERF-01); the pre-cap bounds the Stage-1→Stage-2 handoff payload (PERF-02). Both preserve the 32-column return shape, the single round-trip, and the migration-167 open-hours fold.
Output: 2 SQL files (forward + REVERSE), authored + SQL-parse-validated only. NO `supabase db push` — stage-don't-apply; the operator applies to prod (Plan 05 handoff).
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-cache/07-RESEARCH.md
@.planning/phases/07-performance-cache/07-PATTERNS.md
@infra/supabase/migrations/169_generate_candidates_pushdown.sql
@infra/supabase/migrations/169_REVERSE_ONLY_generate_candidates_pushdown.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 175 — pre-cap + iterative_scan GUC (coordinated)</name>
  <read_first>
    - infra/supabase/migrations/169_generate_candidates_pushdown.sql (THE function being extended — full file. Critical anchors: signature lines 41-55; 32-column RETURNS TABLE lines 56-89; `SET search_path` line 93; `#variable_conflict use_column` directive line 95; the `WITH candidates AS MATERIALIZED (` open at line 101; the column projection 102-144 including the migration-167 fold columns `r.open_hours, r.timezone, r.country_code` at 142-144; the `vector_distance` CASE at 115-119; `distance_m` at 121-124; `popularity_score` at 132; the JOINs at 146-151; the WHERE block 153-299; the inner `ORDER BY vector_distance ASC NULLS LAST, popularity_score DESC, distance_m ASC` at 301-304; `LIMIT p_limit` at 306; the CTE close `)` at 307; the LATERAL outer SELECT begins at 310)
    - infra/supabase/migrations/136_hnsw_dishes_embedding.sql (the HNSW index the iterative_scan GUC operates over — confirm the index name and that it is on dishes.embedding)
    - infra/supabase/migrations/167_REVERSE_ONLY_generate_candidates_open_hours.sql (lines 13-15 — proves the full 13-arg ALTER FUNCTION signature form; `vector(1536)` may be written bare `vector`)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§1 GUC values table + ALTER FUNCTION syntax; §3 Option A pre-cap placement; §5 REVERSE content)
    - .planning/phases/07-performance-cache/07-PATTERNS.md (the migration 175 pattern block — both the GUC excerpt and the Option A windowed-subquery excerpt)
  </read_first>
  <files>infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql</files>
  <action>
    Author `175_generate_candidates_precap_iterative_scan.sql`. Wrap all DDL in `BEGIN; ... COMMIT;` (132/135 convention). Header block (── prose, every line starting with `--`) must state: this is the coordinated D-11 change set (iterative_scan GUC + per-restaurant pre-cap); 32-column shape unchanged → CREATE OR REPLACE, no DROP, no deploy-ordering; authored + dry-run only (operator applies + validates recall/latency on prod, D-04 gate); the GUC values are tunable starting points.

    PART (a) — iterative_scan GUC via ALTER FUNCTION (D-04/D-05). Use the full 13-arg signature taken verbatim from 169:42-54 (write `vector` for the vector(1536) arg):
      ALTER FUNCTION generate_candidates(
        FLOAT, FLOAT, FLOAT, vector, UUID[], TEXT, TEXT[], BOOLEAN, INT, TIME, TEXT, TEXT, BOOLEAN
      )
        SET hnsw.iterative_scan      = 'relaxed_order'   -- D-05: order re-imposed downstream by the CTE + outer ORDER BY
        SET hnsw.max_scan_tuples     = 20000             -- default ceiling; bounds worst-case cost (tunable)
        SET hnsw.scan_mem_multiplier = 2                 -- headroom over work_mem (tunable)
        SET hnsw.ef_search           = 400;              -- >= 2x p_limit=200 per pgvector guidance (tunable)
    Include the Pitfall-1 fallback as a commented note: if the comma-list `SET a, SET b` form is rejected by the prod pg version, emit one `ALTER FUNCTION ... SET <guc> = <val>;` statement per GUC (all independent/idempotent). Document each value's rationale in a trailing `--` comment as above. Do NOT use the comma form between SET clauses if the file targets that fallback — pick the comma-separated multi-SET form as primary (matches RESEARCH §1) and keep the per-GUC fallback as commented prose only.

    PART (b) — per-restaurant ROW_NUMBER pre-cap (D-06/D-07, RESEARCH §3 Option A). Re-emit the ENTIRE `CREATE OR REPLACE FUNCTION generate_candidates(...)` verbatim from migration 169 (signature, 32-col RETURNS TABLE, `LANGUAGE plpgsql STABLE SECURITY DEFINER`, `SET search_path = extensions, public`, the `#variable_conflict use_column` directive — KEEP it, the LATERAL modifier-JSON block 308+, the `reachable_proteins`/`reachable_protein_families` projection, the open_hours/timezone/country_code columns) with EXACTLY ONE structural change: wrap the inner SELECT of the `candidates AS MATERIALIZED` CTE in a windowed subquery and filter `WHERE rn <= 8` before the existing global ORDER BY + LIMIT. Concretely, transform:
      WITH candidates AS MATERIALIZED (
        SELECT <169:102-144 columns> FROM dishes d JOIN ... WHERE <169:153-299> ORDER BY vector_distance ASC NULLS LAST, popularity_score DESC, distance_m ASC LIMIT p_limit )
    into:
      WITH candidates AS MATERIALIZED (
        SELECT * FROM (
          SELECT <169:102-144 columns SAME>,
            ROW_NUMBER() OVER (
              PARTITION BY d.restaurant_id
              ORDER BY
                CASE WHEN p_preference_vector IS NOT NULL AND d.embedding IS NOT NULL
                     THEN (d.embedding <=> p_preference_vector) ELSE NULL END ASC NULLS LAST,
                COALESCE(da.popularity_score, 0) DESC,
                ST_Distance(r.location_point, ST_SetSRID(ST_MakePoint(p_lng, p_lat), 4326)::geography) ASC
            ) AS rn
          FROM dishes d JOIN ... <169:146-151 JOINs SAME> WHERE <169:153-299 SAME>
        ) ranked
        WHERE ranked.rn <= 8   -- per-restaurant pre-cap K (D-07; range 5-10, start 8 = >2.5x the JS max-3 cap at feed/index.ts:951)
        ORDER BY vector_distance ASC NULLS LAST, popularity_score DESC, distance_m ASC
        LIMIT p_limit )
    The window ORDER BY MUST express the same proxy as the global sort (vector_distance, then popularity_score, then distance_m) using the underlying expressions (not the OUT-param aliases) inside the inner subquery, because `rn` is computed before the column aliases are visible at the `ranked` layer. The outer `ORDER BY vector_distance ASC NULLS LAST, popularity_score DESC, distance_m ASC` keeps the bare column refs that `#variable_conflict use_column` resolves. Everything from the CTE-close `)` through the LATERAL outer SELECT and the final projection stays byte-identical to 169. K=8 must be a literal with the inline rationale comment so the operator can tune it.

    Do NOT add a `supabase db push` or any apply step. Do NOT reverse the migration-167 open-hours fold. Do NOT drop the `#variable_conflict use_column` directive.
  </action>
  <verify>
    <automated>test -f infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -q "hnsw.iterative_scan" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -q "ROW_NUMBER() OVER" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -q "PARTITION BY d.restaurant_id" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -q "#variable_conflict use_column" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -q "r.open_hours" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -q "relaxed_order" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && grep -qE "rn <= 8" infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql && echo "175 forward structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `175_generate_candidates_precap_iterative_scan.sql` exists and is wrapped in `BEGIN;` / `COMMIT;`
    - Contains `hnsw.iterative_scan` set to `relaxed_order`, plus `hnsw.max_scan_tuples` = 20000, `hnsw.scan_mem_multiplier` = 2, `hnsw.ef_search` = 400
    - Contains `ALTER FUNCTION generate_candidates(` with the full 13-arg signature (`FLOAT, FLOAT, FLOAT, vector, ...`)
    - Contains `ROW_NUMBER() OVER (` with `PARTITION BY d.restaurant_id` and the filter `rn <= 8`
    - Still contains `#variable_conflict use_column` and `r.open_hours` (167 fold preserved)
    - Does NOT contain `supabase db push` and does NOT contain `DROP FUNCTION`
    - `grep -c 'supabase_functions.http_request' file` == 0 (wrong pattern absent)
  </acceptance_criteria>
  <done>Migration 175 exists with both the iterative_scan GUC (relaxed_order + 3 bounds) and the K=8 per-restaurant ROW_NUMBER pre-cap inside the materialized CTE, the 167 fold + `#variable_conflict` directive + single round-trip preserved, signature unchanged (CREATE OR REPLACE, no DROP), no apply step.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 175 REVERSE pair</name>
  <read_first>
    - infra/supabase/migrations/169_REVERSE_ONLY_generate_candidates_pushdown.sql (the CREATE OR REPLACE restore-prior-body pattern, no DROP — lines 1-25 for header + signature; this file IS the verbatim body the 175 REVERSE restores to)
    - infra/supabase/migrations/170_REVERSE_ONLY_codify_behavioral_rls.sql (lines 1-22 — the security/rollback-warning header prose model)
    - infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql (the forward file authored in Task 1 — the REVERSE must invert exactly its two changes)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§5(a) — exact REVERSE content: RESET each GUC + restore 169 body)
  </read_first>
  <files>infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql</files>
  <action>
    Author `175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql`, wrapped in `BEGIN; ... COMMIT;`. Header (── prose, every line `--`) mirrors 169_REVERSE + 170_REVERSE warning style: states this restores the migration-169 `generate_candidates` body (current HEAD, NOT 167) and clears the iterative_scan GUCs; plain CREATE OR REPLACE (32-col shape unchanged, no DROP); controlled rollback only — reverts the per-restaurant pre-cap and the filtered-ANN recall fix.
    Body, in order:
    1. RESET the four GUCs (inverse of SET) with the full 13-arg signature:
       ALTER FUNCTION generate_candidates(
         FLOAT, FLOAT, FLOAT, vector, UUID[], TEXT, TEXT[], BOOLEAN, INT, TIME, TEXT, TEXT, BOOLEAN
       )
         RESET hnsw.iterative_scan,
         RESET hnsw.max_scan_tuples,
         RESET hnsw.scan_mem_multiplier,
         RESET hnsw.ef_search;
    2. `CREATE OR REPLACE FUNCTION generate_candidates(...)` with the VERBATIM migration-169 body (no pre-cap window, no `ranked` subquery wrap) — i.e. the exact function from `169_generate_candidates_pushdown.sql`. Copy it faithfully so the REVERSE truly restores HEAD.
    Do NOT add a `supabase db push`. Do NOT use `DROP FUNCTION` (signature is unchanged).
  </action>
  <verify>
    <automated>test -f infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql && grep -q "RESET hnsw.iterative_scan" infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql && grep -q "RESET hnsw.ef_search" infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql && grep -q "CREATE OR REPLACE FUNCTION generate_candidates" infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql && ! grep -q "ROW_NUMBER() OVER" infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql && ! grep -q "DROP FUNCTION" infra/supabase/migrations/175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql && echo "175 REVERSE structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `175_REVERSE_ONLY_generate_candidates_precap_iterative_scan.sql` exists, wrapped in `BEGIN;`/`COMMIT;`
    - Contains `RESET hnsw.iterative_scan`, `RESET hnsw.max_scan_tuples`, `RESET hnsw.scan_mem_multiplier`, `RESET hnsw.ef_search`
    - Contains `CREATE OR REPLACE FUNCTION generate_candidates` (restores 169 body)
    - Does NOT contain `ROW_NUMBER() OVER` (pre-cap removed) and does NOT contain `DROP FUNCTION`
    - Naming follows `NNN_REVERSE_ONLY_<slug>.sql` convention
  </acceptance_criteria>
  <done>The 175 REVERSE exists, RESETs all four GUCs, restores the verbatim 169 function body (no pre-cap), and follows the `NNN_REVERSE_ONLY_*` naming + no-DROP convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → feed edge → generate_candidates RPC | Untrusted request params (lat/lng/radius/filters) cross into the SQL function; this migration changes the function body and its index-scan behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | The pre-cap window ORDER BY proxy vs final JS score | mitigate | K=8 (>2.5× the JS max-3 cap) keeps the proxy-best-K superset of the final top-3/restaurant; Plan 01's `precap-behavior.test.ts` asserts the diversified dish-ID list is identical on old-200 vs K=8-capped input (behavior-preserving gate, SC#3) |
| T-07-02 | Denial of Service | iterative_scan walking unbounded tuples under heavy filters | mitigate | `hnsw.max_scan_tuples = 20000` caps worst-case scan cost; `relaxed_order` + the GUC are scoped to ONLY this function via ALTER FUNCTION (not session-global), so no leak across pooled connections |
| T-07-03 | Tampering | `#variable_conflict use_column` dropped during the CTE edit → silent mis-sort | mitigate | The directive is explicitly preserved (acceptance criterion greps for it); the new window ORDER BY uses underlying expressions in the inner subquery, not OUT-param aliases |
| T-07-04 | Information Disclosure | n/a — no new credential, no new external call, SECURITY DEFINER scope unchanged from 169 | accept | The function keeps 169's `SECURITY DEFINER` + `SET search_path = extensions, public`; no broadened privilege |
</threat_model>

<verification>
- Both 175 files exist with the `NNN_REVERSE_ONLY_*` naming convention; forward has both changes, REVERSE inverts both.
- SQL parse / syntax sanity: the executor SHOULD attempt a parse-only validation (e.g. `pg_query` or a dry-run parse) if any tool is available; otherwise the grep-structure gates above stand in for parse (stage-don't-apply — no apply, no local psql).
- The forward migration's CTE edit is purely additive inside `candidates AS MATERIALIZED`; the LATERAL modifier-JSON block, `reachable_proteins` projection, and 167 open-hours columns are untouched (single round-trip preserved).
- Recall/latency of iterative_scan is OPERATOR-GATED (D-04) — not validated here; the Plan 05 handoff carries the prod-validation step.
- Plan 01's `precap-behavior.test.ts` is the automated behavior-preserving gate for the K=8 pre-cap (SC#3).
</verification>

<success_criteria>
- 2 SQL files authored (forward + REVERSE), grep-structure gates green.
- PERF-01 SC#2 (iterative_scan applied via ALTER FUNCTION) and PERF-02 SC#3 (per-restaurant pre-cap) are satisfied at the authored-migration level.
- No apply step, no `supabase db push`, no `DROP FUNCTION`, no `supabase_functions.http_request`.
- 167 fold + `#variable_conflict` directive + single round-trip preserved.
</success_criteria>

<output>
Create `.planning/phases/07-performance-cache/07-02-SUMMARY.md` when done.
</output>
