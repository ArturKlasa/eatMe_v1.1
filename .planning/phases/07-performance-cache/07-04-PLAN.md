---
phase: 07-performance-cache
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - infra/supabase/migrations/176_invalidate_cache_triggers.sql
  - infra/supabase/migrations/176_REVERSE_ONLY_invalidate_cache_triggers.sql
  - infra/supabase/functions/invalidate-cache/index.ts
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "Tracked triggers fire on restaurants/menus/dishes for INSERT+UPDATE+DELETE, calling invalidate-cache via net.http_post with a Vault-sourced service key (D-09)"
    - "The trigger function reads the service key from vault.decrypted_secrets (secret NAME only in the body), NULL-guards with a WARNING, and never blocks the originating write (fire-and-forget)"
    - "invalidate-cache reads old_record on DELETE so the per-restaurant best-effort key resolves; the unconditional feed:v2:* flush-all is unchanged (D-08)"
    - "invalidate-cache CORS is confirmed wired via buildCorsHeaders (D-10 — confirmation only, no new CORS code)"
  artifacts:
    - path: "infra/supabase/migrations/176_invalidate_cache_triggers.sql"
      provides: "_trg_invalidate_feed_cache() trigger fn (Vault + net.http_post) + 3 AFTER INSERT/UPDATE/DELETE triggers"
      contains: "net.http_post"
    - path: "infra/supabase/migrations/176_REVERSE_ONLY_invalidate_cache_triggers.sql"
      provides: "REVERSE: DROP the 3 triggers then DROP the function"
      contains: "DROP FUNCTION IF EXISTS public._trg_invalidate_feed_cache"
    - path: "infra/supabase/functions/invalidate-cache/index.ts"
      provides: "DELETE-path old_record fallback + D-08 flush-all rationale doc + corrected header"
      contains: "old_record"
  key_links:
    - from: "infra/supabase/migrations/176_invalidate_cache_triggers.sql"
      to: "infra/supabase/functions/invalidate-cache/index.ts"
      via: "net.http_post to the invalidate-cache function URL with a Vault Bearer token"
      pattern: "functions/v1/invalidate-cache"
    - from: "infra/supabase/migrations/176_invalidate_cache_triggers.sql"
      to: "vault.decrypted_secrets"
      via: "reads invalidate_cache_service_key by name (never the JWT literal)"
      pattern: "vault\\.decrypted_secrets"
---

<objective>
Widen feed-cache invalidation to INSERT/UPDATE/DELETE on `restaurants`/`menus`/`dishes` by codifying the dashboard-configured webhook as a tracked migration (D-09, PERF-03 SC#4), using the in-repo `net.http_post` + Vault pattern (migrations 132/135) — NOT `supabase_functions.http_request`. Make `invalidate-cache` read `old_record` on DELETE so the best-effort per-restaurant key resolves (the unconditional `feed:v2:*` flush-all is unchanged — D-08). Confirm (do not re-implement) the CORS lockdown (D-10).

Purpose: the repo becomes the source of truth for cache invalidation (codify-drift, Phase 3 precedent), and DELETE events now bust the cache. The flush-all stays deliberate (restaurant-agnostic key, operator-rare writes, 5-min TTL — documented in writing, SC#4's "never flush-all" reconciled per D-08).
Output: migration 176 (forward + REVERSE) authored + dry-run only, and a minimal edit to `invalidate-cache/index.ts`. NO `supabase db push`.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-cache/07-RESEARCH.md
@.planning/phases/07-performance-cache/07-PATTERNS.md
@infra/supabase/migrations/132_vault_based_trigger_auth.sql
@infra/supabase/migrations/135_record_enrich_dish_triggers.sql
@infra/supabase/functions/invalidate-cache/index.ts
</context>

<tasks>

<!-- planner-discipline-allow: supabase_functions.http_request -->

<task type="auto">
  <name>Task 1: Author migration 176 — cache-invalidation trigger fn + 3 triggers</name>
  <read_first>
    - infra/supabase/migrations/132_vault_based_trigger_auth.sql (THE verbatim pattern — full file. Critical anchors: the per-environment Vault prerequisite prose lines 21-32; `CREATE OR REPLACE FUNCTION ... RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER` lines 36-40; the project-ref URL literal line 43; the Vault lookup `SELECT decrypted_secret INTO v_key FROM vault.decrypted_secrets WHERE name = '...'` lines 46-48; the NULL-guard `IF v_key IS NULL THEN RAISE WARNING ...; RETURN COALESCE(NEW, OLD); END IF;` lines 50-53; the `net.http_post(url, body, headers => Bearer)` call lines 84-91; the BEGIN;/COMMIT; wrapper lines 34/97)
    - infra/supabase/migrations/135_record_enrich_dish_triggers.sql (the idempotent DROP IF EXISTS + CREATE TRIGGER pattern, lines 24-59)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§4 — the full recommended trigger function with TG_OP/TG_TABLE_NAME body, the 3 CREATE TRIGGER statements, the secrets resolution, the double-flush + Vault-missing pitfalls)
    - .planning/phases/07-performance-cache/07-PATTERNS.md (the migration 176 pattern block — verbatim 132/135 skeletons + the 176-specific body)
    - infra/supabase/functions/invalidate-cache/index.ts (lines 1-13 header documenting the webhook body shape `{type, table, schema, record, old_record}` the trigger body must match; line 65 — the `table ∈ {restaurants,menus,dishes}` guard the body's `table` field feeds)
  </read_first>
  <files>infra/supabase/migrations/176_invalidate_cache_triggers.sql</files>
  <action>
    Author `176_invalidate_cache_triggers.sql`, wrapped in `BEGIN; ... COMMIT;`. Header (── prose, every line `--`) MUST: (1) state this codifies the dashboard-configured feed-cache webhook as a tracked trigger and widens coverage to INSERT+UPDATE+DELETE (codify-drift, Phase 3 precedent); (2) copy the migration-132:21-32 per-environment Vault prerequisite prose, renamed to the new secret — the operator runs `SELECT vault.create_secret('<service-role JWT>', 'invalidate_cache_service_key', 'Service-role JWT used by _trg_invalidate_feed_cache')` BEFORE applying, else the trigger logs a WARNING and skips (fail-soft, never blocks the write); (3) WARN that the operator must DISABLE/DELETE the existing untracked dashboard Database Webhook for invalidate-cache at apply time, or both fire (harmless double-flush).

    Trigger function `public._trg_invalidate_feed_cache()` — copy the migration-132 skeleton (RETURNS trigger, LANGUAGE plpgsql, SECURITY DEFINER), adapting the body per RESEARCH §4:
      DECLARE
        v_url TEXT := 'https://tqroqqvxabolydyznewa.supabase.co/functions/v1/invalidate-cache';  -- same project ref as migration 132:43
        v_key TEXT;
      BEGIN
        SELECT decrypted_secret INTO v_key FROM vault.decrypted_secrets WHERE name = 'invalidate_cache_service_key';
        IF v_key IS NULL THEN
          RAISE WARNING 'invalidate_cache_service_key not in vault';
          RETURN COALESCE(NEW, OLD);
        END IF;
        PERFORM net.http_post(
          url     := v_url,
          body    := jsonb_build_object(
                       'type',       TG_OP,
                       'table',      TG_TABLE_NAME,
                       'schema',     TG_TABLE_SCHEMA,
                       'record',     CASE WHEN TG_OP = 'DELETE' THEN NULL ELSE to_jsonb(NEW) END,
                       'old_record', CASE WHEN TG_OP = 'INSERT' THEN NULL ELSE to_jsonb(OLD) END
                     ),
          headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer ' || v_key)
        );
        RETURN COALESCE(NEW, OLD);
      END;
    The body shape (`type/table/schema/record/old_record`) MUST match what `invalidate-cache/index.ts` parses (its header lines 6-13). The JWT is NEVER a literal — only the secret NAME appears.

    Three triggers (idempotent DROP IF EXISTS + CREATE, 135 pattern), AFTER INSERT OR UPDATE OR DELETE, FOR EACH ROW (full-row, no column scoping — feed depends on more than name/description):
      DROP TRIGGER IF EXISTS trg_invalidate_cache_on_restaurant_change ON public.restaurants;
      CREATE TRIGGER trg_invalidate_cache_on_restaurant_change AFTER INSERT OR UPDATE OR DELETE ON public.restaurants FOR EACH ROW EXECUTE FUNCTION public._trg_invalidate_feed_cache();
      DROP TRIGGER IF EXISTS trg_invalidate_cache_on_menu_change ON public.menus;
      CREATE TRIGGER trg_invalidate_cache_on_menu_change AFTER INSERT OR UPDATE OR DELETE ON public.menus FOR EACH ROW EXECUTE FUNCTION public._trg_invalidate_feed_cache();
      DROP TRIGGER IF EXISTS trg_invalidate_cache_on_dish_change ON public.dishes;
      CREATE TRIGGER trg_invalidate_cache_on_dish_change AFTER INSERT OR UPDATE OR DELETE ON public.dishes FOR EACH ROW EXECUTE FUNCTION public._trg_invalidate_feed_cache();

    Do NOT use `supabase_functions.http_request` (not the in-repo pattern). Do NOT hardcode the JWT. Do NOT add a `supabase db push`.
  </action>
  <verify>
    <automated>test -f infra/supabase/migrations/176_invalidate_cache_triggers.sql && grep -q "net.http_post" infra/supabase/migrations/176_invalidate_cache_triggers.sql && grep -q "vault.decrypted_secrets" infra/supabase/migrations/176_invalidate_cache_triggers.sql && grep -q "invalidate_cache_service_key" infra/supabase/migrations/176_invalidate_cache_triggers.sql && grep -q "functions/v1/invalidate-cache" infra/supabase/migrations/176_invalidate_cache_triggers.sql && grep -cE "AFTER INSERT OR UPDATE OR DELETE" infra/supabase/migrations/176_invalidate_cache_triggers.sql | grep -qx 3 && test "$(grep -v '^--' infra/supabase/migrations/176_invalidate_cache_triggers.sql | grep -c 'supabase_functions.http_request')" = 0 && echo "176 forward structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `176_invalidate_cache_triggers.sql` exists, wrapped in `BEGIN;`/`COMMIT;`
    - Contains `net.http_post` AND `vault.decrypted_secrets` AND the secret name `invalidate_cache_service_key`
    - Contains the URL `https://tqroqqvxabolydyznewa.supabase.co/functions/v1/invalidate-cache`
    - Contains exactly 3 `AFTER INSERT OR UPDATE OR DELETE` trigger definitions on restaurants/menus/dishes
    - Contains the NULL-guard `IF v_key IS NULL` + `RAISE WARNING` + `RETURN COALESCE(NEW, OLD)` (fire-and-forget, never blocks write)
    - `grep -v '^--' file | grep -c 'supabase_functions.http_request'` == 0 (wrong pattern absent in executable SQL)
    - Does NOT contain a hardcoded JWT literal (no `eyJ` Bearer string) and no `supabase db push`
    - Header carries the Vault `vault.create_secret(...)` prerequisite + the dashboard-webhook-disable warning
  </acceptance_criteria>
  <done>Migration 176 exists with the Vault-authed `_trg_invalidate_feed_cache()` fn (net.http_post, secret-name-only, NULL-guard fail-soft) + 3 AFTER INSERT/UPDATE/DELETE triggers on restaurants/menus/dishes; uses the in-repo pattern (not supabase_functions.http_request); header carries the Vault + dashboard-disable prerequisites; no apply step.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 176 REVERSE pair</name>
  <read_first>
    - infra/supabase/migrations/135_record_enrich_dish_triggers.sql (the CREATE TRIGGER structure to invert — drop in reverse order)
    - infra/supabase/migrations/170_REVERSE_ONLY_codify_behavioral_rls.sql (lines 1-22 — the rollback-warning header prose model)
    - infra/supabase/migrations/176_invalidate_cache_triggers.sql (the forward file from Task 1 — the REVERSE drops exactly what it created)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§5(b) — exact REVERSE content)
  </read_first>
  <files>infra/supabase/migrations/176_REVERSE_ONLY_invalidate_cache_triggers.sql</files>
  <action>
    Author `176_REVERSE_ONLY_invalidate_cache_triggers.sql`, wrapped in `BEGIN; ... COMMIT;`. Header (── prose, every line `--`) mirrors 170_REVERSE warning style: after this, no migration-tracked feed-cache invalidation fires; if the dashboard webhook was deleted at apply-time, re-create it or feed staleness returns to TTL-only (5 min); controlled rollback only.
    Body — DROP the 3 triggers (reverse order: dishes, menus, restaurants) then DROP the function:
      DROP TRIGGER IF EXISTS trg_invalidate_cache_on_dish_change ON public.dishes;
      DROP TRIGGER IF EXISTS trg_invalidate_cache_on_menu_change ON public.menus;
      DROP TRIGGER IF EXISTS trg_invalidate_cache_on_restaurant_change ON public.restaurants;
      DROP FUNCTION IF EXISTS public._trg_invalidate_feed_cache();
    Do NOT add a `supabase db push`.
  </action>
  <verify>
    <automated>test -f infra/supabase/migrations/176_REVERSE_ONLY_invalidate_cache_triggers.sql && grep -cE "DROP TRIGGER IF EXISTS trg_invalidate_cache_on_" infra/supabase/migrations/176_REVERSE_ONLY_invalidate_cache_triggers.sql | grep -qx 3 && grep -q "DROP FUNCTION IF EXISTS public._trg_invalidate_feed_cache" infra/supabase/migrations/176_REVERSE_ONLY_invalidate_cache_triggers.sql && echo "176 REVERSE structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `176_REVERSE_ONLY_invalidate_cache_triggers.sql` exists, wrapped in `BEGIN;`/`COMMIT;`
    - Contains exactly 3 `DROP TRIGGER IF EXISTS trg_invalidate_cache_on_*` statements
    - Contains `DROP FUNCTION IF EXISTS public._trg_invalidate_feed_cache()`
    - Follows the `NNN_REVERSE_ONLY_<slug>.sql` naming convention
  </acceptance_criteria>
  <done>The 176 REVERSE exists, drops the 3 triggers then the function (correct order), follows the naming + no-apply convention.</done>
</task>

<task type="auto">
  <name>Task 3: invalidate-cache DELETE-path old_record fallback + D-08 doc + D-10 CORS confirmation</name>
  <read_first>
    - infra/supabase/functions/invalidate-cache/index.ts (THE file — full. Critical anchors: header lines 1-13 ("on UPDATE events" — must be corrected to INSERT/UPDATE/DELETE); the CORS import line 17 + `buildCorsHeaders` at line 45 (D-10 confirmation); `const record = body.record ?? {}` at line 54 (the edit point); the unconditional `deleteByPattern(redis, 'feed:v2:*')` at line 76 (D-08 — unchanged); the per-restaurant key block lines 79-100)
    - infra/supabase/functions/invalidate-cache/__tests__/delete-path.test.ts (the Plan 01 harness — the old_record fallback contract this edit must satisfy)
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§4 DELETE path — the two-line fix; D-08 flush-all rationale)
  </read_first>
  <files>infra/supabase/functions/invalidate-cache/index.ts</files>
  <action>
    Three minimal edits, no logic restructure:
    1. DELETE-path fallback (line 54): change `const record: Record<string, any> = body.record ?? {};` to `const record: Record<string, any> = body.record ?? body.old_record ?? {};` so the per-restaurant best-effort lookup (lines 79-100) resolves on DELETE (the trigger sends `record: null, old_record: {...}`). Do NOT add complexity to recover the dishes-DELETE restaurant_id via the live `dishes` lookup (the row is gone — acceptable; the flush-all is the correctness guarantee).
    2. D-08 documentation: the existing comment above `deleteByPattern(redis, 'feed:v2:*')` (lines 72-75) already states the flush-all rationale. Strengthen it to explicitly reconcile SC#4's "never flush-all": note it is a DELIBERATE choice — the cache key `feed:v2:{user}:{geo}:{filters}` is restaurant-agnostic (no restaurant_id to target), writes are operator-rare, the 5-min TTL bounds staleness, and entries recompute lazily. The flush logic itself does NOT change.
    3. Header correction (lines 1-13): change "Invoked by Supabase webhooks on UPDATE events" to reflect INSERT/UPDATE/DELETE now (matching the migration 176 trigger coverage). Keep the webhook body-shape doc accurate (`type` is now INSERT|UPDATE|DELETE; `record` is null on DELETE; `old_record` is null on INSERT).
    4. D-10 CORS confirmation (NO code change): confirm `import { buildCorsHeaders } from '../_shared/cors.ts'` (line 17) and the per-request `const corsHeaders = buildCorsHeaders(...)` (line 45) are present and wired. This is a grep assertion only — do NOT add or modify CORS code.
    Do NOT change `deleteByPattern`, the flush-all call, the table guard (line 65), or the response assembly.
  </action>
  <verify>
    <automated>cd infra/supabase && ~/.deno/bin/deno check functions/invalidate-cache/index.ts && grep -q "body.record ?? body.old_record" functions/invalidate-cache/index.ts && grep -q "buildCorsHeaders" functions/invalidate-cache/index.ts && grep -q "deleteByPattern(redis, 'feed:v2:\*')" functions/invalidate-cache/index.ts && ~/.deno/bin/deno test --node-modules-dir=none -A functions/invalidate-cache/__tests__/delete-path.test.ts && echo "invalidate-cache edits OK"</automated>
  </verify>
  <acceptance_criteria>
    - `invalidate-cache/index.ts` line ~54 reads `body.record ?? body.old_record ?? {}`
    - The `feed:v2:*` flush-all call is unchanged and its comment documents the deliberate flush-all rationale (restaurant-agnostic key, operator-rare writes, 5-min TTL)
    - The file header no longer claims "on UPDATE events" exclusively (reflects INSERT/UPDATE/DELETE)
    - `buildCorsHeaders` is still imported and wired (D-10 confirmation, no new CORS code)
    - `~/.deno/bin/deno check infra/supabase/functions/invalidate-cache/index.ts` exits 0
    - The Plan 01 `delete-path.test.ts` harness passes against the real edit
  </acceptance_criteria>
  <done>invalidate-cache resolves the per-restaurant key from old_record on DELETE; the flush-all is unchanged and its deliberate rationale documented (D-08); the header is corrected; CORS is confirmed wired (D-10); deno check + DELETE-path harness green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres trigger → invalidate-cache edge function | Backend→edge HTTP carrying a service-role credential crosses here |
| menu-write client → DB triggers | INSERT/UPDATE/DELETE on restaurants/menus/dishes fires the trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | Hardcoded service JWT in the migration body | mitigate | Read the JWT from `vault.decrypted_secrets` by name (`invalidate_cache_service_key`); the migration body contains ONLY the secret name — the exact mistake migration 132 fixed. Acceptance criterion greps that no `eyJ` Bearer literal is present |
| T-07-09 | Denial of Service | Unauthenticated cache-flush endpoint abused to evict the feed cache | mitigate | `invalidate-cache` requires the service-role Bearer (sent by the trigger) and is CORS-locked to the allowlist (Phase 2, D-10 confirmed wired); it is webhook-only |
| T-07-10 | Denial of Service / Availability | A trigger failure (e.g. Vault secret missing, pg_net error) blocking the originating menu write | mitigate | Fire-and-forget `net.http_post` (PERFORM, not awaited) + the NULL-key `RAISE WARNING` + `RETURN COALESCE(NEW, OLD)` fallback — the trigger never blocks or rolls back the write (migration 132 precedent) |
| T-07-11 | Spoofing | A forged DELETE body with attacker-controlled old_record | accept | `old_record` only feeds the best-effort legacy `restaurant:*` key delete (a cache eviction, not a data mutation); the table guard (line 65) restricts to known tables; worst case is an extra harmless cache miss. The flush-all is the correctness guarantee regardless |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all deps already pinned in-repo (Phase 4 DEBT-05); RESEARCH "Package Legitimacy Audit" = N/A. No [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Both 176 files exist with `NNN_REVERSE_ONLY_*` naming; forward creates the fn + 3 triggers, REVERSE drops them in order.
- The forward migration uses `net.http_post` + `vault.decrypted_secrets` (NOT `supabase_functions.http_request`) and the secret name only (no JWT literal).
- `invalidate-cache/index.ts` typechecks (`deno check`), reads `old_record` on DELETE, keeps the flush-all unchanged with documented rationale, and the CORS wiring is confirmed.
- The Plan 01 `delete-path.test.ts` harness passes against the real edit.
- The 9-row trigger-catalog assertion (3 tables × 3 events) is OPERATOR-GATED (Plan 05 handoff) — `information_schema.triggers` only reflects truth after apply.
</verification>

<success_criteria>
- PERF-03 SC#4 satisfied at the authored level: trigger coverage widened to INSERT/UPDATE/DELETE on the 3 tables; CORS confirmed locked (D-10); flush-all kept deliberate (D-08).
- No `supabase db push`, no hardcoded JWT, no `supabase_functions.http_request`.
- `deno check` + DELETE-path harness green.
</success_criteria>

<output>
Create `.planning/phases/07-performance-cache/07-04-SUMMARY.md` when done.
</output>
