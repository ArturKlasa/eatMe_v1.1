---
phase: 07-performance-cache
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03]
must_haves:
  truths:
    - "The operator has a single apply-and-verify checklist covering: Vault secret creation, applying 175 + 176 to a prod branch/clone, the trigger-catalog assertion, iterative_scan recall/latency validation, and disabling the old dashboard webhook"
    - "The phase does not claim the schema is live — migrations are authored + dry-run only; apply + validation is the operator's gated action (stage-don't-apply)"
  artifacts:
    - path: ".planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md"
      provides: "The operator apply/validation runbook for migrations 175 + 176"
      contains: "invalidate_cache_service_key"
  key_links:
    - from: ".planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md"
      to: "infra/supabase/migrations/176_invalidate_cache_triggers.sql"
      via: "the runbook's apply step references the authored migrations 175 + 176"
      pattern: "176"
---

<objective>
Close Phase 7 with the operator apply-and-verify handoff (stage-don't-apply): the agent has authored migrations 175 + 176 and the edge changes, but cannot apply them (no local psql, REST-only, operator owns prod deploys). Author the runbook and gate phase completion on the operator's confirmation.

Purpose: the only items that genuinely require prod are operator-gated — the iterative_scan recall/latency validation (D-04), the 9-row trigger-catalog assertion (SC#4), the Vault secret creation, and disabling the untracked dashboard webhook. This plan packages them into one checklist and blocks until the operator confirms.
Output: `07-OPERATOR-HANDOFF.md` + a blocking human checkpoint.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-cache/07-RESEARCH.md
@.planning/phases/07-performance-cache/07-VALIDATION.md
@infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql
@infra/supabase/migrations/176_invalidate_cache_triggers.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the operator apply/validation runbook</name>
  <read_first>
    - .planning/phases/07-performance-cache/07-RESEARCH.md (§1 caveat — iterative_scan only helps personalized requests; §4 — dashboard webhook disable + Vault prerequisite; "Validation Architecture" → SC#4 trigger-catalog assertion SQL; "Open Questions for the Planner" 1-4)
    - .planning/phases/07-performance-cache/07-VALIDATION.md ("Manual-Only Verifications" table — the three operator-gated items)
    - .planning/phases/06-schema-teardown-spine/06-06-PLAN.md or 06-OPERATOR-HANDOFF.md if present (the in-repo operator-handoff format precedent)
    - infra/supabase/migrations/175_generate_candidates_precap_iterative_scan.sql + infra/supabase/migrations/176_invalidate_cache_triggers.sql (the authored migrations the runbook directs the operator to apply)
  </read_first>
  <files>.planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md</files>
  <action>
    Author `07-OPERATOR-HANDOFF.md` — a numbered apply-and-verify checklist for migrations 175 + 176. It MUST contain, in order:
    1. PREREQUISITE — create the Vault secret BEFORE applying 176: `SELECT vault.create_secret('<service-role JWT for this project>', 'invalidate_cache_service_key', 'Service-role JWT used by _trg_invalidate_feed_cache');` (without it, the trigger logs a WARNING and skips — fail-soft).
    2. APPLY — apply migration 175 then 176 to a prod BRANCH/CLONE first (dry-run → sample → full discipline; never straight to prod). List the file paths.
    3. TRIGGER-CATALOG ASSERTION (SC#4) — run on the branch after applying 176: `SELECT event_object_table, action_timing, event_manipulation FROM information_schema.triggers WHERE trigger_name LIKE 'trg_invalidate_cache%' ORDER BY 1,3;` — EXPECT 9 rows (3 tables × INSERT/UPDATE/DELETE, all AFTER). Note: bundle into one jsonb_build_object SELECT if pasting back through the Supabase SQL editor (it returns only the last statement).
    4. ITERATIVE_SCAN RECALL + LATENCY VALIDATION (SC#2, D-04) — compare feed recall + p95 latency before/after migration 175 on representative PERSONALIZED requests (the GUC only helps when `p_preference_vector IS NOT NULL` — note the §1 caveat). Decision: KEEP the GUC or RESET it (run the 175 REVERSE's RESET block). Record the chosen ef_search/max_scan_tuples if tuned from the 400/20000 starting values.
    5. DISABLE THE OLD DASHBOARD WEBHOOK — delete/disable the existing untracked dashboard Database Webhook for invalidate-cache when applying 176, or both fire (harmless double-flush). It lives in the `supabase_functions` schema (invisible to a `public` trigger dump).
    6. POST-APPLY SMOKE — one real menu write (e.g. UPDATE a dish) → confirm a `feed:v2:*` cache flush occurred (feed recomputes on next request).
    Each step is a checkbox. State explicitly at the top: migrations are authored + dry-run only; the agent applied nothing (stage-don't-apply); PERF-01 SC#2 and PERF-03 SC#4 are CONFIRMED only after the operator's clean paste-back.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md && grep -q "invalidate_cache_service_key" .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md && grep -q "information_schema.triggers" .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md && grep -q "trg_invalidate_cache%" .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md && grep -qi "dashboard" .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md && grep -qi "recall" .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md && echo "handoff runbook OK"</automated>
  </verify>
  <acceptance_criteria>
    - `07-OPERATOR-HANDOFF.md` exists and lists, as checkboxes: Vault secret creation, apply 175+176 to a branch/clone, the 9-row trigger-catalog assertion, iterative_scan recall/latency keep-or-RESET decision, dashboard-webhook disable, post-apply smoke
    - Contains the literal trigger-catalog SQL referencing `information_schema.triggers` and `trg_invalidate_cache%` with the expected count of 9
    - Contains the `vault.create_secret(..., 'invalidate_cache_service_key', ...)` prerequisite
    - States migrations are authored + dry-run only; agent applied nothing
  </acceptance_criteria>
  <done>The operator runbook exists with all six gated steps; phase-completion claims are correctly deferred to the operator's apply + paste-back.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator apply-and-verify checkpoint (BLOCKING)</name>
  <action>Pause for the operator. Do NOT apply any migration (stage-don't-apply). Present the runbook at .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md and wait for the operator to apply 175 + 176 on a prod branch/clone, run the trigger-catalog assertion, validate iterative_scan recall/latency, disable the old dashboard webhook, and paste back the results. Resume only after the operator approves.</action>
  <what-built>
    Phase 7 authored (NOT applied): migration 175 (per-restaurant pre-cap K=8 + hnsw.iterative_scan GUCs) + REVERSE; migration 176 (cache-invalidation triggers on restaurants/menus/dishes for INSERT/UPDATE/DELETE via net.http_post + Vault) + REVERSE; the tiered-radius loop in feed/index.ts; the invalidate-cache DELETE-path old_record fallback. All Deno harnesses + SQL-structure gates green. The operator runbook is at .planning/phases/07-performance-cache/07-OPERATOR-HANDOFF.md.
  </what-built>
  <how-to-verify>
    1. Create the Vault secret: `SELECT vault.create_secret('<service-role JWT>', 'invalidate_cache_service_key', 'Service-role JWT used by _trg_invalidate_feed_cache');`
    2. Apply migrations 175 then 176 to a prod branch/clone (dry-run → sample → full). Confirm both parse + apply clean.
    3. Run the trigger-catalog assertion: `SELECT event_object_table, action_timing, event_manipulation FROM information_schema.triggers WHERE trigger_name LIKE 'trg_invalidate_cache%' ORDER BY 1,3;` — expect 9 rows (restaurants/menus/dishes × INSERT/UPDATE/DELETE, all AFTER).
    4. Validate iterative_scan: compare feed recall + p95 latency on personalized requests before/after 175; decide KEEP or RESET (run the 175 REVERSE RESET block if rejecting). Record the decision.
    5. Disable/delete the existing untracked dashboard Database Webhook for invalidate-cache (avoid double-flush).
    6. Smoke: UPDATE a dish → confirm the feed cache flushed (feed recomputes next request).
    Paste back the trigger-catalog result + the iterative_scan keep/reset decision.
  </how-to-verify>
  <resume-signal>Type "approved" with the trigger-catalog row count + iterative_scan decision, or describe issues found during apply.</resume-signal>
</task>

</tasks>

<verification>
- The runbook exists and covers all six operator-gated steps.
- Phase completion is GATED on the operator's blocking-human confirmation (clean apply + 9-row catalog + iterative_scan decision).
- The agent applies nothing (stage-don't-apply) — PERF-01 SC#2 and PERF-03 SC#4 are confirmed only post-paste-back.
</verification>

<success_criteria>
- Operator has a single, complete apply/validation checklist.
- The blocking checkpoint prevents the phase from being marked complete until the operator validates on prod.
- All three requirements (PERF-01/02/03) reach their operator-gated confirmation path.
</success_criteria>

<output>
Create `.planning/phases/07-performance-cache/07-05-SUMMARY.md` when done.
</output>
