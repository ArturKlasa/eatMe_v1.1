---
phase: 09-mobile-map-modal-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/map/DailyFilterModal/index.tsx
  - apps/mobile/src/components/map/DailyFilterModal/sections/PriceSection.tsx
  - apps/mobile/src/components/map/DailyFilterModal/sections/ProteinSection.tsx
  - apps/mobile/src/components/map/DailyFilterModal/sections/CuisineSection.tsx
  - apps/mobile/src/components/map/DailyFilterModal/sections/MealSection.tsx
  - apps/mobile/src/components/map/DailyFilterModal/modals/CuisineSelectionModal.tsx
  - apps/mobile/src/components/map/DailyFilterModal/modals/MealSelectionModal.tsx
  - apps/mobile/src/components/map/DailyFilterModal/DualRangeSlider.tsx
  - apps/mobile/src/components/map/DailyFilterModal/helpers.ts
  - apps/mobile/src/components/map/DailyFilterModal/constants.ts
  - apps/mobile/src/components/map/DailyFilterModal.tsx
autonomous: true
requirements: [RFCT-03]
must_haves:
  truths:
    - "BasicMapScreen and components/map/index.ts still resolve `DailyFilterModal` from the directory barrel (D-08)"
    - "The modal opens seeded from the currently-applied daily filters, edits stay local until Apply, and Apply commits once via replaceDailyFilters (local-draft → Apply preserved, D-01)"
    - "The seed-on-open effect deps stay `[visible]` ONLY — in-progress edits are not clobbered (LANDMINE #2 preserved)"
    - "Selecting Meat pre-selects chicken/beef/pork; deselecting clears all meat sub-types; vegetarian/vegan toggle dietPreference — the parent reducer holds this special-casing verbatim (LANDMINE #3, D-03)"
    - "DualRangeSlider keeps the Android measure() polling (width=0 for ~1s after slide-in) — LANDMINE #4 preserved"
    - "The commented-out Diet Type Tabs block is removed (D-12); no other behavior changes"
    - "mobile typecheck (cd apps/mobile && npx tsc --noEmit) exits 0; old monolith file deleted"
  artifacts:
    - path: "apps/mobile/src/components/map/DailyFilterModal/index.tsx"
      provides: "Parent composition root + barrel; owns localFilters draft + ALL reducers; named export DailyFilterModal"
      contains: "export const DailyFilterModal"
    - path: "apps/mobile/src/components/map/DailyFilterModal/sections/ProteinSection.tsx"
      provides: "Presentational protein+meat section (value+onChange against parent draft); meat-subtype conditional render driven by value prop"
      contains: "ProteinSection"
    - path: "apps/mobile/src/components/map/DailyFilterModal/DualRangeSlider.tsx"
      provides: "Dual-thumb price slider with Android measure() polling (LANDMINE #4)"
      contains: "measure"
    - path: "apps/mobile/src/components/map/DailyFilterModal/modals/CuisineSelectionModal.tsx"
      provides: "Verbatim cuisine selection sub-modal (kept separate from MealSelectionModal, D-09)"
      contains: "CuisineSelectionModal"
    - path: "apps/mobile/src/components/map/DailyFilterModal/constants.ts"
      provides: "ALL_MEALS list moved verbatim (D-09)"
      contains: "ALL_MEALS"
    - path: "apps/mobile/src/components/map/DailyFilterModal/helpers.ts"
      provides: "toLocaleKey helper moved verbatim (D-09)"
      contains: "toLocaleKey"
  key_links:
    - from: "apps/mobile/src/components/map/index.ts"
      to: "apps/mobile/src/components/map/DailyFilterModal/index.tsx"
      via: "export { DailyFilterModal } from './DailyFilterModal' — resolves to directory index.tsx named export"
      pattern: "export \\{ DailyFilterModal"
    - from: "apps/mobile/src/components/map/DailyFilterModal/index.tsx"
      to: "apps/mobile/src/stores/filterStore/index.ts"
      via: "useFilterStore / DailyFilters / defaultDailyFilters from the rebased '../../../stores/filterStore' barrel; commits on Apply via replaceDailyFilters"
      pattern: "replaceDailyFilters"
---

<objective>
Decompose `apps/mobile/src/components/map/DailyFilterModal.tsx` (891 lines) into a co-located directory: a parent `index.tsx` that owns the `localFilters` draft + ALL reducers, one presentational `value`+`onChange` child per filter section, two verbatim selection sub-modals, the DualRangeSlider, and `helpers.ts` / `constants.ts`. A pure, behavior-preserving structural move (RFCT-03). The ONLY deletion permitted is the dead commented-out Diet Type Tabs block (D-12).

Purpose: Shrink the 891-line modal into reviewable units using the Phase 8 directory+barrel doctrine while preserving the local-draft→Apply semantics, the protein/meat special-casing, the [visible]-only seed effect, and the Android slider measure-poll exactly. SC#2 is satisfied in spirit (presentational sections; store is source of truth via Apply) — NOT by per-change store writes (D-02). The operator confirms live behavior on a physical device in 09-03 (SC#4).

Output: `components/map/DailyFilterModal/` directory — `index.tsx` (parent + barrel), `sections/{PriceSection,ProteinSection,CuisineSection,MealSection}.tsx`, `modals/{CuisineSelectionModal,MealSelectionModal}.tsx`, `DualRangeSlider.tsx`, `helpers.ts`, `constants.ts`. The old `DailyFilterModal.tsx` monolith is deleted.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase spec — AUTHORITATIVE. D-01/D-02/D-03/D-07/D-08/D-09/D-10/D-11/D-12 govern this plan.
@.planning/phases/09-mobile-map-modal-refactor/09-CONTEXT.md
@.planning/phases/09-mobile-map-modal-refactor/09-PATTERNS.md

# Phase 8 precedent (directory + barrel, preserve-quirks doctrine)
@.planning/phases/08-mobile-filter-store-refactor/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract leaf modules — helpers, constants, DualRangeSlider (LANDMINE #4), both selection sub-modals</name>
  <files>apps/mobile/src/components/map/DailyFilterModal/helpers.ts, apps/mobile/src/components/map/DailyFilterModal/constants.ts, apps/mobile/src/components/map/DailyFilterModal/DualRangeSlider.tsx, apps/mobile/src/components/map/DailyFilterModal/modals/MealSelectionModal.tsx, apps/mobile/src/components/map/DailyFilterModal/modals/CuisineSelectionModal.tsx</files>
  <read_first>
    - apps/mobile/src/components/map/DailyFilterModal.tsx (the file being decomposed — read in full; lines 38-73 helpers+searchBox, 479-539 ALL_MEALS, 546-633 MealSelectionModal, 640-727 CuisineSelectionModal, 734-890 DualRangeSlider)
    - apps/mobile/src/components/map/MapFooter.tsx (presentational-child shape reference)
    - .planning/phases/09-mobile-map-modal-refactor/09-PATTERNS.md ("DualRangeSlider.tsx — LANDMINE #4", "modals/...", "helpers.ts / constants.ts" sections)
  </read_first>
  <action>
    Create the leaf modules first (no dependence on the parent), so each is an independent unit. New depth is `src/components/map/DailyFilterModal/` (one level deeper than the old `src/components/map/`), so old `../../` relative imports become `../../../`; alias imports (`@/styles`, `@eatme/tokens`, `@eatme/shared`) are unchanged.

    `helpers.ts` — move `toLocaleKey` VERBATIM (lines 38-51), keep the `export` so the modals/sections can import it. Move the shared `searchBox` StyleSheet (lines 54-73) here too as an `export const searchBox` (used by both selection modals; keep shared, NOT duplicated — D-09 planner's-call lands it in helpers.ts). Imports: `react-native` (StyleSheet), `@eatme/tokens` (colors, spacing, typography, borderRadius).

    `constants.ts` — move `ALL_MEALS` VERBATIM (lines 479-539), keep `export const ALL_MEALS: string[]`.

    `DualRangeSlider.tsx` — move the `DualRangeSliderProps` interface (734-742) + the `DualRangeSlider` component (744-890) VERBATIM. LANDMINE #4: KEEP the Android `measure()` polling effect (lines 762-786) byte-for-byte, including its existing comment "Poll measure() until non-zero — some Android devices report width=0 for ~1s after Modal slide-in." Immediately above that `React.useEffect` add this exact anchor line on its own line: `// LANDMINE — do NOT "fix"; see 09-CONTEXT.md D-11.4 (Android measure-poll; width=0 ~1s after Modal slide-in)`. Export the component (`export const DualRangeSlider` or keep as named — the parent imports it). Imports: `react` (React), `react-native` (View, PanResponder), `@/styles` (modals). Adding a comment is NOT a behavior change (D-11-safe).

    `modals/MealSelectionModal.tsx` — move `MealSelectionModalProps` (546-551) + `MealSelectionModal` (553-633) VERBATIM, export it. It imports `ALL_MEALS` from `../constants`, `toLocaleKey` + `searchBox` from `../helpers`, `modals` from `@/styles`, tokens from `@eatme/tokens`, `useTranslation` from `react-i18next`, RN primitives from `react-native`.

    `modals/CuisineSelectionModal.tsx` — move `CuisineSelectionModalProps` (640-645) + `CuisineSelectionModal` (647-727) VERBATIM, export it. It imports `ALL_CUISINES` from `@eatme/shared`, `toLocaleKey` + `searchBox` from `../helpers`, `modals` from `@/styles`, tokens from `@eatme/tokens`, `useTranslation`, RN primitives. KEEP it SEPARATE from MealSelectionModal — merging into one generic modal is a deferred behavior change (D-09).

    Do NOT touch DailyFilterModal.tsx in this task (Task 3 deletes/replaces it).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/components/map/DailyFilterModal/helpers.ts` exists and contains `toLocaleKey` and `searchBox`
    - `apps/mobile/src/components/map/DailyFilterModal/constants.ts` exists and contains `ALL_MEALS`
    - `apps/mobile/src/components/map/DailyFilterModal/DualRangeSlider.tsx` exists and contains `measure` and `PanResponder`
    - `grep -c 'LANDMINE — do NOT' apps/mobile/src/components/map/DailyFilterModal/DualRangeSlider.tsx` is >= 1
    - `apps/mobile/src/components/map/DailyFilterModal/modals/MealSelectionModal.tsx` and `apps/mobile/src/components/map/DailyFilterModal/modals/CuisineSelectionModal.tsx` both exist as separate files (D-09: not merged)
    - `cd apps/mobile && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Five leaf modules created with their code moved verbatim, import paths rebased one level deeper, LANDMINE #4 preserved + guard-commented, both selection modals kept separate; mobile tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the four presentational section children (value+onChange)</name>
  <files>apps/mobile/src/components/map/DailyFilterModal/sections/PriceSection.tsx, apps/mobile/src/components/map/DailyFilterModal/sections/ProteinSection.tsx, apps/mobile/src/components/map/DailyFilterModal/sections/CuisineSection.tsx, apps/mobile/src/components/map/DailyFilterModal/sections/MealSection.tsx</files>
  <read_first>
    - apps/mobile/src/components/map/DailyFilterModal.tsx (lines 152-181 PriceSection, 183-342 Protein+Meat section, 343-384 Cuisine section, 386-440 Meal section)
    - apps/mobile/src/components/map/MapFooter.tsx (the props = data `value` + `onX` callbacks, no store access pattern to mirror)
    - .planning/phases/09-mobile-map-modal-refactor/09-PATTERNS.md ("sections/*.tsx" table + the ProteinSection note about the meat-subtype conditional render driven by the value prop)
  </read_first>
  <action>
    Create four presentational section children under `sections/`. Each renders ONE `<View style={modals.section}>` block lifted from the parent, receiving its slice of `localFilters` as a `value` prop and typed `onChange`/`onToggle` callbacks. NO section touches the store or owns draft state (D-03) — the parent owns ALL reducer logic; sections just render and call the callbacks. Mirror MapFooter's `React.FC<Props>` (or `React.memo`) presentational shape. New depth `sections/` (`src/components/map/DailyFilterModal/sections/`): alias imports (`@/styles`, `@eatme/tokens`, `@eatme/shared`) unchanged; `toLocaleKey` from `../helpers`.

    `PriceSection.tsx` — move the price `<View style={[modals.section, { marginTop: -20 }]}>` block (152-181) VERBATIM. Props: `valueMin`, `valueMax`, `priceMin`, `priceMax`, `priceStep`, `currency`, plus `onValuesChange: (min, max) => void` and `onDragStateChange: (dragging: boolean) => void`. The section renders `<DualRangeSlider .../>` — import it from `../DualRangeSlider`. The price-label formatting (`formatCurrency`, lines 159-167) moves with the block; import `formatCurrency` from `../../../utils/i18nUtils` and `useTranslation` from `react-i18next`. The `onValuesChange`/`onDragStateChange` callbacks are passed down from the parent (which owns the `setLocalFilters` update at old lines 175-178 and the `setSliderDragging` at 178).

    `ProteinSection.tsx` — move the diet/protein `<View style={modals.section}>` block (183-342) VERBATIM **except** the dead commented Diet Type Tabs block at lines 189-212 (DROP it per D-12 — handled in this task; note it in the commit body). LANDMINE #3 lives in the PARENT, NOT here: the section receives `value` props `dietPreference`, `proteinTypes`, `meatTypes` and calls `onToggleProtein(key)` / `onToggleMeat(key)` callbacks. Keep the meat-subtype conditional render `{value.proteinTypes.meat && (<View ...>...meat sub-types...</View>)}` (old line 300) — driven by the `value` prop, NOT by store/draft access. The `isSelected` derivations (lines 225-232 for proteins, 310-311 for meats) move with the JSX since they read only the `value` prop. Props: `dietPreference`, `proteinTypes`, `meatTypes`, `onToggleProtein: (key: string) => void`, `onToggleMeat: (key: string) => void`. `useTranslation` from `react-i18next`.

    `CuisineSection.tsx` — move the cuisine `<View style={modals.section}>` block (343-384) VERBATIM. Renders `POPULAR_CUISINES` (from `@eatme/shared`) with `toLocaleKey` (from `../helpers`). Props: `cuisineTypes: string[]`, `onToggleCuisine: (cuisine: string) => void`, `onOpenAll: () => void` (the "Other" button sets the parent's `cuisineModalVisible` — old line 377). `useTranslation`.

    `MealSection.tsx` — move the meal `<View style={modals.section}>` block (386-440) VERBATIM. Renders the inline 11-item popular meals array (lines 392-404; keep it inline as-is — it is a DIFFERENT list from `ALL_MEALS` and moving it to constants is out of scope) with `toLocaleKey`. Props: `meals: string[]`, `onToggleMeal: (meal: string) => void`, `onOpenAll: () => void` (the "Other" button sets the parent's `mealModalVisible` — old line 433). `useTranslation`.

    Do NOT touch DailyFilterModal.tsx in this task.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist: `sections/PriceSection.tsx`, `sections/ProteinSection.tsx`, `sections/CuisineSection.tsx`, `sections/MealSection.tsx` under `apps/mobile/src/components/map/DailyFilterModal/`
    - `grep -c 'Diet Type Tabs' apps/mobile/src/components/map/DailyFilterModal/sections/ProteinSection.tsx` is 0 (D-12 dead block dropped, not carried over)
    - `grep -c 'proteinTypes.meat' apps/mobile/src/components/map/DailyFilterModal/sections/ProteinSection.tsx` is >= 1 (meat-subtype conditional render preserved, driven by value prop)
    - `grep -rn "useFilterStore\|replaceDailyFilters\|setLocalFilters" apps/mobile/src/components/map/DailyFilterModal/sections/` returns nothing (sections are pure presentational — no store/draft access, D-03)
    - `cd apps/mobile && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Four presentational section children created, each value+onChange-driven with no store/draft access; the dead Diet Type Tabs block dropped (D-12); the meat-subtype conditional render preserved via the value prop; mobile tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Compose parent index.tsx (barrel + reducers, LANDMINES #2/#3) + delete the old monolith</name>
  <files>apps/mobile/src/components/map/DailyFilterModal/index.tsx, apps/mobile/src/components/map/DailyFilterModal.tsx</files>
  <read_first>
    - apps/mobile/src/components/map/DailyFilterModal.tsx (lines 75-101 props/state/seed-effect, 103-150 header+Apply, 152-473 the section wiring + reducers, 445-473 sub-modal wiring)
    - apps/mobile/src/components/map/index.ts (line 7 — consumer that must keep resolving: `export { DailyFilterModal } from './DailyFilterModal'`)
    - apps/mobile/src/screens/BasicMapScreen/index.tsx (the 09-01 sibling import `import { DailyFilterModal } from '../../components/map/DailyFilterModal'`)
    - apps/mobile/src/stores/filterStore/index.ts (the Phase 8 barrel SHAPE)
  </read_first>
  <action>
    Create `apps/mobile/src/components/map/DailyFilterModal/index.tsx` as the parent composition root AND barrel, then delete the old `apps/mobile/src/components/map/DailyFilterModal.tsx`.

    `index.tsx` keeps `export const DailyFilterModal: React.FC<DailyFilterModalProps> = ({ visible, onClose }) => { ... }` (D-08 — `components/map/index.ts:7` and `BasicMapScreen/index.tsx` both import the named `DailyFilterModal` from `./DailyFilterModal` / `../../components/map/DailyFilterModal`, which resolve to this directory's index.tsx). Keep the `DailyFilterModalProps` interface (lines 75-78). The parent OWNS all state and reducers (D-01/D-03):
    - State VERBATIM: `localFilters` draft (line 91, `React.useState<DailyFilters>({ ...currentDaily })`), `cuisineModalVisible` (92), `mealModalVisible` (93), `sliderDragging` (94). Store reads: `replaceDailyFilters` (82), `currentDaily` (84). Settings: `currency` (85), `currencyInfo`/`priceMin`/`priceMax`/`priceStep` (86-87).
    - LANDMINE #2 — the seed-on-open effect (lines 97-101) MUST keep deps `[visible]` ONLY; `currentDaily` deliberately excluded. Move it VERBATIM, keep the existing trailing comment `// intentionally only on open; currentDaily not in deps`, and immediately above the `React.useEffect` add this exact anchor line on its own line: `// LANDMINE — do NOT add currentDaily to deps; clobbers in-progress edits. See 09-CONTEXT.md D-11.2`. Do NOT add `currentDaily` to the deps array.
    - LANDMINE #3 — the protein/meat toggle reducer logic (old lines 238-283 inside the protein `onPress`, plus the meat-subtype toggle at 316-325) lives in the PARENT as named callbacks passed to ProteinSection. Create `onToggleProtein = (key: string) => { ... }` containing the EXACT branch logic from old lines 239-282 (vegetarian → toggle dietPreference vegetarian/all; vegan → toggle dietPreference vegan/all; else → set dietPreference 'all', toggle proteinTypes[key], and when key==='meat' pre-select chicken/beef/pork on select or clear all meat sub-types on deselect — the `...(protein.key === 'meat' && { meatTypes: prev.proteinTypes.meat ? {all false} : {chicken,beef,pork true; lamb,goat,other false} })` block VERBATIM). Create `onToggleMeat = (key: string) => setLocalFilters(prev => ({ ...prev, meatTypes: { ...prev.meatTypes, [key]: !prev.meatTypes[key...] } }))` from old lines 317-324. Immediately above `onToggleProtein` add: `// LANDMINE — protein/meat special-casing; preserve branch logic exactly. See 09-CONTEXT.md D-11.3`.
    - Other reducer callbacks (parent-owned, moved verbatim): price `onValuesChange={(min, max) => setLocalFilters(prev => ({ ...prev, priceRange: { min, max } }))}` (175-177) + `onDragStateChange={setSliderDragging}` (178); cuisine `onToggleCuisine` (355-362) + `onOpenAll={() => setCuisineModalVisible(true)}`; meal `onToggleMeal` (411-418) + `onOpenAll={() => setMealModalVisible(true)}`. The sub-modal toggle callbacks (`onToggleCuisine` 450-457, `onToggleMeal` 465-472) are the SAME shape — reuse the same callbacks for both the section grid and the selection modal.
    - JSX: keep the `<Modal>` + overlay + header + Apply button (103-142) VERBATIM, including the Apply `onPress={() => { replaceDailyFilters(localFilters); onClose(); }}` (127-130). Inside the `<ScrollView>` (144-150) render `<PriceSection .../>`, `<ProteinSection .../>`, `<CuisineSection .../>`, `<MealSection .../>` in place of the four lifted blocks, passing the `value` slices + callbacks. After the ScrollView/overlay close, render `<CuisineSelectionModal visible={cuisineModalVisible} onClose={() => setCuisineModalVisible(false)} selectedCuisines={localFilters.cuisineTypes} onToggleCuisine={onToggleCuisine} />` and `<MealSelectionModal visible={mealModalVisible} onClose={() => setMealModalVisible(false)} selectedMeals={localFilters.meals} onToggleMeal={onToggleMeal} />`.
    - Imports: sections from `./sections/PriceSection` etc.; modals from `./modals/CuisineSelectionModal` and `./modals/MealSelectionModal`. Rebased relative imports (one level deeper, `../../` → `../../../`): `useFilterStore`, `DailyFilters`, `defaultDailyFilters` from `../../../stores/filterStore`; `useSettingsStore` from `../../../stores/settingsStore`; `getCurrencyInfo` from `../../../utils/currencyConfig`; `formatCurrency` from `../../../utils/i18nUtils`. Alias imports unchanged: `modals` from `@/styles`; tokens from `@eatme/tokens`. `react`, `react-native`, `react-i18next` unchanged. Note: `POPULAR_CUISINES`/`ALL_CUISINES`/`toLocaleKey`/`ALL_MEALS`/`DualRangeSlider`/`searchBox` no longer need importing in index.tsx — they live in the children now; remove any now-unused imports.

    Then DELETE `apps/mobile/src/components/map/DailyFilterModal.tsx` (the old monolith). The directory satisfies both consumer imports via index.tsx resolution.

    Commit this RFCT-03 refactor as its OWN commit, SEPARATE from the 09-01 BasicMapScreen commit (SC#3 — one refactor per commit). Note the D-12 Diet Type Tabs deletion in the commit body. Suggested message: `refactor(09-02): decompose DailyFilterModal into sections + sub-modals (RFCT-03); drop dead Diet Type Tabs (D-12)`.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/components/map/DailyFilterModal/index.tsx` exists and contains `export const DailyFilterModal`
    - `test ! -f apps/mobile/src/components/map/DailyFilterModal.tsx` (old monolith deleted)
    - `grep -F "export { DailyFilterModal } from './DailyFilterModal'" apps/mobile/src/components/map/index.ts` still matches (consumer unchanged, resolves to the new directory)
    - `grep -F '}, [visible]); // intentionally only on open; currentDaily not in deps' apps/mobile/src/components/map/DailyFilterModal/index.tsx` matches (LANDMINE #2 deps verbatim)
    - `grep -c 'LANDMINE — do NOT add currentDaily' apps/mobile/src/components/map/DailyFilterModal/index.tsx` is >= 1 AND `grep -c 'LANDMINE — protein/meat' apps/mobile/src/components/map/DailyFilterModal/index.tsx` is >= 1
    - `grep -F 'replaceDailyFilters(localFilters)' apps/mobile/src/components/map/DailyFilterModal/index.tsx` matches (Apply commit preserved, D-01)
    - `grep -c 'Diet Type Tabs' apps/mobile/src/components/map/DailyFilterModal/index.tsx` is 0 (D-12 confirmed dropped across the whole new tree)
    - `cd apps/mobile && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Parent index.tsx owns the draft + all reducers (LANDMINE #2 [visible]-only seed effect + LANDMINE #3 protein/meat special-casing verbatim + guard-commented), composes the four sections + two sub-modals, commits on Apply via replaceDailyFilters; dead Diet Type Tabs block gone; old monolith deleted; both consumers resolve unchanged; mobile tsc green. Committed as a single RFCT-03 refactor commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a pure behavior-preserving structural move of existing mobile UI code. No new inputs, endpoints, auth surfaces, data flows, or dependencies are introduced. The only external touchpoint — `replaceDailyFilters(localFilters)` writing the draft to the in-memory filterStore on Apply — is preserved VERBATIM (same call site, same payload shape). No network, no storage-format change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | DailyFilterModal draft/reducer logic (moved into parent + sections) | accept | No new attack surface — UI code relocated byte-for-byte; the ONLY risk is behavioral regression (e.g. losing cancel-on-close or meat-subtype semantics), not a security regression. Mitigated by `cd apps/mobile && npx tsc --noEmit` + zero-residue grep + the operator on-device smoke (09-03, SC#4). The filterStore commit boundary (Apply → replaceDailyFilters) is unchanged. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — no new dependencies added. No legitimacy gate required. |
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit` exits 0 after each task (root `turbo/pnpm check-types` SKIPS apps/mobile — do NOT use it).
- Zero-residue grep gate (mobile tsconfig has no `noUnusedLocals`): old monolith `apps/mobile/src/components/map/DailyFilterModal.tsx` deleted; `Diet Type Tabs` appears nowhere in the new tree (D-12); sections contain no `useFilterStore`/`replaceDailyFilters`/`setLocalFilters` (D-03 — pure presentational).
- Consumer import paths intact: `components/map/index.ts:7` and the 09-01 `BasicMapScreen/index.tsx` import both resolve `DailyFilterModal` from the directory.
- LANDMINES verbatim + guard-commented: #2 `[visible]`-only seed deps; #3 protein/meat special-casing in the parent reducer; #4 DualRangeSlider Android measure-poll.
- Execution runs on the MAIN working tree, not a worktree.
</verification>

<success_criteria>
- `components/map/DailyFilterModal/` directory exists with `index.tsx` + `sections/{Price,Protein,Cuisine,Meal}Section.tsx` + `modals/{Cuisine,Meal}SelectionModal.tsx` + `DualRangeSlider.tsx` + `helpers.ts` + `constants.ts`.
- Old `DailyFilterModal.tsx` deleted; both consumers resolve unchanged.
- Local-draft → Apply preserved (D-01); sections are pure presentational value+onChange (D-02/D-03); the two selection modals kept separate (D-09).
- LANDMINES #2/#3/#4 verbatim + guard-commented; dead Diet Type Tabs block removed (D-12).
- `cd apps/mobile && npx tsc --noEmit` exits 0.
- Committed as a single RFCT-03 refactor commit, separate from 09-01 (clean bisect, SC#3).
</success_criteria>

<output>
Create `.planning/phases/09-mobile-map-modal-refactor/09-02-SUMMARY.md` when done.
</output>
