---
phase: 09-mobile-map-modal-refactor
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified: []
autonomous: false
requirements: [RFCT-02, RFCT-03]
must_haves:
  truths:
    - "On a physical device, the map camera centers on the user's location"
    - "Dish markers render on the map and tapping a marker opens the correct dish (RestaurantDetail with the right featuredDishId)"
    - "The daily filter modal opens, edits apply on Apply, and reset/cancel-on-close behaves as before"
    - "Navigation / deep-link opens the correct dish"
    - "No regression vs. pre-refactor behavior for camera, markers, feed, rating banner, or filters"
  artifacts:
    - path: ".planning/phases/09-mobile-map-modal-refactor/09-03-SUMMARY.md"
      provides: "Record of the operator's on-device smoke result (SC#4) — the authoritative regression gate"
      contains: "SC#4"
  key_links:
    - from: "operator (physical device)"
      to: "apps/mobile (BasicMapScreen + DailyFilterModal, refactored)"
      via: "manual on-device smoke checklist — the agent cannot run an emulator"
      pattern: "on-device"
---

<objective>
Authoritative regression gate for Phase 9 (SC#4): the operator runs the mobile app on a physical device and confirms the decomposed `BasicMapScreen` and `DailyFilterModal` behave exactly as before the refactor. The agent cannot run an emulator and the project has no automated UI/unit tests for this surface, so this on-device smoke IS the regression authority for both RFCT-02 and RFCT-03.

Purpose: A behavior-preserving refactor is only "done" when behavior is proven preserved. tsc + zero-residue grep (09-01/09-02) prove the move is shape-identical; this plan proves it is behavior-identical on real hardware.

Output: `09-03-SUMMARY.md` recording the operator's pass/fail against the SC#4 checklist.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-map-modal-refactor/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm the refactor is shape-clean before handing to the operator</name>
  <files>.planning/phases/09-mobile-map-modal-refactor/09-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-mobile-map-modal-refactor/09-01-SUMMARY.md (BasicMapScreen refactor result)
    - .planning/phases/09-mobile-map-modal-refactor/09-02-SUMMARY.md (DailyFilterModal refactor result)
  </read_first>
  <action>
    Run the automated gates one final time so the operator tests a known-clean tree. Confirm: (1) mobile typecheck passes; (2) the old monolith files are gone; (3) the new directory index.tsx files exist (consumer import paths resolve); (4) no `useLocationPermission` was introduced and `useUserLocation.ts` is intact; (5) no dead "Diet Type Tabs" residue. Do NOT attempt to launch an emulator or run the app — mobile visual/behavioral testing is done by the operator on a physical device (no emulator in the agent loop). If any check fails, STOP and report which 09-01/09-02 acceptance criterion regressed before handing off.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && test ! -f src/screens/BasicMapScreen.tsx && test ! -f src/components/map/DailyFilterModal.tsx && test -f src/screens/BasicMapScreen/index.tsx && test -f src/components/map/DailyFilterModal/index.tsx && test -f src/hooks/useUserLocation.ts && ! grep -rq "useLocationPermission" src && ! grep -rq "Diet Type Tabs" src/components/map/DailyFilterModal</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0
    - `apps/mobile/src/screens/BasicMapScreen.tsx` and `apps/mobile/src/components/map/DailyFilterModal.tsx` no longer exist
    - `apps/mobile/src/screens/BasicMapScreen/index.tsx` and `apps/mobile/src/components/map/DailyFilterModal/index.tsx` exist
    - `apps/mobile/src/hooks/useUserLocation.ts` exists; `grep -r "useLocationPermission" apps/mobile/src` returns nothing
    - `grep -r "Diet Type Tabs" apps/mobile/src/components/map/DailyFilterModal` returns nothing
  </acceptance_criteria>
  <done>The combined automated gate exits 0 — the tree is shape-clean and ready for the operator's on-device smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator on-device smoke (SC#4) — authoritative regression gate</name>
  <action>Operator runs apps/mobile on a physical device and walks the SC#4 checklist below (camera centers, dish markers render + tap-through to the correct dish, daily filters apply/reset with cancel-on-close intact, deep-link/navigation opens the correct dish, rating banner works). The agent performs no work here — this is a blocking human verification.</action>
  <what-built>
    BasicMapScreen.tsx (581 lines) and DailyFilterModal.tsx (891 lines) were each decomposed into a co-located directory (custom hooks + presentational children for the map screen; a parent + per-section presentational children + verbatim sub-modals for the filter modal). This is a pure behavior-preserving structural move — NO intended UX change. The only deletion was a dead commented-out "Diet Type Tabs" block. tsc passes and the zero-residue grep is clean; live behavior must now be confirmed on a physical device.
  </what-built>
  <how-to-verify>
    Build/run apps/mobile on a physical device (the operator's normal on-device flow — there is no emulator in the agent loop). Walk the SC#4 checklist and confirm each behaves exactly as before the refactor:

    1. MAP CAMERA: Open the map screen. The camera auto-centers on your current location once the map loads and permission is granted. Tap the "My Location" control — the camera re-centers smoothly on your location.
    2. DISH MARKERS: Dish markers render on the map for the ~5 restaurants backing the visible footer cards (not the whole feed). Markers update when you change filters. Tap a dish marker — it opens RestaurantDetail for the CORRECT restaurant with the tapped dish featured/pinned at the top.
    3. FOOTER + PAGING: The footer shows recommended dish cards. "Get more dishes" reveals the next batch (5 at a time). Tapping a footer card opens the correct dish.
    4. DAILY FILTERS — APPLY: Open the daily filter modal (footer filter button). It opens seeded with the currently-applied filters. Adjust the price slider (both thumbs drag correctly — especially on Android, no stuck/zero-width thumb), toggle protein types (selecting "Meat" reveals + pre-selects Chicken/Beef/Pork; deselecting "Meat" hides the sub-types), toggle Vegetarian/Vegan, pick cuisines + meals (including via the "Other" search modals). Press Apply — the feed updates to match and the modal closes.
    5. DAILY FILTERS — CANCEL-ON-CLOSE: Re-open the modal, change some selections, then dismiss WITHOUT pressing Apply (tap outside / back). Re-open — your applied filters are unchanged (the in-progress edits were discarded). This confirms the local-draft → Apply semantics survived.
    6. RATING BANNER: After viewing some restaurants, the rating banner appears at the top; tapping it opens the rating flow; completing a rating submits successfully.
    7. DEEP-LINK / NAVIGATION: Navigating to a dish (marker tap, footer tap, or deep-link) opens the CORRECT dish/restaurant.

    Confirm: no crash, no console errors, no visual or behavioral difference vs. the pre-refactor build for any of the above.
  </how-to-verify>
  <resume-signal>Type "approved" if all SC#4 checks pass on-device, or describe any regression (which check, what differed) so it can be fixed before the phase closes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds NO code — it is a verification-only checkpoint. No new inputs, endpoints, auth surfaces, data flows, or dependencies. The trust boundaries exercised on-device (Supabase feed/rating queries, Expo location permission) are unchanged from the pre-refactor build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | on-device verification of the refactored screens | accept | No new attack surface — verification only, zero code added. The on-device smoke is itself the mitigation for the behavioral-regression risk introduced by 09-01/09-02. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — no new dependencies added. No legitimacy gate required. |
</threat_model>

<verification>
- Pre-flight combined gate exits 0 (tsc green, monoliths deleted, new index.tsx files present, useUserLocation intact, no useLocationPermission, no Diet Type Tabs residue).
- Operator confirms the SC#4 checklist on a physical device (the authoritative regression gate; the agent cannot run an emulator).
- Execution runs on the MAIN working tree, not a worktree.
</verification>

<success_criteria>
- The pre-flight automated gate exits 0.
- The operator approves the SC#4 on-device smoke (camera centers, markers render + tap-through to the correct dish, daily filters apply/reset with cancel-on-close intact, deep-link/navigation opens the correct dish, rating banner works) with no regression.
- RFCT-02 and RFCT-03 are confirmed behavior-preserving and can be marked Complete.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-map-modal-refactor/09-03-SUMMARY.md` when done, recording the operator's SC#4 result.
</output>
