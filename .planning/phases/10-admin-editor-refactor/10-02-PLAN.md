---
phase: 10-admin-editor-refactor
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx
  - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/BundledItemsBlock.tsx
  - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/CategorySection.tsx
  - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/DishCard.tsx
autonomous: true
requirements: [RFCT-04]

must_haves:
  truths:
    - "BundledItemsBlock is its own file, moved verbatim, imported by DishCard"
    - "CategorySection renders the per-group <section> header presentationally (props in, callbacks out), preserving the 'none'-bucket-last render order (L-5)"
    - "DishCard renders the whole per-dish <li> presentationally; the onActiveImageIndexChange wiring (onFocusCapture + onMouseDown firing source_image_index) survives the move"
    - "index.tsx keeps all orchestration (D-06), getGroupMeta, validation, the single submit, L-1 and L-4; it threads value+callbacks down to CategorySection/DishCard"
    - "turbo check-types passes and the full admin vitest suite (incl. buildConfirmPayload + admin-confirm-rpc + useReviewState) stays green"
  artifacts:
    - path: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/BundledItemsBlock.tsx"
      provides: "Verbatim bundled-items presentational block (D-04)"
      contains: "'use client'"
    - path: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/CategorySection.tsx"
      provides: "Per-group <section> header (display name + badge, Merge-into dropdown, Select-all, description textarea)"
      contains: "'use client'"
    - path: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/DishCard.tsx"
      provides: "Whole per-dish <li> (fields grid, category controls, BundledItemsBlock, ModifierGroupsEditor, ScanExtrasPanel, copy-groups button)"
      contains: "onActiveImageIndexChange"
  key_links:
    - from: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/DishCard.tsx"
      to: "apps/admin/src/app/(admin)/menu-scan/[jobId]/AdminJobShell.tsx (SourceImageStrip, parent-owned)"
      via: "onFocusCapture + onMouseDown on the dish <li> fire onActiveImageIndexChange(source_image_index) upward"
      pattern: "onActiveImageIndexChange\\?\\.\\(.*source_image_index"
    - from: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx"
      to: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/CategorySection.tsx"
      via: "groups.map renders <CategorySection> with getGroupMeta results threaded as props"
      pattern: "<CategorySection"
    - from: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/DishCard.tsx"
      to: "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/BundledItemsBlock.tsx"
      via: "DishCard imports and renders BundledItemsBlock"
      pattern: "import.*BundledItemsBlock"
---

<objective>
Extract the three presentational children — `BundledItemsBlock` (verbatim move), `CategorySection` (per-group header), and `DishCard` (whole per-dish `<li>`) — leaving `index.tsx` as the orchestration composition root that threads `value` + typed callbacks down (D-03/D-06). Behavior-preserving: no UX change; the `onActiveImageIndexChange` wiring, the `'none'`-bucket-last render order (L-5), and all collaborators (`ModifierGroupsEditor`, `ScanExtrasPanel`, the three comboboxes) stay wired identically.

Purpose: Completes the RFCT-04 / ROADMAP SC#1 decomposition along real form regions (D-02: `ModifierGroupsEditor` already exists and is reused; there is no image panel in this file — `SourceImageStrip` is parent-owned). Three atomic, independently-revertable commits (SC#3).

Output: `BundledItemsBlock.tsx`, `CategorySection.tsx`, `DishCard.tsx`, and a slimmed `index.tsx`.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-admin-editor-refactor/10-CONTEXT.md
@.planning/phases/10-admin-editor-refactor/10-PATTERNS.md
@apps/admin/src/app/(admin)/menu-scan/[jobId]/AdminJobShell.tsx
@apps/admin/src/components/modifiers/ModifierGroupsEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move BundledItemsBlock to its own file, verbatim (Commit 4)</name>
  <files>apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/BundledItemsBlock.tsx (created), apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx (modified)</files>
  <read_first>
    - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx (lines 1192-1258 — the BundledBlockProps interface + BundledItemsBlock function being moved; line 1127-1133 — its current call site inside the dish <li>)
    - apps/admin/src/app/(admin)/menu-scan/[jobId]/useReviewState.ts (EditableDish type)
    - apps/admin/src/components/modifiers/ModifierGroupsEditor.tsx (lines 1-47 — the presentational-component shape: 'use client' + Props + destructured signature)
    - .planning/phases/10-admin-editor-refactor/10-PATTERNS.md (§ "BundledItemsBlock.tsx (presentational, verbatim move)")
  </read_first>
  <action>
    Create NEW file `apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/BundledItemsBlock.tsx`:
    1. Add `'use client';` at the top (it has event handlers).
    2. Move VERBATIM (byte-for-byte) the `BundledBlockProps` interface (current lines 1192-1198) and the `BundledItemsBlock` function (1200-1258) out of `index.tsx` into this file. Export the function: `export function BundledItemsBlock(...)`.
    3. Import `type EditableDish` from `./useReviewState` (BundledBlockProps references it).
    4. In `index.tsx`: delete the moved interface + function; add `import { BundledItemsBlock } from './BundledItemsBlock';`. The call site inside the dish `<li>` (current lines 1127-1133: `<BundledItemsBlock dish={d} saving={saving} onAdd={...} onRemove={...} onUpdate={...} />`) stays EXACTLY as-is for now (it moves into DishCard in Task 3).
    No logic change, no prop change. This is a pure relocation.
    Commit message: `refactor(10-02): move BundledItemsBlock to its own file (verbatim)`.
  </action>
  <acceptance_criteria>
    - File `BundledItemsBlock.tsx` exists, starts with `'use client'`, and contains `export function BundledItemsBlock`.
    - BundledItemsBlock.tsx contains the verbatim copy text `Bundled items (` and `+ Add item` (proves the JSX moved intact).
    - index.tsx no longer defines `function BundledItemsBlock` locally: `grep -c "^function BundledItemsBlock" index.tsx` is 0.
    - index.tsx imports `BundledItemsBlock` from `./BundledItemsBlock`.
    - `turbo check-types` exits 0; `cd apps/admin && npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && D="apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor" && grep -q "export function BundledItemsBlock" "$D/BundledItemsBlock.tsx" && grep -q "from './BundledItemsBlock'" "$D/index.tsx" && ! grep -q "^function BundledItemsBlock" "$D/index.tsx" && turbo check-types && (cd apps/admin && npx vitest run)</automated>
  </verify>
  <done>BundledItemsBlock lives in its own 'use client' file (verbatim), index.tsx imports it, the call site is unchanged, check-types and the admin suite are green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract CategorySection.tsx — the per-group header (Commit 5)</name>
  <files>apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/CategorySection.tsx (created), apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx (modified)</files>
  <read_first>
    - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx (lines 730-815 — the per-group header JSX being extracted; lines 427-446 — the groups memo with L-5 'none'-last ordering; lines 448-477 — getGroupMeta which STAYS in index.tsx; lines 479-486 — updateGroupDescription)
    - apps/admin/src/components/modifiers/ModifierGroupsEditor.tsx (lines 7-47 — Props-in/callbacks-out presentational shape to mirror)
    - .planning/phases/10-admin-editor-refactor/10-PATTERNS.md (§ "CategorySection.tsx (presentational)" — L-5 note)
  </read_first>
  <action>
    Create NEW file `apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/CategorySection.tsx` with `'use client';`:
    1. Move the per-group `<section>` HEADER block (current lines 740-815: the `<header>` with display name + badge, the "Merge into…" `<select>` 756-780, the Select-all/Deselect-all button 781-791, and the locked/editable description textarea 794-814). KEEP the surrounding `<section key=...>` wrapper and the `<ul>` of dishes structure: `CategorySection` renders the `<section>` + `<header>` and receives its dish list as `children` (the `<ul>...DishCard...</ul>` is passed in by index.tsx so render structure stays identical). Render exactly: `<section className=...><header>...</header>{children}</section>`.
    2. Props (presentational — data slice + callbacks, NO orchestration state, NO getGroupMeta call inside): `meta` (the precomputed `getGroupMeta(group.key)` result: `{ displayName, descriptionLocked, badge }`), `groupKey: string`, `dishCount: number` (active dish count for the "N dishes" label) and the raw `dishes` if needed for the count expressions — pass whatever the header's count/select-all expressions read; `mergeTargets` as an array of `{ key, label }` already computed in index.tsx (index.tsx calls `getGroupMeta` per target and builds the `m.badge ? `${m.displayName} · ${m.badge}` : m.displayName` label, since getGroupMeta is impure — D-05); `hasActiveDishes: boolean`; `allSelected: boolean` (the precomputed Select-all/Deselect-all state); `saving: boolean`; `sourceLanguage: string`; `description: string` (the `categoryDescriptions.get(group.key) ?? ''` value); and callbacks `onMergeGroup(targetKey: string)`, `onToggleGroupSelection()`, `onUpdateGroupDescription(value: string)`. Children: the dish `<ul>`.
    3. The "Merge into…" `<select>` onChange calls `onMergeGroup(e.target.value)` (index.tsx binds it to `handleMergeGroup(group.key, value)`). The Select-all button onClick calls `onToggleGroupSelection()` (index.tsx binds `toggleGroupSelection(group.dishes)`). The textarea onChange calls `onUpdateGroupDescription(e.target.value)` (index.tsx binds `updateGroupDescription(group.key, value)`). The locked-vs-editable branch (`meta.descriptionLocked ? <p>...</p> : <textarea>...`) and the `group.key !== 'none'` guard around it move VERBATIM.
    4. In `index.tsx`: keep `getGroupMeta` (L-5 groups memo, getGroupMeta, updateGroupDescription, handleMergeGroup, toggleGroupSelection all STAY). In the `groups.map(group => {...})` body, compute `meta`, `mergeTargets` (with labels), `hasActiveDishes`, `allSelected`, `desc` as today, then render `<CategorySection meta={meta} groupKey={group.key} ... onMergeGroup={v => handleMergeGroup(group.key, v)} onToggleGroupSelection={() => toggleGroupSelection(group.dishes)} onUpdateGroupDescription={v => updateGroupDescription(group.key, v)}>{<ul>...the dish list...</ul>}</CategorySection>`. The dish `<ul>` still maps `group.dishes` to the inline `<li>` for now (DishCard extraction is Task 3).
    LANDMINE L-5: the `groups` memo (lines 427-446) that splices the `'none'` bucket to the END stays in index.tsx unchanged. Add `// LANDMINE L-5 — 'none' bucket pushed last; preserves render order; do NOT reorder; see 10-CONTEXT.md` on the line immediately above the `const noneIdx = order.indexOf('none');` line. The `groups.map` rendering order MUST remain driven by that memo.
    Commit message: `refactor(10-02): extract CategorySection (per-group header)`.
  </action>
  <acceptance_criteria>
    - File `CategorySection.tsx` exists, starts with `'use client'`, exports `CategorySection`, and renders `<section` + `<header`.
    - CategorySection.tsx contains the verbatim header strings `Merge into…` and (the locked-description note) `existing description — edit on the restaurant page`.
    - CategorySection.tsx does NOT call `getGroupMeta` (it receives `meta` as a prop): `grep -c "getGroupMeta" CategorySection.tsx` is 0.
    - index.tsx renders `<CategorySection` and STILL contains `function getGroupMeta` and the L-5 memo with the marker `LANDMINE L-5`.
    - index.tsx groups memo still contains `order.indexOf('none')` and `order.push('none')` (L-5 ordering preserved).
    - `turbo check-types` exits 0; `cd apps/admin && npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && D="apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor" && grep -q "export function CategorySection" "$D/CategorySection.tsx" && ! grep -q "getGroupMeta" "$D/CategorySection.tsx" && grep -q "<CategorySection" "$D/index.tsx" && grep -q "function getGroupMeta" "$D/index.tsx" && grep -q "LANDMINE L-5" "$D/index.tsx" && grep -q "order.push('none')" "$D/index.tsx" && turbo check-types && (cd apps/admin && npx vitest run)</automated>
  </verify>
  <done>CategorySection is a presentational header (props in, callbacks out, no getGroupMeta), index.tsx threads precomputed meta/mergeTargets/state down and keeps getGroupMeta + the L-5-ordered groups memo with a guard marker; check-types and the admin suite are green.</done>
</task>

<task type="auto">
  <name>Task 3: Extract DishCard.tsx — the whole per-dish &lt;li&gt; (Commit 6)</name>
  <files>apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/DishCard.tsx (created), apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx (modified)</files>
  <read_first>
    - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx (lines 818-1182 — the entire per-dish <li> being extracted; lines 821-822 — the onActiveImageIndexChange wiring that MUST survive; the import block for the symbols the <li> uses)
    - apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/reviewHelpers.ts (encodeCategoryValue, decodeCategoryValue, confidenceTone — DishCard imports these)
    - apps/admin/src/app/(admin)/menu-scan/[jobId]/AdminJobShell.tsx (lines 13, 288, 450, 457-468 — the onActiveImageIndexChange callback wired to the parent-owned SourceImageStrip; confirms DishCard must keep firing source_image_index upward)
    - apps/admin/src/components/modifiers/ModifierGroupsEditor.tsx (the presentational Props shape)
    - .planning/phases/10-admin-editor-refactor/10-PATTERNS.md (§ "DishCard.tsx (presentational)" — the Integration-point callout + the relocated imports list)
  </read_first>
  <action>
    Create NEW file `apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/DishCard.tsx` with `'use client';`:
    1. Move the entire per-dish `<li>` JSX (current lines 818-1182) into a `export function DishCard(props: Props)` returning the `<li key={d._id} ...>...</li>`. Inside DishCard the dish is `props.dish` (rename the loop var `d` to `dish` via a `const d = props.dish;` alias so the moved JSX stays byte-identical and you don't have to rewrite every `d.`). Move intact: the checkbox + name input + dining-format badge + confidence badge + page badge + Remove/Restore button (828-875), the description textarea (877-885), the price/portion/price-label/protein/dining-format grid (887-1034), the Menu-category control (`MenuCategoryCombobox` + custom-name input, 1037-1067), the Dish-category control (`DishCategoryCombobox` + `DishCategoryCreateInline` + the unmatched/resolved hints, 1069-1124), the `BundledItemsBlock` call (1127-1133), the `ModifierGroupsEditor` + per-dish `ScanExtrasPanel` + copy-groups button block (1136-1181).
    2. Relocate these imports into DishCard.tsx (they leave index.tsx if no longer used there — check before deleting from index): from `@eatme/shared` — `PRIMARY_PROTEINS`, `DINING_FORMATS`, `DINING_FORMAT_META`, `type DiningFormat`; from `@/lib/priceWarnings` — `isSuspiciouslyHighPrice`, `priceWarnMessage`; from `@/components/MenuCategoryCombobox` — `MenuCategoryCombobox`, `type MenuCategoryOption`; from `@/components/DishCategoryCombobox` — `DishCategoryCombobox`; from `@/components/DishCategoryCreateInline` — `DishCategoryCreateInline`; from `@/components/modifiers/ModifierGroupsEditor` — `ModifierGroupsEditor`; `./ScanExtrasPanel` — `ScanExtrasPanel`; `./BundledItemsBlock` — `BundledItemsBlock`; from `./reviewHelpers` — `encodeCategoryValue`, `decodeCategoryValue`, `confidenceTone`; from `./useReviewState` — `type EditableDish`, `type PricePrefix`, `type Protein`.
    3. Props (presentational data slice + callbacks the parent owns — DishCard owns NO store/orchestration state): `dish: EditableDish`, `saving: boolean`, `currencyCode: string`, `currencySymbol: string`, `sourceLanguage: string`, `menuCategoryOptions: MenuCategoryOption[]`, `dishCategories: DishCategoryOption[]`, `dishCategoryById: Map<string, DishCategoryOption>`, `selected: boolean` (is this dish in selectedIds), `selectedActiveIds: string[]` (for the copy-button count + the "show copy button" predicate), `scanTargetForThisDish: boolean` (whether `scanTarget?.kind === 'dish' && scanTarget.dishId === dish._id`), and callbacks: `onUpdate(patch: Partial<EditableDish>)` (-> `update(d._id, patch)`), `onToggleDelete()`, `onToggleSelected()`, `onActiveImageIndexChange?: (index: number) => void`, `onScanFromImage()`, `onScannedExtrasAttach(scannedGroups, scannedItems)` (binds to `handleScannedExtrasAttach([dish._id], ...)`), `onScanClose()`, `onCopyGroups()`, `onDishCategoryCreated(cat: DishCategoryOption)`, plus the modifier-group handlers threaded straight to `ModifierGroupsEditor` (`onAddGroup`, `onRemoveGroup`, `onMoveGroup`, `onUpdateGroup`, `onAddOption`, `onRemoveOption`, `onMoveOption`, `onUpdateOption`) and the bundled-item handlers (`onAddBundledItem`, `onRemoveBundledItem`, `onUpdateBundledItem`).

    INTEGRATION POINT — MUST survive (CONTEXT #4): the `<li>`'s `onFocusCapture={() => onActiveImageIndexChange?.(d.source_image_index)}` and `onMouseDown={() => onActiveImageIndexChange?.(d.source_image_index)}` (current lines 821-822) move into DishCard and fire `props.onActiveImageIndexChange?.(dish.source_image_index)` exactly as today — both handlers, on the `<li>`. This is what syncs the parent-owned `SourceImageStrip` in AdminJobShell. Do NOT drop either handler. Do NOT move/recreate `SourceImageStrip` (it lives in the parent; there is no DishImagePanel here — D-02).

    4. In `index.tsx`: the dish `<ul>` inside `CategorySection`'s children now maps `group.dishes` to `<DishCard dish={d} ... />`, binding every callback to the orchestration owner (e.g. `onUpdate={patch => update(d._id, patch)}`, `onToggleDelete={() => toggleDelete(d._id)}`, `onToggleSelected={() => toggleSelected(d._id)}`, `onActiveImageIndexChange={onActiveImageIndexChange}`, `onScanFromImage={() => setScanTarget({ kind: 'dish', dishId: d._id })}`, `scanTargetForThisDish={scanTarget?.kind === 'dish' && scanTarget.dishId === d._id}`, `onScannedExtrasAttach={(g, i) => handleScannedExtrasAttach([d._id], g, i)}`, `onScanClose={() => setScanTarget(null)}`, `onCopyGroups={() => handleCopyGroups(d._id)}`, `onDishCategoryCreated={cat => handleDishCategoryCreated(d._id, cat)}`, `selected={selectedIds.has(d._id)}`, `selectedActiveIds={selectedActiveIds}`, and the modifier/bundled handlers each closing over `d._id`). index.tsx KEEPS all orchestration (D-06): useReviewState call, selectedIds, scanTarget, categoryDescriptions, getGroupMeta, the L-1 useMemo, the L-4 lazy initializer, handleSave. Remove the now-unused `confidenceTone`/`encodeCategoryValue`/`decodeCategoryValue` imports from index.tsx ONLY if no longer referenced there.

    LANDMINE L-1 (index.tsx, the `useReviewState(useMemo(() => initialDishes.map((d, i) => asEditable(...)), []))` with empty deps + `eslint-disable react-hooks/exhaustive-deps`): UNCHANGED — keep empty `[]` deps + the disable comment + the "initial only — recomputing on prop change would clobber edits" comment. Do NOT add `initialDishes`. Add `// LANDMINE L-1 — empty deps intentional; do NOT add initialDishes; see 10-CONTEXT.md` on the line above the existing comment.
    LANDMINE L-4 (index.tsx, the `categoryDescriptions` lazy `useState(() => {...})` initializer, lines 313-333): UNCHANGED — keep the lazy `() => {...}` form. Add `// LANDMINE L-4 — lazy one-time seed; do NOT convert to initial-value + effect; see 10-CONTEXT.md` on the line above the `useState`.
    Commit message: `refactor(10-02): extract DishCard (whole per-dish li)`.
  </action>
  <acceptance_criteria>
    - File `DishCard.tsx` exists, starts with `'use client'`, exports `DishCard`, and returns a `<li`.
    - DishCard.tsx wires BOTH `onFocusCapture` and `onMouseDown` firing `onActiveImageIndexChange?.(` with `source_image_index`: `grep -c "onActiveImageIndexChange?.(.*source_image_index" DishCard.tsx` is >= 2 (or both handlers present — confirm onFocusCapture and onMouseDown each call it).
    - DishCard.tsx imports `ModifierGroupsEditor`, `ScanExtrasPanel`, `BundledItemsBlock`, and `encodeCategoryValue`/`decodeCategoryValue` from `./reviewHelpers`.
    - DishCard.tsx does NOT call `useReviewState`, `useState`, or reference `selectedIds`/`categoryDescriptions`/`scanTarget` directly (presentational only): `grep -c "useReviewState\|categoryDescriptions\|setScanTarget" DishCard.tsx` is 0.
    - index.tsx renders `<DishCard` and STILL contains the L-1 marker `LANDMINE L-1`, the L-4 marker `LANDMINE L-4`, the empty-deps eslint-disable `eslint-disable-next-line react-hooks/exhaustive-deps`, and the lazy initializer `useState<Map<string, string>>(() =>`.
    - `turbo check-types` exits 0; `cd apps/admin && npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && D="apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor" && grep -q "export function DishCard" "$D/DishCard.tsx" && grep -q "onFocusCapture" "$D/DishCard.tsx" && grep -q "onMouseDown" "$D/DishCard.tsx" && grep -q "onActiveImageIndexChange" "$D/DishCard.tsx" && ! grep -q "useReviewState" "$D/DishCard.tsx" && grep -q "<DishCard" "$D/index.tsx" && grep -q "LANDMINE L-1" "$D/index.tsx" && grep -q "LANDMINE L-4" "$D/index.tsx" && grep -q "react-hooks/exhaustive-deps" "$D/index.tsx" && turbo check-types && (cd apps/admin && npx vitest run)</automated>
  </verify>
  <done>DishCard renders the whole per-dish <li> presentationally with the onActiveImageIndexChange (onFocusCapture + onMouseDown) wiring intact; index.tsx threads value+callbacks down and keeps all orchestration incl. L-1 and L-4 with guard markers; SourceImageStrip is untouched (parent-owned); check-types and the admin suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> server action (`adminConfirmMenuScan`) | Unchanged from Plan 01. The confirm payload still crosses here; this plan only moves UI rendering into presentational children — no payload-shaping logic moves. |

This is a behavior-preserving extraction of presentational JSX. No new inputs, no changed auth/RLS boundary, no new network calls. The `onActiveImageIndexChange` callback is a UI-sync side channel (image preview index) with no security relevance; it is preserved verbatim, not changed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | DishCard / CategorySection prop wiring | mitigate | All payload-shaping stays in index.tsx + buildConfirmPayload (Plan 01); children are pure presentational props-in/callbacks-out. Payload byte-identity is guarded by the Plan 01 snapshot test + turbo check-types + the Plan 03 operator save. |
| T-10-05 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure JSX relocation; no dependency surface change. |
| T-10-06 | Information Disclosure | onActiveImageIndexChange UI-sync channel | accept | Unchanged. Fires only a non-sensitive source-image index upward to the parent-owned preview; no auth/data boundary affected. |

No high-severity threats introduced. The regression guard is the snapshot test + check-types + the operator browser save (Plan 03), not new mitigations.
</threat_model>

<verification>
- `turbo check-types` exits 0 after each task.
- `cd apps/admin && npx vitest run` exits 0 — `buildConfirmPayload.test.ts`, `admin-confirm-rpc.test.ts`, `useReviewState.test.ts` all green.
- Zero-residue consumer grep unchanged: `grep -rn "from './ReviewDishEditor'" apps/admin/src` returns only `AdminJobShell.tsx:13`.
- The `onActiveImageIndexChange` (onFocusCapture + onMouseDown) wiring is present in DishCard.tsx; `SourceImageStrip` in AdminJobShell is untouched.
- L-1 (empty-deps useMemo + eslint-disable), L-4 (lazy useState initializer), L-5 ('none'-bucket-last memo) all remain in index.tsx with guard markers.
- Each task is one atomic, independently-revertable commit (SC#3).
</verification>

<success_criteria>
- `BundledItemsBlock`, `CategorySection`, `DishCard` are presentational children (props in, typed callbacks out, no orchestration state); `ModifierGroupsEditor`/`ScanExtrasPanel`/the three comboboxes stay wired unchanged.
- The `onActiveImageIndexChange` wiring survives the move into DishCard; `SourceImageStrip` (parent-owned) is not moved or recreated.
- index.tsx is the orchestration root (D-06) keeping getGroupMeta, validation, the single submit, L-1, L-4, L-5.
- `turbo check-types` + the full admin vitest suite are green.
</success_criteria>

<output>
Create `.planning/phases/10-admin-editor-refactor/10-02-SUMMARY.md` when done.
</output>
