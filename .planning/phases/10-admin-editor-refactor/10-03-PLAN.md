---
phase: 10-admin-editor-refactor
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified: []
autonomous: false
requirements: [RFCT-04]

must_haves:
  truths:
    - "An operator loads a real needs_review menu-scan job in the admin portal, edits it, hits Save, and the job lands as completed with dishes/groups persisted"
    - "The operator confirms no visual or behavioral difference vs before the refactor (the decomposed editor behaves identically)"
    - "turbo check-types and the full admin vitest suite (incl. buildConfirmPayload snapshot, admin-confirm-rpc, useReviewState) are green at the head of the phase"
    - "The zero-residue consumer grep shows AdminJobShell.tsx:13 is the only importer and it resolves to the new directory index.tsx"
  artifacts: []
  key_links:
    - from: "operator browser (admin portal port 3001)"
      to: "admin_confirm_menu_scan RPC"
      via: "Save button on a real needs_review job -> single adminConfirmMenuScan submit -> job completed"
      pattern: "adminConfirmMenuScan"
---

<objective>
Close RFCT-04 with the authoritative end-to-end regression gate: an operator in-browser Save on a real `needs_review` menu-scan job (D-11). Admin is a browser surface (port 3001, no emulator constraint), so the operator confirms the decomposed `ReviewDishEditor` produces an identical, persisting Save. The automated gates (turbo check-types + the buildConfirmPayload snapshot + admin-confirm-rpc + useReviewState) prove shape stability; this plan proves the live submit still lands.

Purpose: Per CONTEXT D-09/D-10/D-11 — the named `admin-confirm-rpc.test.ts` does NOT import the component, and the snapshot test proves payload shape but not the live browser path. The operator save is the real behavior-preservation proof, mirroring the Phase 8/9 on-device gates.

Output: An operator confirmation recorded in the SUMMARY; RFCT-04 marked complete only after the operator approves.
</objective>

<execution_context>
@/home/art/Documents/eatMe_v1/.claude/gsd-core/workflows/execute-plan.md
@/home/art/Documents/eatMe_v1/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-admin-editor-refactor/10-CONTEXT.md
@apps/admin/src/app/(admin)/menu-scan/[jobId]/AdminJobShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the full automated gate at the phase head</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/10-admin-editor-refactor/10-01-SUMMARY.md (what Plan 01 changed)
    - .planning/phases/10-admin-editor-refactor/10-02-SUMMARY.md (what Plan 02 changed)
    - .planning/phases/10-admin-editor-refactor/10-PATTERNS.md (§ "Verification Gate")
  </read_first>
  <action>
    Run the complete automated regression gate once more on the fully decomposed tree (all six commits landed) to confirm nothing drifted across plans:
    1. `turbo check-types` (admin is covered).
    2. `cd apps/admin && npx vitest run` — confirm `buildConfirmPayload.test.ts`, `admin-confirm-rpc.test.ts`, `useReviewState.test.ts` are all green.
    3. Zero-residue consumer grep: `grep -rn "from './ReviewDishEditor'" apps/admin/src` — must return ONLY `AdminJobShell.tsx:13`, resolving to `ReviewDishEditor/index.tsx`.
    4. Confirm the barrel still re-exports both symbols: `grep -n "export type { ExtractedDish } from './useReviewState'" "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx"` and `grep -n "export function ReviewDishEditor" "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx"`.
    If any check fails, STOP — this is a regression introduced by the cross-plan interaction; do not proceed to the operator checkpoint until green.
  </action>
  <acceptance_criteria>
    - `turbo check-types` exits 0.
    - `cd apps/admin && npx vitest run` exits 0 with all three named tests green.
    - `grep -rn "from './ReviewDishEditor'" apps/admin/src` returns exactly one line (AdminJobShell.tsx:13).
    - The index.tsx barrel exports both `ReviewDishEditor` and `type ExtractedDish`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/art/Documents/eatMe_v1 && turbo check-types && (cd apps/admin && npx vitest run) && grep -q "export type { ExtractedDish } from './useReviewState'" "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx" && grep -q "export function ReviewDishEditor" "apps/admin/src/app/(admin)/menu-scan/[jobId]/ReviewDishEditor/index.tsx"</automated>
  </verify>
  <done>The full automated gate is green on the fully decomposed tree; the barrel re-exports both symbols; AdminJobShell's import resolves to the new index.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator in-browser Save on a real needs_review job (D-11)</name>
  <files>(none — verification only)</files>
  <action>
    Operator-only end-to-end gate. The agent cannot run a real browser Save against prod; it pauses here
    and hands the operator the verification steps below. On "approved", record the result in the SUMMARY and
    mark RFCT-04 complete. If the operator reports any difference or save failure, treat it as a regression
    and route back to fix before closing the phase.
  </action>
  <what-built>
    `ReviewDishEditor.tsx` (1258 lines) was decomposed into a `ReviewDishEditor/` directory:
    `index.tsx` (composition root + barrel), `reviewHelpers.ts` + `buildConfirmPayload.ts` (pure modules),
    and `BundledItemsBlock.tsx` / `CategorySection.tsx` / `DishCard.tsx` (presentational children).
    Behavior-preserving: the `admin_confirm_menu_scan` payload is byte-identical (locked by the new
    snapshot test), the single submit path is unchanged, and all five landmines (L-1..L-5) are preserved
    verbatim. This checkpoint is the authoritative end-to-end regression gate (D-11) — the agent cannot
    run a real browser Save against prod.
  </what-built>
  <how-to-verify>
    1. Start the admin portal: `cd apps/admin && pnpm dev` (port 3001), or use the running instance.
    2. Open a real menu-scan job in `needs_review` status: navigate to `/menu-scan/<jobId>` (pick a recent
       needs_review job — the dashboard or the menu-scan list links to it).
    3. Confirm the editor renders identically to before: the per-category sections (with the "(no category)"
       bucket LAST), each dish card's fields (name, description, price + currency symbol, portion, price
       label, primary protein, dining-format badge, confidence %, page badge), the menu-category + dish-category
       controls, bundled items, and the modifier-groups editor.
    4. Make at least one edit (e.g. change a dish name, toggle a category, add/edit a modifier group, edit a
       section description) and confirm the source-image preview on the side still syncs when you focus/click a
       dish card (proves `onActiveImageIndexChange` survived).
    5. Click Save. Confirm: no error banner; the job transitions to `completed`; and the dishes/modifier
       groups/bundled items persist (re-open the restaurant or the job to verify the saved data).
    6. Report any visual or behavioral difference vs the pre-refactor editor — there should be NONE.
  </how-to-verify>
  <resume-signal>Type "approved" if Save lands and the editor behaves identically, or describe any visual/behavioral difference or save failure.</resume-signal>
  <verify>
    <human-check>Operator confirms a real needs_review job Save lands (job -> completed, data persisted) with no visual/behavioral difference vs the pre-refactor editor.</human-check>
  </verify>
  <done>Operator approved: live in-browser Save lands and the decomposed editor behaves identically. RFCT-04 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> `admin_confirm_menu_scan` RPC | The real Save crosses here. This plan runs no code change — it verifies the refactored editor's live submit lands. Authenticated operator-only surface (admin portal, port 3001). |

This plan modifies no files; it is a verification gate. No new inputs, no changed auth/RLS boundary, no new network calls — the entire point is to confirm the existing path is unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | live confirm-Save payload | mitigate | The operator browser Save is the authoritative proof the decomposition did not alter the persisted payload; backed by the Plan 01 snapshot test + turbo check-types. |
| T-10-08 | Tampering | npm/pip/cargo installs | accept | No installs in this plan — verification only; no dependency surface change. |

No high-severity threats introduced. This plan is the regression gate itself.
</threat_model>

<verification>
- Task 1 automated gate green: `turbo check-types` + `cd apps/admin && npx vitest run` exit 0; barrel re-exports both symbols; consumer grep returns only `AdminJobShell.tsx:13`.
- Operator checkpoint: a real `needs_review` job Save lands (job -> completed, data persisted) with no visual/behavioral difference. This is the authoritative behavior-preservation gate (D-11), blocking-human (operator-only).
</verification>

<success_criteria>
- The full automated regression gate is green on the fully decomposed tree.
- The operator confirms a real in-browser Save lands and the editor behaves identically to before the refactor.
- RFCT-04 closed: `ReviewDishEditor.tsx` decomposed, behavior-preserving, payload byte-identical, single submit, verified live.
</success_criteria>

<output>
Create `.planning/phases/10-admin-editor-refactor/10-03-SUMMARY.md` when done.
</output>
