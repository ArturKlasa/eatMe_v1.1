{
  "name": "@eatme/tokens",
  "version": "0.1.0",
  "description": "Platform-agnostic design tokens for EatMe — colors, typography, spacing, borderRadius, shadows",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "generate:css": "tsx scripts/generate-css-vars.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "culori": "^4.0.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.2"
  }
}
